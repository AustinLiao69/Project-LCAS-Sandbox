
# 8806. TDD_P006_首頁儀表板_v1.0.0

## 文件資訊
- **文件標題**: P006 首頁儀表板技術設計文件
- **文件版本**: v1.0.0
- **創建日期**: 2025-08-06 20:00:00 +08:00 (台灣時間)
- **創建者**: LCAS SA Team
- **最後更新**: 2025-08-06 20:00:00 +08:00 (台灣時間)
- **對應需求文件**: P006_首頁儀表板_SRS.md
- **對應PRD文件**: 9005. Flutter_Presentation layer.md
- **頁面代碼**: P006 (Dashboard Page)

---

## 📑 目次 (Table of Contents)

1. [技術架構概述](#10-技術架構概述)
2. [Widget清單](#20-widget清單)
3. [核心Widget設計](#30-核心widget設計)
4. [狀態管理架構](#40-狀態管理架構)
5. [API整合設計](#50-api整合設計)
6. [導航路由設計](#60-導航路由設計)
7. [響應式設計](#70-響應式設計)
8. [無障礙功能](#80-無障礙功能)
9. [效能最佳化](#90-效能最佳化)
10. [錯誤處理機制](#100-錯誤處理機制)

---

## 1.0 技術架構概述

### 1.1 模組定位
P006首頁儀表板作為LCAS 2.0 Flutter應用程式的主要入口，負責提供個人化的記帳資料總覽、快速記帳功能入口、重要財務指標展示及四種使用模式的差異化體驗。本頁面採用Clean Architecture設計，整合多個API服務提供完整的財務儀表板功能。

### 1.2 技術堆疊
- **前端框架**: Flutter 3.x
- **狀態管理**: Provider + ChangeNotifier
- **圖表視覺化**: FL Chart + SyncFusion Charts
- **HTTP客戶端**: Dio
- **本地儲存**: SharedPreferences + Hive
- **即時更新**: WebSocket + Server-Sent Events
- **快取策略**: CacheManager
- **下拉刷新**: RefreshIndicator + Pull-to-Refresh

### 1.3 模組架構
```dart
/**
 * P006_首頁儀表板_1.0.0
 * @module P006首頁儀表板
 * @description Flutter Presentation Layer主要入口頁面 - 財務總覽、快速記帳、智慧建議、四模式差異化
 * @update 2025-08-06: 升級至v1.0版本，新增完整儀表板功能設計，支援多帳本、預算追蹤、協作提醒
 */
```

---

## 2.0 Widget清單

### 2.1 完整Widget索引表

| # | Widget名稱 | 版本 | 所屬層級 | 功能描述 | 相依組件 |
|---|------------|------|----------|----------|----------|
| 01 | DashboardPage | V1.0.0 | 頁面容器層 | 主儀表板頁面容器及生命週期管理 | DashboardProvider、Router |
| 02 | DashboardScaffold | V1.0.0 | 佈局結構層 | 基礎頁面結構與下拉刷新 | RefreshIndicator、ScrollView |
| 03 | DashboardHeader | V1.0.0 | 標頭區塊層 | 使用者問候與通知入口 | UserGreeting、NotificationBadge |
| 04 | FinancialOverviewCard | V1.0.0 | 財務總覽層 | 財務概況展示卡片 | BalanceDisplay、OverviewGrid |
| 05 | BalanceDisplay | V1.0.0 | 餘額展示層 | 當前餘額與變化顯示 | AnimatedCounter、TrendIndicator |
| 06 | OverviewGrid | V1.0.0 | 統計網格層 | 收入支出統計網格 | OverviewItem、GridView |
| 07 | QuickExpenseButton | V1.0.0 | 快速記帳層 | 浮動快速記帳按鈕 | FloatingActionButton、QuickEntryModal |
| 08 | QuickEntryModal | V1.0.0 | 快速輸入層 | 快速記帳彈出視窗 | EntryForm、CategorySelector |
| 09 | SmartSuggestionsCard | V1.0.0 | 智慧建議層 | AI智慧建議卡片 | SuggestionItem、AnimatedList |
| 10 | SuggestionItem | V1.0.0 | 建議項目層 | 單一智慧建議項目 | IconButton、ActionButton |
| 11 | BudgetProgressSection | V1.0.0 | 預算進度層 | 預算執行進度區塊 | BudgetProgressBar、BudgetItem |
| 12 | BudgetProgressBar | V1.0.0 | 進度條層 | 預算進度條元件 | LinearProgressIndicator、AnimatedContainer |
| 13 | RecentTransactionsCard | V1.0.0 | 近期交易層 | 近期交易記錄卡片 | TransactionItem、LoadMoreButton |
| 14 | TransactionItem | V1.0.0 | 交易項目層 | 單一交易記錄項目 | TransactionIcon、AmountText |
| 15 | StatisticsChartsCard | V1.0.0 | 統計圖表層 | 統計圖表展示卡片 | PieChart、LineChart、BarChart |
| 16 | PieChart | V1.0.0 | 圓餅圖層 | 支出分類圓餅圖 | FL Chart、ChartLegend |
| 17 | TrendChart | V1.0.0 | 趨勢圖層 | 收支趨勢折線圖 | FL Chart、TimeAxisLabels |
| 18 | CollaborationNotifications | V1.0.0 | 協作通知層 | 協作帳本通知區塊 | NotificationBadge、ActivityList |
| 19 | LedgerSwitcher | V1.0.0 | 帳本切換層 | 多帳本切換元件 | Dropdown、LedgerIcon |
| 20 | ModeBasedCustomization | V1.0.0 | 模式客製化層 | 四模式差異化顯示 | ModeDetector、LayoutAdapter |

---

## 3.0 核心Widget設計

### 3.1 頁面容器層Widget

#### 3.1.1 DashboardPage Widget
```dart
/**
 * 01. 對應 2.1：主儀表板頁面容器Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 儀表板頁面的主容器，支援多API整合與四模式差異化
 */
class DashboardPage extends StatefulWidget {
  static const String routeName = '/dashboard';
  final UserMode? userMode;
  final String? initialLedgerId;

  const DashboardPage({Key? key, this.userMode, this.initialLedgerId});

  @override
  State<DashboardPage> createState();
}

/**
 * 02. 對應 2.1：儀表板頁面狀態類別
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 管理儀表板頁面的狀態和多個動畫控制器
 */
class _DashboardPageState extends State<DashboardPage> 
    with TickerProviderStateMixin {
  late AnimationController _fadeController;
  late AnimationController _slideController;
  late AnimationController _scaleController;
  late Animation<double> _fadeAnimation;
  late Animation<Offset> _slideAnimation;
  late Animation<double> _scaleAnimation;

  @override
  void initState();

  /**
   * 03. 對應 2.1：初始化動畫控制器群組
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 設定頁面進入動畫效果群組
   */
  void _initializeAnimations();

  /**
   * 04. 對應 2.1：初始化儀表板資料
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 調用Provider載入儀表板所需的多種資料
   */
  Future<void> _initializeDashboard();

  /**
   * 05. 對應 2.1：設定定時刷新
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 設定儀表板資料的定時自動刷新
   */
  void _setupPeriodicRefresh();

  @override
  Widget build(BuildContext context);

  @override
  void dispose();
}
```

### 3.2 佈局結構層Widget

#### 3.2.1 DashboardScaffold Widget
```dart
/**
 * 06. 對應 2.2：儀表板頁面佈局結構Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 提供儀表板頁面的基礎佈局結構，支援下拉刷新與四模式差異化背景
 */
class DashboardScaffold extends StatelessWidget {
  final Animation<double> fadeAnimation;
  final Animation<Offset> slideAnimation;
  final DashboardProvider provider;
  final UserMode? userMode;
  final VoidCallback onRefresh;

  const DashboardScaffold({
    Key? key,
    required this.fadeAnimation,
    required this.slideAnimation,
    required this.provider,
    this.userMode,
    required this.onRefresh,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 07. 對應 2.2：建立儀表板主內容
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據模式建立對應的儀表板主要內容
   */
  Widget _buildDashboardContent(BuildContext context, DashboardProvider provider);

  /**
   * 08. 對應 2.2：建立載入狀態指示器
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 顯示儀表板資料載入狀態
   */
  Widget _buildLoadingIndicator(BuildContext context);
}
```

### 3.3 標頭區塊層Widget

#### 3.3.1 DashboardHeader Widget
```dart
/**
 * 09. 對應 3.1：儀表板標頭區塊Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 顯示使用者問候、帳本選擇、通知入口，支援四模式差異化樣式
 */
class DashboardHeader extends StatelessWidget {
  final UserMode? userMode;
  final String currentLedgerName;
  final int notificationCount;
  final VoidCallback onLedgerSwitch;
  final VoidCallback onNotificationTap;

  const DashboardHeader({
    Key? key,
    this.userMode,
    required this.currentLedgerName,
    required this.notificationCount,
    required this.onLedgerSwitch,
    required this.onNotificationTap,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 10. 對應 3.1：取得模式對應標頭樣式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者模式返回對應的標頭展示樣式
   */
  HeaderStyle _getHeaderStyleForMode(UserMode? mode);

  /**
   * 11. 對應 3.1：取得當前時間問候語
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據當前時間返回適當的問候語
   */
  String _getTimeBasedGreeting();
}
```

### 3.4 財務總覽層Widget

#### 3.4.1 FinancialOverviewCard Widget
```dart
/**
 * 12. 對應 3.2：財務總覽卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 顯示財務概況，包含餘額、收支統計、變化趨勢
 */
class FinancialOverviewCard extends StatefulWidget {
  final FinancialOverview overview;
  final UserMode? userMode;
  final String timeRange;
  final VoidCallback? onDetailTap;

  const FinancialOverviewCard({
    Key? key,
    required this.overview,
    this.userMode,
    required this.timeRange,
    this.onDetailTap,
  });

  @override
  State<FinancialOverviewCard> createState();
}

class _FinancialOverviewCardState extends State<FinancialOverviewCard>
    with SingleTickerProviderStateMixin {
  late AnimationController _controller;
  late Animation<double> _fadeAnimation;
  late Animation<Offset> _slideAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 13. 對應 3.2：建立餘額顯示區塊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立當前餘額與變化的顯示區塊
   */
  Widget _buildBalanceSection();

  /**
   * 14. 對應 3.2：建立統計網格
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立收入支出統計的網格顯示
   */
  Widget _buildStatisticsGrid();

  @override
  void dispose();
}
```

#### 3.4.2 BalanceDisplay Widget
```dart
/**
 * 15. 對應 3.2.1：餘額顯示Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 動畫顯示當前餘額與變化趨勢
 */
class BalanceDisplay extends StatefulWidget {
  final double currentBalance;
  final double balanceChange;
  final double changePercentage;
  final String currency;
  final UserMode? userMode;

  const BalanceDisplay({
    Key? key,
    required this.currentBalance,
    required this.balanceChange,
    required this.changePercentage,
    required this.currency,
    this.userMode,
  });

  @override
  State<BalanceDisplay> createState();
}

class _BalanceDisplayState extends State<BalanceDisplay>
    with TickerProviderStateMixin {
  late AnimationController _counterController;
  late Animation<double> _counterAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 16. 對應 3.2.1：建立趨勢指示器
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立餘額變化的視覺趨勢指示器
   */
  Widget _buildTrendIndicator();

  @override
  void dispose();
}
```

### 3.5 快速記帳層Widget

#### 3.5.1 QuickExpenseButton Widget
```dart
/**
 * 17. 對應 3.3：快速記帳按鈕Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 浮動快速記帳按鈕，支援四模式差異化樣式
 */
class QuickExpenseButton extends StatefulWidget {
  final VoidCallback onPressed;
  final UserMode? userMode;
  final bool isVisible;

  const QuickExpenseButton({
    Key? key,
    required this.onPressed,
    this.userMode,
    this.isVisible = true,
  });

  @override
  State<QuickExpenseButton> createState();
}

class _QuickExpenseButtonState extends State<QuickExpenseButton>
    with SingleTickerProviderStateMixin {
  late AnimationController _pulseController;
  late Animation<double> _pulseAnimation;
  late Animation<double> _scaleAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 18. 對應 3.3：取得模式對應按鈕樣式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據模式類型返回對應的快速記帳按鈕樣式
   */
  QuickButtonStyle _getButtonStyleForMode(UserMode? mode);

  @override
  void dispose();
}
```

#### 3.5.2 QuickEntryModal Widget
```dart
/**
 * 19. 對應 3.3.1：快速記帳彈出視窗Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 快速記帳的彈出表單視窗
 */
class QuickEntryModal extends StatefulWidget {
  final String currentLedgerId;
  final UserMode? userMode;
  final Function(QuickEntryData) onSubmit;
  final VoidCallback onCancel;

  const QuickEntryModal({
    Key? key,
    required this.currentLedgerId,
    this.userMode,
    required this.onSubmit,
    required this.onCancel,
  });

  @override
  State<QuickEntryModal> createState();
}

class _QuickEntryModalState extends State<QuickEntryModal> {
  final GlobalKey<FormState> _formKey = GlobalKey<FormState>();
  late TextEditingController _amountController;
  late TextEditingController _descriptionController;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 20. 對應 3.3.1：建立科目選擇器
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立快速科目選擇器
   */
  Widget _buildCategorySelector();

  /**
   * 21. 對應 3.3.1：提交快速記帳
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 執行快速記帳提交
   */
  Future<void> _submitQuickEntry();

  @override
  void dispose();
}
```

### 3.6 智慧建議層Widget

#### 3.6.1 SmartSuggestionsCard Widget
```dart
/**
 * 22. 對應 3.4：智慧建議卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description AI智慧建議卡片，提供個人化財務建議
 */
class SmartSuggestionsCard extends StatefulWidget {
  final List<SmartSuggestion> suggestions;
  final UserMode? userMode;
  final Function(SmartSuggestion) onSuggestionAction;

  const SmartSuggestionsCard({
    Key? key,
    required this.suggestions,
    this.userMode,
    required this.onSuggestionAction,
  });

  @override
  State<SmartSuggestionsCard> createState();
}

class _SmartSuggestionsCardState extends State<SmartSuggestionsCard>
    with TickerProviderStateMixin {
  late AnimationController _listController;
  late List<Animation<Offset>> _itemAnimations;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 23. 對應 3.4：建立建議項目列表
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立智慧建議項目的動畫列表
   */
  Widget _buildSuggestionsList();

  @override
  void dispose();
}
```

#### 3.6.2 SuggestionItem Widget
```dart
/**
 * 24. 對應 3.4.1：智慧建議項目Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 單一智慧建議項目的顯示與互動
 */
class SuggestionItem extends StatelessWidget {
  final SmartSuggestion suggestion;
  final VoidCallback onAction;
  final VoidCallback onDismiss;

  const SuggestionItem({
    Key? key,
    required this.suggestion,
    required this.onAction,
    required this.onDismiss,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 25. 對應 3.4.1：取得建議類型圖示
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據建議類型返回對應圖示
   */
  IconData _getSuggestionIcon(SuggestionType type);

  /**
   * 26. 對應 3.4.1：取得建議優先級顏色
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據建議優先級返回對應顏色
   */
  Color _getPriorityColor(SuggestionPriority priority);
}
```

### 3.7 預算進度層Widget

#### 3.7.1 BudgetProgressSection Widget
```dart
/**
 * 27. 對應 3.5：預算進度區塊Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 預算執行進度的整體顯示區塊
 */
class BudgetProgressSection extends StatelessWidget {
  final List<BudgetStatus> budgetStatuses;
  final UserMode? userMode;
  final VoidCallback onViewAll;

  const BudgetProgressSection({
    Key? key,
    required this.budgetStatuses,
    this.userMode,
    required this.onViewAll,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 28. 對應 3.5：篩選頂級預算
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 篩選出需要優先顯示的預算項目
   */
  List<BudgetStatus> _getTopPriorityBudgets();
}
```

#### 3.7.2 BudgetProgressBar Widget
```dart
/**
 * 29. 對應 3.5.1：預算進度條Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 單一預算項目的進度條顯示
 */
class BudgetProgressBar extends StatefulWidget {
  final BudgetStatus budgetStatus;
  final bool showDetails;

  const BudgetProgressBar({
    Key? key,
    required this.budgetStatus,
    this.showDetails = true,
  });

  @override
  State<BudgetProgressBar> createState();
}

class _BudgetProgressBarState extends State<BudgetProgressBar>
    with SingleTickerProviderStateMixin {
  late AnimationController _progressController;
  late Animation<double> _progressAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 30. 對應 3.5.1：取得進度條顏色
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據預算執行狀況返回對應進度條顏色
   */
  Color _getProgressColor(double percentage);

  @override
  void dispose();
}
```

### 3.8 近期交易層Widget

#### 3.8.1 RecentTransactionsCard Widget
```dart
/**
 * 31. 對應 3.6：近期交易卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 近期交易記錄的卡片顯示
 */
class RecentTransactionsCard extends StatelessWidget {
  final List<Transaction> transactions;
  final UserMode? userMode;
  final VoidCallback onViewAll;
  final Function(Transaction) onTransactionTap;

  const RecentTransactionsCard({
    Key? key,
    required this.transactions,
    this.userMode,
    required this.onViewAll,
    required this.onTransactionTap,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 32. 對應 3.6：建立交易項目列表
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立近期交易項目的列表顯示
   */
  Widget _buildTransactionsList();
}
```

#### 3.8.2 TransactionItem Widget
```dart
/**
 * 33. 對應 3.6.1：交易項目Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 單一交易記錄項目的顯示
 */
class TransactionItem extends StatelessWidget {
  final Transaction transaction;
  final VoidCallback onTap;
  final bool showDate;

  const TransactionItem({
    Key? key,
    required this.transaction,
    required this.onTap,
    this.showDate = true,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 34. 對應 3.6.1：取得交易類型圖示
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據交易類型返回對應圖示
   */
  IconData _getTransactionIcon(String entryType, String subjectCode);

  /**
   * 35. 對應 3.6.1：格式化金額顯示
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 格式化交易金額的顯示方式
   */
  String _formatAmount(double amount, String entryType);
}
```

### 3.9 統計圖表層Widget

#### 3.9.1 StatisticsChartsCard Widget
```dart
/**
 * 36. 對應 3.7：統計圖表卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 統計圖表的整體卡片容器
 */
class StatisticsChartsCard extends StatefulWidget {
  final ChartData chartData;
  final UserMode? userMode;
  final String selectedPeriod;
  final Function(String) onPeriodChange;

  const StatisticsChartsCard({
    Key? key,
    required this.chartData,
    this.userMode,
    required this.selectedPeriod,
    required this.onPeriodChange,
  });

  @override
  State<StatisticsChartsCard> createState();
}

class _StatisticsChartsCardState extends State<StatisticsChartsCard>
    with TickerProviderStateMixin {
  late TabController _tabController;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 37. 對應 3.7：建立圖表分頁
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立圖表類型的分頁顯示
   */
  Widget _buildChartTabs();

  @override
  void dispose();
}
```

#### 3.9.2 PieChart & TrendChart Widgets
```dart
/**
 * 38. 對應 3.7.1：圓餅圖Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 支出分類圓餅圖顯示
 */
class PieChart extends StatefulWidget {
  final List<PieChartData> data;
  final String title;

  const PieChart({
    Key? key,
    required this.data,
    required this.title,
  });

  @override
  State<PieChart> createState();
}

class _PieChartState extends State<PieChart>
    with SingleTickerProviderStateMixin {
  late AnimationController _animationController;
  late Animation<double> _animation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  @override
  void dispose();
}

/**
 * 39. 對應 3.7.2：趨勢圖Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 收支趨勢折線圖顯示
 */
class TrendChart extends StatefulWidget {
  final List<TrendData> data;
  final String title;
  final String period;

  const TrendChart({
    Key? key,
    required this.data,
    required this.title,
    required this.period,
  });

  @override
  State<TrendChart> createState();
}

class _TrendChartState extends State<TrendChart>
    with SingleTickerProviderStateMixin {
  late AnimationController _animationController;
  late Animation<double> _animation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  @override
  void dispose();
}
```

---

## 4.0 狀態管理架構

### 4.1 DashboardProvider 狀態管理器
```dart
/**
 * 40. 對應 4.1：儀表板頁面狀態管理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 管理儀表板頁面的複雜狀態，支援多API整合和四模式差異化
 */
class DashboardProvider extends ChangeNotifier {
  // 核心狀態屬性
  DashboardState _dashboardState = DashboardState.loading;
  UserMode? _userMode;
  String? _currentLedgerId;
  
  // 財務資料狀態
  FinancialOverview? _financialOverview;
  List<BudgetStatus> _budgetStatuses = [];
  List<Transaction> _recentTransactions = [];
  List<SmartSuggestion> _smartSuggestions = [];
  ChartData? _chartData;
  
  // UI狀態
  bool _isLoading = false;
  bool _isRefreshing = false;
  String? _errorMessage;
  String _selectedTimeRange = 'monthly';
  DateTime _lastRefreshed = DateTime.now();
  
  // 快速記帳狀態
  bool _isQuickEntryVisible = true;
  bool _isQuickEntrySubmitting = false;
  
  // 協作狀態
  List<CollaborationNotification> _collaborationNotifications = [];

  // Getters
  DashboardState get dashboardState;
  UserMode? get userMode;
  String? get currentLedgerId;
  FinancialOverview? get financialOverview;
  List<BudgetStatus> get budgetStatuses;
  List<Transaction> get recentTransactions;
  List<SmartSuggestion> get smartSuggestions;
  ChartData? get chartData;
  bool get isLoading;
  bool get isRefreshing;
  String? get errorMessage;
  String get selectedTimeRange;
  DateTime get lastRefreshed;
  bool get isQuickEntryVisible;
  bool get isQuickEntrySubmitting;
  List<CollaborationNotification> get collaborationNotifications;

  /**
   * 41. 對應 4.1：初始化儀表板
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 初始化儀表板狀態和載入所有必要資料
   */
  Future<void> initializeDashboard(UserMode? mode, String? ledgerId);

  /**
   * 42. 對應 4.1：載入財務總覽資料
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 載入財務總覽相關資料
   */
  Future<void> loadFinancialOverview();

  /**
   * 43. 對應 4.1：載入預算狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 載入所有預算的執行狀態
   */
  Future<void> loadBudgetStatuses();

  /**
   * 44. 對應 4.1：載入近期交易
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 載入近期交易記錄
   */
  Future<void> loadRecentTransactions();

  /**
   * 45. 對應 4.1：載入智慧建議
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 載入AI智慧建議
   */
  Future<void> loadSmartSuggestions();

  /**
   * 46. 對應 4.1：載入圖表資料
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 載入統計圖表資料
   */
  Future<void> loadChartData();

  /**
   * 47. 對應 4.1：載入協作通知
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 載入協作相關通知
   */
  Future<void> loadCollaborationNotifications();

  /**
   * 48. 對應 4.1：刷新所有資料
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 刷新儀表板所有資料
   */
  Future<void> refreshAllData();

  /**
   * 49. 對應 4.1：執行快速記帳
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 執行快速記帳並更新相關資料
   */
  Future<void> submitQuickEntry(QuickEntryData entryData);

  /**
   * 50. 對應 4.1：切換帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 切換當前帳本並重新載入資料
   */
  Future<void> switchLedger(String ledgerId);

  /**
   * 51. 對應 4.1：更新時間範圍
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新統計時間範圍並重新載入資料
   */
  Future<void> updateTimeRange(String timeRange);

  /**
   * 52. 對應 4.1：處理智慧建議動作
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理智慧建議的動作回應
   */
  Future<void> handleSuggestionAction(SmartSuggestion suggestion);

  /**
   * 53. 對應 4.1：設定載入狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新載入狀態並通知監聽者
   */
  void _setLoading(bool loading);

  /**
   * 54. 對應 4.1：設定刷新狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新刷新狀態並通知監聽者
   */
  void _setRefreshing(bool refreshing);

  /**
   * 55. 對應 4.1：設定儀表板狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新儀表板狀態並通知監聽者
   */
  void _setDashboardState(DashboardState state);

  /**
   * 56. 對應 4.1：設定錯誤訊息
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 設定錯誤訊息和錯誤類型
   */
  void _setError(String message, dynamic error);

  /**
   * 57. 對應 4.1：清除錯誤狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 清除錯誤狀態並決定後續動作
   */
  void clearError();
}
```

### 4.2 狀態枚舉定義
```dart
/**
 * 58. 對應 4.2：儀表板頁面狀態枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 定義儀表板頁面的各種狀態
 */
enum DashboardState {
  loading,        // 初始載入中
  loaded,         // 載入完成
  refreshing,     // 刷新中
  error,          // 錯誤狀態
  empty,          // 無資料狀態
}

/**
 * 59. 對應 4.2：智慧建議類型枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 智慧建議的類型分類
 */
enum SuggestionType {
  budgetAlert,     // 預算警示
  spendingTrend,   // 支出趨勢
  savingTip,       // 節省建議
  categoryInsight, // 分類洞察
  goalProgress,    // 目標進度
  collaboration,   // 協作提醒
}

/**
 * 60. 對應 4.2：建議優先級枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 智慧建議的優先級分類
 */
enum SuggestionPriority {
  high,      // 高優先級
  medium,    // 中優先級
  low,       // 低優先級
}
```

---

## 5.0 API整合設計

### 5.1 DashboardApiService API服務
```dart
/**
 * 61. 對應 5.1：儀表板頁面API服務
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 處理儀表板頁面相關的API調用，支援F015儀表板功能
 */
class DashboardApiService {
  final ApiClient _apiClient = ApiClient();

  /**
   * 62. 對應 5.1：載入儀表板資料
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 載入儀表板資料API調用（對應F015端點）
   */
  Future<ApiResponse<DashboardData>> loadDashboardData(DashboardRequest request);

  /**
   * 63. 對應 5.1：快速記帳
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 快速記帳API調用（對應F007端點）
   */
  Future<ApiResponse<EntryResponse>> quickEntry(QuickEntryRequest request);

  /**
   * 64. 對應 5.1：取得預算進度
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 取得預算進度API調用（對應F017端點）
   */
  Future<ApiResponse<BudgetProgressResponse>> getBudgetProgress(String ledgerId);

  /**
   * 65. 對應 5.1：取得智慧建議
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 取得智慧建議API調用
   */
  Future<ApiResponse<SuggestionsResponse>> getSmartSuggestions(String ledgerId);

  /**
   * 66. 對應 5.1：刷新儀表板
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 刷新儀表板資料API調用
   */
  Future<ApiResponse<RefreshResponse>> refreshDashboard(RefreshRequest request);
}
```

### 5.2 資料模型定義
```dart
/**
 * 67. 對應 5.2：儀表板請求模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 儀表板資料請求資料結構
 */
class DashboardRequest {
  final String userId;
  final String ledgerId;
  final String timeRange;
  final List<String> components;

  DashboardRequest({
    required this.userId,
    required this.ledgerId,
    required this.timeRange,
    required this.components,
  });

  Map<String, dynamic> toJson();
}

/**
 * 68. 對應 5.2：儀表板回應模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 儀表板資料回應資料結構
 */
class DashboardData {
  final bool success;
  final FinancialOverview financialOverview;
  final List<BudgetStatus> budgetStatuses;
  final List<Transaction> recentTransactions;
  final List<SmartSuggestion> smartSuggestions;
  final ChartData chartData;

  DashboardData({
    required this.success,
    required this.financialOverview,
    required this.budgetStatuses,
    required this.recentTransactions,
    required this.smartSuggestions,
    required this.chartData,
  });

  factory DashboardData.fromJson(Map<String, dynamic> json);
  Map<String, dynamic> toJson();
}

/**
 * 69. 對應 5.2：快速記帳請求模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 快速記帳請求資料結構
 */
class QuickEntryRequest {
  final String entryType;
  final double amount;
  final String subjectCode;
  final String ledgerId;
  final DateTime entryDate;
  final String? description;

  QuickEntryRequest({
    required this.entryType,
    required this.amount,
    required this.subjectCode,
    required this.ledgerId,
    required this.entryDate,
    this.description,
  });

  Map<String, dynamic> toJson();
}
```

---

## 6.0 導航路由設計

### 6.1 路由配置
```dart
/**
 * 70. 對應 6.1：儀表板頁面路由配置
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 定義儀表板頁面的路由規則與導航邏輯
 */
class DashboardRoutes {
  static const String dashboard = '/dashboard';
  static const String quickEntry = '/quick-entry';
  static const String budgetDetail = '/budget/{id}';
  static const String transactionDetail = '/transaction/{id}';
  static const String statistics = '/statistics';

  /**
   * 71. 對應 6.1：取得儀表板頁面路由
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立儀表板頁面的GoRoute配置
   */
  static GoRoute getDashboardRoute();
}

/**
 * 72. 對應 6.1：儀表板導航服務
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 處理儀表板相關的導航邏輯
 */
class DashboardNavigationService {
  static final GoRouter _router;
  static GoRouter get router;

  /**
   * 73. 對應 6.1：導航到預算詳情
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 導航到預算詳情頁面
   */
  Future<void> navigateToBudgetDetail(String budgetId);

  /**
   * 74. 對應 6.1：導航到交易詳情
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 導航到交易詳情頁面
   */
  Future<void> navigateToTransactionDetail(String transactionId);

  /**
   * 75. 對應 6.1：導航到統計頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 導航到統計分析頁面
   */
  Future<void> navigateToStatistics();

  /**
   * 76. 對應 6.1：導航到記帳歷史
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 導航到記帳歷史頁面
   */
  Future<void> navigateToEntryHistory();
}
```

---

## 7.0 響應式設計

### 7.1 響應式輔助工具
```dart
/**
 * 77. 對應 7.1：儀表板頁面響應式設計輔助工具
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 遵循9005文件的響應式設計規範，提供儀表板頁面專用的佈局管理
 */
class DashboardResponsiveHelper {
  final BuildContext context;
  late final MediaQueryData _mediaQuery;
  late final Size _screenSize;

  DashboardResponsiveHelper.of(BuildContext context);

  // 斷點判斷屬性（9005規範）
  bool get isMobile;
  bool get isTablet;
  bool get isSmallDesktop;
  bool get isLargeDesktop;
  DeviceType get deviceType;

  // 儀表板專用尺寸屬性
  double get cardSpacing;
  double get cardPadding;
  double get cardRadius;
  EdgeInsets get pageMargin;
  double get chartHeight;
  double get quickButtonSize;
  int get gridColumns;

  /**
   * 78. 對應 7.1：取得四模式卡片佈局
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者模式和裝置類型返回適當卡片佈局
   */
  GridLayoutConfig getCardLayoutForMode(UserMode? mode);

  /**
   * 79. 對應 7.1：取得圖表適應尺寸
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據螢幕尺寸返回適當圖表尺寸
   */
  ChartSize getAdaptiveChartSize();

  /**
   * 80. 對應 7.1：是否顯示側邊欄
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 判斷當前螢幕是否適合顯示側邊欄
   */
  bool shouldShowSidebar();
}
```

---

## 8.0 無障礙功能

### 8.1 語意化支援
```dart
/**
 * 81. 對應 8.1：儀表板頁面無障礙語意化Widget包裝器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 提供WCAG 2.1 AA標準的完整無障礙支援（9005規範）
 */
class DashboardAccessibilityWrapper {

  /**
   * 82. 對應 8.1：為財務卡片提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為財務總覽卡片添加適當的語意化標籤
   */
  static Widget wrapFinancialCard(Widget child, FinancialOverview overview);

  /**
   * 83. 對應 8.1：為圖表提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為統計圖表添加適當的語意化標籤
   */
  static Widget wrapChart(Widget child, ChartType type, String description);

  /**
   * 84. 對應 8.1：為快速記帳按鈕提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為快速記帳按鈕添加適當的語意化標籤
   */
  static Widget wrapQuickEntryButton(Widget child, bool isLoading);

  /**
   * 85. 對應 8.1：為智慧建議提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為智慧建議項目添加適當的語意化標籤
   */
  static Widget wrapSuggestion(Widget child, SmartSuggestion suggestion);
}
```

---

## 9.0 效能最佳化

### 9.1 Widget快取策略
```dart
/**
 * 86. 對應 9.1：儀表板頁面Widget快取管理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 優化儀表板頁面Widget重建效能，減少不必要的重建
 */
class DashboardWidgetCache {
  static final Map<String, Widget> _cache = {};
  static final Map<String, DateTime> _cacheTimestamps = {};
  static const Duration _cacheValidDuration = Duration(minutes: 5);

  /**
   * 87. 對應 9.1：取得快取的圖表或建立新的
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 從快取中獲取圖表Widget或創建新的
   */
  static Widget getChartOrCreate(String chartKey, Widget Function() creator);

  /**
   * 88. 對應 9.1：取得快取的統計卡片或建立新的
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 從快取中獲取統計卡片Widget或創建新的
   */
  static Widget getStatisticsCardOrCreate(String cardKey, Widget Function() creator);

  /**
   * 89. 對應 9.1：清除儀表板快取
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 清除儀表板相關的Widget快取
   */
  static void clearDashboardCache();
}
```

### 9.2 資料載入優化
```dart
/**
 * 90. 對應 9.2：儀表板資料載入優化器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 優化儀表板資料載入效能，提升使用者體驗
 */
class DashboardLoadingOptimizer {
  static final Map<String, Timer> _debounceTimers = {};
  static const Duration _debounceDelay = Duration(milliseconds: 300);

  /**
   * 91. 對應 9.2：並行載入儀表板資料
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 並行載入多個API資料源以提升載入速度
   */
  static Future<DashboardData> parallelLoadDashboardData(String ledgerId);

  /**
   * 92. 對應 9.2：分批載入非關鍵資料
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 先載入關鍵資料，再分批載入非關鍵資料
   */
  static Future<void> batchLoadSecondaryData(String ledgerId);

  /**
   * 93. 對應 9.2：智慧預載入
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者行為預載入可能需要的資料
   */
  static Future<void> preloadPredictiveData(UserBehaviorPattern pattern);
}
```

---

## 10.0 錯誤處理機制

### 10.1 統一錯誤處理
```dart
/**
 * 94. 對應 10.1：儀表板頁面錯誤處理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 處理儀表板頁面的各種錯誤情境
 */
class DashboardErrorHandler {

  /**
   * 95. 對應 10.1：處理API載入錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理儀表板API載入錯誤
   */
  static String handleDashboardLoadError(dynamic error);

  /**
   * 96. 對應 10.1：處理快速記帳錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理快速記帳錯誤
   */
  static String handleQuickEntryError(dynamic error);

  /**
   * 97. 對應 10.1：處理圖表載入錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理統計圖表載入錯誤
   */
  static String handleChartLoadError(dynamic error);

  /**
   * 98. 對應 10.1：顯示友善錯誤訊息
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 顯示使用者友善的錯誤訊息
   */
  static Future<void> showFriendlyErrorMessage(
    BuildContext context,
    String message,
    DashboardErrorType errorType,
    {VoidCallback? onRetry}
  );

  /**
   * 99. 對應 10.1：處理離線狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理儀表板離線狀態顯示
   */
  static Widget buildOfflineDashboard({VoidCallback? onRetry});

  /**
   * 100. 對應 10.1：錯誤恢復策略
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 實施儀表板錯誤的自動恢復策略
   */
  static Future<bool> attemptErrorRecovery(DashboardErrorType errorType);
}
```

---

## 遵循0055設計原則

### 階層架構對應
- 本TDD文件基於P006_首頁儀表板_SRS.md的需求規格
- 與9005. Flutter_Presentation layer.md的產品規格保持一致
- 所有函數節點編號對應上層文件的功能條目

### 設計原則遵循
1. **階層架構邏輯**: 嚴格基於SRS文件定義的功能條目設計
2. **同層級依賴關係**: 考量與登入、註冊等頁面的依賴關係
3. **代碼節點編號**: 採用不加前綴的節點編號格式（如2.1、3.2）
4. **命名一致性**: 保持與使用者介面操作流程的名稱同步
5. **可讀性設計**: 採用模組化設計，遵循低耦合、高內聚原則

## 文件維護記錄

| 版本 | 日期 | 修改者 | 修改內容 |
|------|------|--------|----------|
| **v1.0.0** | **2025-08-06 20:00:00 +08:00** | **LCAS SA Team** | **初版建立：完整P006首頁儀表板TDD規格，支援多API整合（財務總覽、預算進度、近期交易、智慧建議、統計圖表），實作四模式差異化設計，完整響應式佈局、無障礙支援、效能優化，遵循0055設計原則，對應P006 SRS規格** |

---

## 備註

- **純函數宣告**: 完全移除所有實作代碼，僅保留函數名稱、參數定義和註解
- **多API整合**: 支援財務總覽、預算進度、智慧建議、統計圖表等多個API服務整合
- **四模式差異化**: 根據使用者模式提供不同的儀表板佈局複雜度和功能顯示
- **即時更新**: 完整的下拉刷新、定時更新、WebSocket即時同步機制
- **完整技術架構**: Widget分層、狀態管理、API整合、響應式設計、無障礙支援、效能最佳化
- **規範完全對齊**: 與0055、P006 SRS、9005文件完全一致
- **節點編號標準化**: 統一編號01-100，遵循0055規範格式
- **版本統一升級**: 所有組件版本統一為v1.0.0
- **圖表視覺化**: 整合FL Chart與SyncFusion Charts提供豐富的統計圖表顯示
- **智慧建議**: 支援AI驅動的個人化財務建議功能
- **協作支援**: 整合協作帳本的通知與提醒功能
