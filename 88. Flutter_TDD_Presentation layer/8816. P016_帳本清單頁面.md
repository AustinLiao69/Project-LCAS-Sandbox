
# 8816. TDD_P016_帳本清單頁面_v1.0.0

## 文件資訊
- **文件標題**: P016 帳本清單頁面技術設計文件
- **文件版本**: v1.0.0
- **創建日期**: 2025-08-06 22:00:00 +08:00 (台灣時間)
- **創建者**: LCAS SA Team
- **最後更新**: 2025-08-06 22:00:00 +08:00 (台灣時間)
- **對應需求文件**: P016_帳本清單頁面_SRS.md
- **對應PRD文件**: 9005. Flutter_Presentation layer.md
- **頁面代碼**: P016 (Ledger List Page)

---

## 📑 目次 (Table of Contents)

1. [技術架構概述](#10-技術架構概述)
2. [Widget清單](#20-widget清單)
3. [核心Widget設計](#30-核心widget設計)
4. [狀態管理架構](#40-狀態管理架構)
5. [API整合設計](#50-api整合設計)
6. [導航路由設計](#60-導航路由設計)
7. [響應式設計](#70-響應式設計)
8. [無障礙功能](#80-無障礙功能)
9. [效能最佳化](#90-效能最佳化)
10. [錯誤處理機制](#100-錯誤處理機制)

---

## 1.0 技術架構概述

### 1.1 模組定位
P016帳本清單頁面作為LCAS 2.0 Flutter應用程式的多帳本管理中心，負責帳本統一檢視、分類管理、批量操作、狀態監控及快速帳本操作。本頁面採用Clean Architecture設計，確保高效能與可擴展性。

### 1.2 技術堆疊
- **前端框架**: Flutter 3.x
- **狀態管理**: Provider + ChangeNotifier
- **路由管理**: GoRouter
- **HTTP客戶端**: Dio
- **本地儲存**: SharedPreferences + Hive
- **無限滾動**: flutter_staggered_grid_view
- **下拉重新整理**: pull_to_refresh
- **搜尋功能**: flutter_search_bar

### 1.3 模組架構
```dart
/**
 * P016_帳本清單頁面_1.0.0
 * @module P016帳本清單頁面
 * @description Flutter Presentation Layer多帳本管理中心 - 帳本清單檢視、分類管理、批量操作、狀態監控
 * @update 2025-08-06: 升級至v1.0版本，完整帳本清單管理功能設計，支援多維度篩選、批量操作、狀態同步
 */
```

---

## 2.0 Widget清單

### 2.1 完整Widget索引表

| # | Widget名稱 | 版本 | 所屬層級 | 功能描述 | 相依組件 |
|---|------------|------|----------|----------|----------|
| 01 | LedgerListPage | V1.0.0 | 頁面容器層 | 主帳本清單頁面容器及生命週期管理 | LedgerListProvider、Router |
| 02 | LedgerListScaffold | V1.0.0 | 佈局結構層 | 基礎頁面結構與導航設定 | AppBar、RefreshIndicator |
| 03 | LedgerListHeader | V1.0.0 | 頁面標頭層 | 標題、統計資訊與檢視切換 | StatisticsCard、ViewModeToggle |
| 04 | ViewModeToggle | V1.0.0 | 檢視控制層 | 列表/卡片/分類檢視切換 | ToggleButtons、AnimatedSwitcher |
| 05 | SearchAndFilterBar | V1.0.0 | 搜尋篩選層 | 搜尋欄與篩選選項 | SearchTextField、FilterDropdown |
| 06 | StatisticsOverview | V1.0.0 | 統計資訊層 | 帳本統計概覽 | StatCard、BalanceDisplay |
| 07 | LedgerListContent | V1.0.0 | 內容容器層 | 帳本清單內容容器 | ListView、GridView、StaggeredGrid |
| 08 | LedgerListView | V1.0.0 | 清單檢視層 | 清單模式帳本顯示 | LedgerListItem、InfiniteScroll |
| 09 | LedgerGridView | V1.0.0 | 網格檢視層 | 卡片模式帳本顯示 | LedgerCard、StaggeredGridView |
| 10 | LedgerCategoryView | V1.0.0 | 分類檢視層 | 分類模式帳本顯示 | CategorySection、LedgerGroup |
| 11 | LedgerListItem | V1.0.0 | 清單項目層 | 單一帳本清單項目 | ListTile、StatusIndicator |
| 12 | LedgerCard | V1.0.0 | 卡片項目層 | 單一帳本卡片項目 | Card、LedgerInfo |
| 13 | LedgerInfo | V1.0.0 | 帳本資訊層 | 帳本基本資訊顯示 | LedgerTitle、LedgerStats |
| 14 | LedgerStatusIndicator | V1.0.0 | 狀態指示層 | 帳本同步狀態指示器 | StatusIcon、SyncProgress |
| 15 | LedgerActionMenu | V1.0.0 | 動作選單層 | 帳本操作選單 | PopupMenuButton、MenuItem |
| 16 | CreateLedgerButton | V1.0.0 | 建立按鈕層 | 建立新帳本浮動按鈕 | FloatingActionButton、SpeedDial |
| 17 | BatchActionBar | V1.0.0 | 批量操作層 | 批量選擇操作欄 | SelectionControls、ActionButtons |
| 18 | FilterDialog | V1.0.0 | 篩選對話層 | 進階篩選對話框 | BottomSheet、FilterOptions |
| 19 | SortDialog | V1.0.0 | 排序對話層 | 排序選項對話框 | SimpleDialog、SortOption |
| 20 | LedgerDeleteDialog | V1.0.0 | 刪除確認層 | 帳本刪除確認對話框 | AlertDialog、ConfirmationButtons |
| 21 | EmptyStateWidget | V1.0.0 | 空狀態層 | 無帳本時的空狀態顯示 | EmptyIllustration、CreatePrompt |
| 22 | LoadingStateWidget | V1.0.0 | 載入狀態層 | 帳本載入狀態顯示 | SkeletonLoader、LinearProgressIndicator |
| 23 | ErrorStateWidget | V1.0.0 | 錯誤狀態層 | 載入錯誤狀態顯示 | ErrorMessage、RetryButton |
| 24 | PullToRefreshWrapper | V1.0.0 | 重新整理層 | 下拉重新整理包裝器 | RefreshIndicator、CustomRefresh |

---

## 3.0 核心Widget設計

### 3.1 頁面容器層Widget

#### 3.1.1 LedgerListPage Widget
```dart
/**
 * 01. 對應 2.1：主帳本清單頁面容器Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 帳本清單頁面的主容器，支援多種檢視模式與四模式差異化
 */
class LedgerListPage extends StatefulWidget {
  static const String routeName = '/ledger-list';
  final UserMode? userMode;
  final String? initialFilter;

  const LedgerListPage({
    Key? key,
    this.userMode,
    this.initialFilter,
  });

  @override
  State<LedgerListPage> createState();
}

/**
 * 02. 對應 2.1：帳本清單頁面狀態類別
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 管理帳本清單頁面的狀態和生命週期
 */
class _LedgerListPageState extends State<LedgerListPage>
    with TickerProviderStateMixin {
  late TabController _tabController;
  late ScrollController _scrollController;
  late TextEditingController _searchController;

  @override
  void initState();

  /**
   * 03. 對應 2.1：初始化控制器
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 初始化各種控制器
   */
  void _initializeControllers();

  /**
   * 04. 對應 2.1：初始化頁面數據
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 載入帳本清單數據
   */
  Future<void> _initializeData();

  @override
  Widget build(BuildContext context);

  @override
  void dispose();
}
```

### 3.2 佈局結構層Widget

#### 3.2.1 LedgerListScaffold Widget
```dart
/**
 * 05. 對應 2.2：帳本清單頁面佈局結構Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 提供帳本清單頁面的基礎佈局結構，支援四模式差異化佈局
 */
class LedgerListScaffold extends StatelessWidget {
  final LedgerListProvider provider;
  final ScrollController scrollController;
  final UserMode? userMode;
  final VoidCallback? onRefresh;

  const LedgerListScaffold({
    Key? key,
    required this.provider,
    required this.scrollController,
    this.userMode,
    this.onRefresh,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 06. 對應 2.2：建立應用欄
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立帳本清單頁面的應用欄
   */
  PreferredSizeWidget _buildAppBar(BuildContext context, LedgerListProvider provider);

  /**
   * 07. 對應 2.2：建立浮動操作按鈕
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立建立新帳本的浮動操作按鈕
   */
  Widget? _buildFloatingActionButton(BuildContext context, LedgerListProvider provider);
}
```

### 3.3 頁面標頭層Widget

#### 3.3.1 LedgerListHeader Widget
```dart
/**
 * 08. 對應 3.1：帳本清單標頭Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 顯示統計資訊、檢視切換與搜尋篩選，支援四模式差異化
 */
class LedgerListHeader extends StatelessWidget {
  final LedgerListProvider provider;
  final UserMode? userMode;
  final VoidCallback? onViewModeChanged;
  final ValueChanged<String>? onSearchChanged;

  const LedgerListHeader({
    Key? key,
    required this.provider,
    this.userMode,
    this.onViewModeChanged,
    this.onSearchChanged,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 09. 對應 3.1：取得模式對應標頭配置
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 根據使用者模式返回對應的標頭配置
   */
  HeaderConfig _getHeaderConfigForMode(UserMode? mode);
}
```

### 3.4 檢視控制層Widget

#### 3.4.1 ViewModeToggle Widget
```dart
/**
 * 10. 對應 3.2：檢視模式切換Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 列表、卡片、分類檢視模式切換控制
 */
class ViewModeToggle extends StatefulWidget {
  final ViewMode currentMode;
  final ValueChanged<ViewMode> onModeChanged;
  final UserMode? userMode;

  const ViewModeToggle({
    Key? key,
    required this.currentMode,
    required this.onModeChanged,
    this.userMode,
  });

  @override
  State<ViewModeToggle> createState();
}

class _ViewModeToggleState extends State<ViewModeToggle>
    with SingleTickerProviderStateMixin {
  late AnimationController _animationController;
  late Animation<double> _fadeAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 11. 對應 3.2：取得模式可用檢視類型
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 根據使用者模式返回可用的檢視類型
   */
  List<ViewMode> _getAvailableViewModesForMode(UserMode? mode);

  @override
  void dispose();
}
```

### 3.5 搜尋篩選層Widget

#### 3.5.1 SearchAndFilterBar Widget
```dart
/**
 * 12. 對應 3.3：搜尋與篩選欄Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 搜尋欄與篩選選項，支援智能搜尋與多維度篩選
 */
class SearchAndFilterBar extends StatefulWidget {
  final String searchQuery;
  final FilterOptions filterOptions;
  final ValueChanged<String> onSearchChanged;
  final ValueChanged<FilterOptions> onFilterChanged;
  final UserMode? userMode;

  const SearchAndFilterBar({
    Key? key,
    required this.searchQuery,
    required this.filterOptions,
    required this.onSearchChanged,
    required this.onFilterChanged,
    this.userMode,
  });

  @override
  State<SearchAndFilterBar> createState();
}

class _SearchAndFilterBarState extends State<SearchAndFilterBar> {
  late TextEditingController _searchController;
  Timer? _searchDebounce;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 13. 對應 3.3：處理搜尋輸入防抖
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 實作搜尋輸入防抖機制
   */
  void _onSearchChanged(String query);

  /**
   * 14. 對應 3.3：顯示進階篩選對話框
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 顯示進階篩選選項對話框
   */
  Future<void> _showAdvancedFilter();

  @override
  void dispose();
}
```

### 3.6 統計資訊層Widget

#### 3.6.1 StatisticsOverview Widget
```dart
/**
 * 15. 對應 3.4：統計概覽Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 帳本統計資訊概覽，包含總數、餘額、活躍度等
 */
class StatisticsOverview extends StatelessWidget {
  final LedgerStatistics statistics;
  final UserMode? userMode;
  final bool isCompact;

  const StatisticsOverview({
    Key? key,
    required this.statistics,
    this.userMode,
    this.isCompact = false,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 16. 對應 3.4：取得模式對應統計項目
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 根據使用者模式返回應顯示的統計項目
   */
  List<StatisticsItem> _getStatisticsItemsForMode(UserMode? mode);
}
```

### 3.7 內容容器層Widget

#### 3.7.1 LedgerListContent Widget
```dart
/**
 * 17. 對應 4.1：帳本清單內容容器Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 根據檢視模式顯示不同的帳本清單內容
 */
class LedgerListContent extends StatelessWidget {
  final List<LedgerModel> ledgers;
  final ViewMode viewMode;
  final ScrollController scrollController;
  final bool isLoading;
  final bool hasMore;
  final VoidCallback? onLoadMore;
  final ValueChanged<LedgerModel>? onLedgerTap;
  final ValueChanged<LedgerModel>? onLedgerLongPress;

  const LedgerListContent({
    Key? key,
    required this.ledgers,
    required this.viewMode,
    required this.scrollController,
    this.isLoading = false,
    this.hasMore = true,
    this.onLoadMore,
    this.onLedgerTap,
    this.onLedgerLongPress,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 18. 對應 4.1：建立檢視模式對應內容
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 根據檢視模式建立對應的內容Widget
   */
  Widget _buildViewModeContent(BuildContext context);
}
```

### 3.8 清單檢視層Widget

#### 3.8.1 LedgerListView Widget
```dart
/**
 * 19. 對應 4.2：帳本清單檢視Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 列表模式的帳本顯示，支援無限滾動與下拉重新整理
 */
class LedgerListView extends StatefulWidget {
  final List<LedgerModel> ledgers;
  final ScrollController scrollController;
  final bool isLoading;
  final bool hasMore;
  final VoidCallback? onLoadMore;
  final ValueChanged<LedgerModel>? onLedgerTap;
  final ValueChanged<LedgerModel>? onLedgerLongPress;

  const LedgerListView({
    Key? key,
    required this.ledgers,
    required this.scrollController,
    this.isLoading = false,
    this.hasMore = true,
    this.onLoadMore,
    this.onLedgerTap,
    this.onLedgerLongPress,
  });

  @override
  State<LedgerListView> createState();
}

class _LedgerListViewState extends State<LedgerListView> {
  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 20. 對應 4.2：檢查是否需要載入更多
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 監聽滾動位置並觸發載入更多數據
   */
  void _checkForLoadMore();

  /**
   * 21. 對應 4.2：建立載入更多指示器
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立載入更多數據的指示器
   */
  Widget _buildLoadMoreIndicator();
}
```

#### 3.8.2 LedgerListItem Widget
```dart
/**
 * 22. 對應 4.2.1：帳本清單項目Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 單一帳本的清單項目顯示
 */
class LedgerListItem extends StatefulWidget {
  final LedgerModel ledger;
  final bool isSelected;
  final VoidCallback? onTap;
  final VoidCallback? onLongPress;
  final ValueChanged<bool>? onSelectionChanged;

  const LedgerListItem({
    Key? key,
    required this.ledger,
    this.isSelected = false,
    this.onTap,
    this.onLongPress,
    this.onSelectionChanged,
  });

  @override
  State<LedgerListItem> createState();
}

class _LedgerListItemState extends State<LedgerListItem>
    with SingleTickerProviderStateMixin {
  late AnimationController _animationController;
  late Animation<double> _scaleAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 23. 對應 4.2.1：建立帳本資訊顯示
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立帳本基本資訊的顯示內容
   */
  Widget _buildLedgerInfo(BuildContext context);

  /**
   * 24. 對應 4.2.1：建立帳本統計顯示
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立帳本統計資訊的顯示內容
   */
  Widget _buildLedgerStats(BuildContext context);

  @override
  void dispose();
}
```

### 3.9 網格檢視層Widget

#### 3.9.1 LedgerGridView Widget
```dart
/**
 * 25. 對應 4.3：帳本網格檢視Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 卡片模式的帳本顯示，支援響應式網格佈局
 */
class LedgerGridView extends StatefulWidget {
  final List<LedgerModel> ledgers;
  final ScrollController scrollController;
  final int crossAxisCount;
  final bool isLoading;
  final bool hasMore;
  final VoidCallback? onLoadMore;
  final ValueChanged<LedgerModel>? onLedgerTap;
  final ValueChanged<LedgerModel>? onLedgerLongPress;

  const LedgerGridView({
    Key? key,
    required this.ledgers,
    required this.scrollController,
    this.crossAxisCount = 2,
    this.isLoading = false,
    this.hasMore = true,
    this.onLoadMore,
    this.onLedgerTap,
    this.onLedgerLongPress,
  });

  @override
  State<LedgerGridView> createState();
}

class _LedgerGridViewState extends State<LedgerGridView> {
  @override
  Widget build(BuildContext context);

  /**
   * 26. 對應 4.3：建立網格項目
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立網格中的單一帳本卡片
   */
  Widget _buildGridItem(BuildContext context, int index);
}
```

#### 3.9.2 LedgerCard Widget
```dart
/**
 * 27. 對應 4.3.1：帳本卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 單一帳本的卡片顯示
 */
class LedgerCard extends StatefulWidget {
  final LedgerModel ledger;
  final bool isSelected;
  final VoidCallback? onTap;
  final VoidCallback? onLongPress;
  final ValueChanged<bool>? onSelectionChanged;

  const LedgerCard({
    Key? key,
    required this.ledger,
    this.isSelected = false,
    this.onTap,
    this.onLongPress,
    this.onSelectionChanged,
  });

  @override
  State<LedgerCard> createState();
}

class _LedgerCardState extends State<LedgerCard>
    with SingleTickerProviderStateMixin {
  late AnimationController _animationController;
  late Animation<double> _elevationAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 28. 對應 4.3.1：建立卡片頭部
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立帳本卡片的頭部區域
   */
  Widget _buildCardHeader(BuildContext context);

  /**
   * 29. 對應 4.3.1：建立卡片內容
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立帳本卡片的內容區域
   */
  Widget _buildCardContent(BuildContext context);

  /**
   * 30. 對應 4.3.1：建立卡片底部
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立帳本卡片的底部區域
   */
  Widget _buildCardFooter(BuildContext context);

  @override
  void dispose();
}
```

---

## 4.0 狀態管理架構

### 4.1 LedgerListProvider 狀態管理器
```dart
/**
 * 31. 對應 4.1：帳本清單頁面狀態管理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 管理帳本清單頁面的狀態，支援多維度篩選、批量操作與同步管理
 */
class LedgerListProvider extends ChangeNotifier {
  // 狀態屬性
  LedgerListPageState _pageState = LedgerListPageState.initial;
  ViewMode _viewMode = ViewMode.list;
  List<LedgerModel> _allLedgers = [];
  List<LedgerModel> _filteredLedgers = [];
  Set<String> _selectedLedgers = {};
  LedgerStatistics? _statistics;
  String _searchQuery = '';
  FilterOptions _filterOptions = FilterOptions.defaults();
  SortOptions _sortOptions = SortOptions.defaults();
  bool _isLoading = false;
  bool _isRefreshing = false;
  bool _hasMore = true;
  int _currentPage = 0;
  String? _errorMessage;
  UserMode? _userMode;

  // Getters
  LedgerListPageState get pageState;
  ViewMode get viewMode;
  List<LedgerModel> get allLedgers;
  List<LedgerModel> get filteredLedgers;
  Set<String> get selectedLedgers;
  LedgerStatistics? get statistics;
  String get searchQuery;
  FilterOptions get filterOptions;
  SortOptions get sortOptions;
  bool get isLoading;
  bool get isRefreshing;
  bool get hasMore;
  int get currentPage;
  String? get errorMessage;
  UserMode? get userMode;
  bool get isInSelectionMode;
  bool get canCreateLedger;

  /**
   * 32. 對應 4.1：初始化帳本清單頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 初始化頁面狀態並載入帳本清單
   */
  Future<void> initializePage(UserMode? mode);

  /**
   * 33. 對應 4.1：載入帳本清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 載入帳本清單數據
   */
  Future<void> loadLedgers({bool refresh = false});

  /**
   * 34. 對應 4.1：載入更多帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 載入下一頁帳本數據
   */
  Future<void> loadMoreLedgers();

  /**
   * 35. 對應 4.1：重新整理帳本清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 重新整理帳本清單數據
   */
  Future<void> refreshLedgers();

  /**
   * 36. 對應 4.1：設定搜尋查詢
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 更新搜尋查詢並篩選帳本
   */
  void setSearchQuery(String query);

  /**
   * 37. 對應 4.1：設定篩選選項
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 更新篩選選項並重新篩選帳本
   */
  void setFilterOptions(FilterOptions options);

  /**
   * 38. 對應 4.1：設定排序選項
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 更新排序選項並重新排序帳本
   */
  void setSortOptions(SortOptions options);

  /**
   * 39. 對應 4.1：切換檢視模式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 切換帳本清單的檢視模式
   */
  void setViewMode(ViewMode mode);

  /**
   * 40. 對應 4.1：切換帳本選擇狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 切換指定帳本的選擇狀態
   */
  void toggleLedgerSelection(String ledgerId);

  /**
   * 41. 對應 4.1：全選/取消全選帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 全選或取消全選所有可見帳本
   */
  void toggleSelectAll();

  /**
   * 42. 對應 4.1：清除選擇狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 清除所有帳本的選擇狀態
   */
  void clearSelection();

  /**
   * 43. 對應 4.1：批量歸檔帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 批量歸檔選中的帳本
   */
  Future<void> batchArchiveLedgers(List<String> ledgerIds);

  /**
   * 44. 對應 4.1：批量刪除帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 批量刪除選中的帳本
   */
  Future<void> batchDeleteLedgers(List<String> ledgerIds);

  /**
   * 45. 對應 4.1：篩選與排序帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 根據搜尋、篩選、排序條件處理帳本清單
   */
  void _filterAndSortLedgers();

  /**
   * 46. 對應 4.1：計算統計資訊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 計算帳本清單的統計資訊
   */
  LedgerStatistics _calculateStatistics(List<LedgerModel> ledgers);

  /**
   * 47. 對應 4.1：設定載入狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 更新載入狀態並通知監聽者
   */
  void _setLoading(bool loading);

  /**
   * 48. 對應 4.1：設定頁面狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 更新頁面狀態並通知監聽者
   */
  void _setPageState(LedgerListPageState state);

  /**
   * 49. 對應 4.1：設定錯誤訊息
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 設定錯誤訊息並更新頁面狀態
   */
  void _setError(String message, dynamic error);

  /**
   * 50. 對應 4.1：清除錯誤狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 清除錯誤狀態並恢復正常顯示
   */
  void clearError();
}
```

### 4.2 狀態枚舉定義
```dart
/**
 * 51. 對應 4.2：帳本清單頁面狀態枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 定義帳本清單頁面的各種狀態
 */
enum LedgerListPageState {
  initial,        // 初始狀態
  loading,        // 載入中
  loaded,         // 已載入
  refreshing,     // 重新整理中
  loadingMore,    // 載入更多中
  empty,          // 空狀態
  error,          // 錯誤狀態
}

/**
 * 52. 對應 4.2：檢視模式枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 帳本清單的檢視模式
 */
enum ViewMode {
  list,       // 列表檢視
  card,       // 卡片檢視
  category,   // 分類檢視
}

/**
 * 53. 對應 4.2：帳本類型枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 帳本類型分類
 */
enum LedgerType {
  personal,     // 個人帳本
  shared,       // 協作帳本
  project,      // 專案帳本
  category,     // 分類帳本
}

/**
 * 54. 對應 4.2：同步狀態枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 帳本同步狀態
 */
enum SyncStatus {
  synced,       // 已同步
  syncing,      // 同步中
  conflict,     // 衝突
  error,        // 錯誤
  offline,      // 離線
}
```

---

## 5.0 API整合設計

### 5.1 LedgerListApiService API服務
```dart
/**
 * 55. 對應 5.1：帳本清單頁面API服務
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 處理帳本清單頁面相關的API調用，整合ProjectLedgerService功能
 */
class LedgerListApiService {
  final ApiClient _apiClient = ApiClient();

  /**
   * 56. 對應 5.1：獲取帳本清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 獲取使用者帳本清單API調用
   */
  Future<ApiResponse<LedgerListResponse>> getLedgers(LedgerListRequest request);

  /**
   * 57. 對應 5.1：建立新帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立新帳本API調用
   */
  Future<ApiResponse<LedgerCreateResponse>> createLedger(LedgerCreateRequest request);

  /**
   * 58. 對應 5.1：批量歸檔帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 批量歸檔帳本API調用
   */
  Future<ApiResponse<BatchOperationResponse>> batchArchiveLedgers(BatchArchiveRequest request);

  /**
   * 59. 對應 5.1：批量刪除帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 批量刪除帳本API調用
   */
  Future<ApiResponse<BatchOperationResponse>> batchDeleteLedgers(BatchDeleteRequest request);

  /**
   * 60. 對應 5.1：同步帳本狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 同步帳本狀態API調用
   */
  Future<ApiResponse<SyncStatusResponse>> syncLedgerStatus(String ledgerId);

  /**
   * 61. 對應 5.1：獲取帳本統計
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 獲取帳本統計資訊API調用
   */
  Future<ApiResponse<LedgerStatisticsResponse>> getLedgerStatistics();

  /**
   * 62. 對應 5.1：處理Dio錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 處理Dio錯誤並轉換為使用者友善訊息
   */
  String _handleDioError(DioError error);
}
```

### 5.2 資料模型定義
```dart
/**
 * 63. 對應 5.2：帳本清單請求模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 帳本清單請求資料結構
 */
class LedgerListRequest {
  final String? searchQuery;
  final List<LedgerType>? types;
  final List<SyncStatus>? syncStatuses;
  final SortBy sortBy;
  final SortOrder sortOrder;
  final int page;
  final int limit;
  final bool includeArchived;

  LedgerListRequest({
    this.searchQuery,
    this.types,
    this.syncStatuses,
    required this.sortBy,
    required this.sortOrder,
    required this.page,
    required this.limit,
    this.includeArchived = false,
  });

  Map<String, dynamic> toJson();
}

/**
 * 64. 對應 5.2：帳本清單回應模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 帳本清單回應資料結構
 */
class LedgerListResponse {
  final bool success;
  final List<LedgerModel> ledgers;
  final LedgerStatistics statistics;
  final PaginationInfo pagination;
  final String message;

  LedgerListResponse({
    required this.success,
    required this.ledgers,
    required this.statistics,
    required this.pagination,
    required this.message,
  });

  factory LedgerListResponse.fromJson(Map<String, dynamic> json);
}

/**
 * 65. 對應 5.2：帳本模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 帳本基本資料結構
 */
class LedgerModel {
  final String id;
  final String name;
  final String? description;
  final LedgerType type;
  final String currency;
  final bool isPrivate;
  final String ownerId;
  final List<MemberInfo> members;
  final double totalBalance;
  final int entryCount;
  final SyncStatus syncStatus;
  final DateTime createdAt;
  final DateTime updatedAt;
  final DateTime? lastUsedAt;
  final bool isArchived;

  LedgerModel({
    required this.id,
    required this.name,
    this.description,
    required this.type,
    required this.currency,
    required this.isPrivate,
    required this.ownerId,
    required this.members,
    required this.totalBalance,
    required this.entryCount,
    required this.syncStatus,
    required this.createdAt,
    required this.updatedAt,
    this.lastUsedAt,
    required this.isArchived,
  });

  factory LedgerModel.fromJson(Map<String, dynamic> json);
  Map<String, dynamic> toJson();
}
```

---

## 6.0 導航路由設計

### 6.1 路由配置
```dart
/**
 * 66. 對應 6.1：帳本清單頁面路由配置
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 定義帳本清單頁面的路由規則與導航邏輯
 */
class LedgerListRoutes {
  static const String ledgerList = '/ledger-list';
  static const String createLedger = '/create-ledger';
  static const String ledgerSettings = '/ledger-settings';
  static const String ledgerDetail = '/ledger-detail';

  /**
   * 67. 對應 6.1：取得帳本清單頁面路由
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 建立帳本清單頁面的GoRoute配置
   */
  static GoRoute getLedgerListRoute();
}

/**
 * 68. 對應 6.1：帳本清單導航服務
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 處理帳本清單相關的導航邏輯
 */
class LedgerListNavigationService {
  static final GoRouter _router;
  static GoRouter get router;

  /**
   * 69. 對應 6.1：導航到建立帳本頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 導航到建立新帳本頁面
   */
  Future<void> navigateToCreateLedger();

  /**
   * 70. 對應 6.1：導航到帳本詳情頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 導航到指定帳本的詳情頁面
   */
  Future<void> navigateToLedgerDetail(String ledgerId);

  /**
   * 71. 對應 6.1：導航到帳本設定頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 導航到帳本設定頁面
   */
  Future<void> navigateToLedgerSettings(String ledgerId);

  /**
   * 72. 對應 6.1：返回首頁
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 返回到首頁儀表板
   */
  void navigateToHome();
}
```

---

## 7.0 響應式設計

### 7.1 響應式輔助工具
```dart
/**
 * 73. 對應 7.1：帳本清單頁面響應式設計輔助工具
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 遵循9005文件的響應式設計規範，提供帳本清單頁面專用的佈局管理
 */
class LedgerListResponsiveHelper {
  final BuildContext context;
  late final MediaQueryData _mediaQuery;
  late final Size _screenSize;

  LedgerListResponsiveHelper.of(BuildContext context);

  // 斷點判斷屬性（9005規範）
  bool get isMobile;
  bool get isTablet;
  bool get isSmallDesktop;
  bool get isLargeDesktop;
  DeviceType get deviceType;

  // 帳本清單專用尺寸屬性
  int get gridCrossAxisCount;
  double get listItemHeight;
  double get cardWidth;
  double get cardHeight;
  EdgeInsets get listPadding;
  double get searchBarHeight;

  /**
   * 74. 對應 7.1：取得四模式佈局配置
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 根據使用者模式和裝置類型返回適當佈局配置
   */
  LayoutConfig getLayoutConfigForMode(UserMode? mode);

  /**
   * 75. 對應 7.1：取得模式對應項目尺寸
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 根據使用者模式返回適當項目尺寸
   */
  ItemSize getItemSizeForMode(UserMode? mode, ViewMode viewMode);
}
```

---

## 8.0 無障礙功能

### 8.1 語意化支援
```dart
/**
 * 76. 對應 8.1：帳本清單頁面無障礙語意化Widget包裝器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 提供WCAG 2.1 AA標準的完整無障礙支援（9005規範）
 */
class LedgerListAccessibilityWrapper {

  /**
   * 77. 對應 8.1：為帳本項目提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 為帳本清單項目添加適當的語意化標籤
   */
  static Widget wrapLedgerItem(Widget child, LedgerModel ledger, bool isSelected);

  /**
   * 78. 對應 8.1：為操作按鈕提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 為各種操作按鈕添加適當的語意化標籤
   */
  static Widget wrapActionButton(Widget child, String actionType, {bool isEnabled = true});

  /**
   * 79. 對應 8.1：為搜尋欄位提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 為搜尋和篩選欄位添加適當的語意化標籤
   */
  static Widget wrapSearchField(Widget child, String fieldType);

  /**
   * 80. 對應 8.1：為統計資訊提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 為統計資訊顯示添加適當的語意化標籤
   */
  static Widget wrapStatistics(Widget child, LedgerStatistics statistics);
}
```

---

## 9.0 效能最佳化

### 9.1 Widget快取策略
```dart
/**
 * 81. 對應 9.1：帳本清單頁面Widget快取管理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 優化帳本清單頁面Widget重建效能，減少不必要的重建
 */
class LedgerListWidgetCache {
  static final Map<String, Widget> _cache = {};
  static final Map<String, DateTime> _cacheTimestamps = {};
  static const Duration _cacheValidDuration = Duration(minutes: 5);

  /**
   * 82. 對應 9.1：取得快取的帳本項目或建立新的
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 從快取中獲取帳本項目Widget或創建新的
   */
  static Widget getLedgerItemOrCreate(String ledgerId, Widget Function() creator);

  /**
   * 83. 對應 9.1：取得快取的統計資訊或建立新的
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 從快取中獲取統計資訊Widget或創建新的
   */
  static Widget getStatisticsOrCreate(String key, Widget Function() creator);

  /**
   * 84. 對應 9.1：清除帳本清單快取
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 清除帳本清單相關的Widget快取
   */
  static void clearLedgerListCache();
}
```

### 9.2 數據載入優化
```dart
/**
 * 85. 對應 9.2：帳本清單數據載入優化器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 優化帳本清單數據載入效能，提升使用者體驗
 */
class LedgerListLoadingOptimizer {
  static const Duration _debounceDelay = Duration(milliseconds: 300);
  static Timer? _searchDebounceTimer;

  /**
   * 86. 對應 9.2：防抖動搜尋
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 防止使用者快速輸入造成重複搜尋請求
   */
  static void debounceSearch(String query, Function(String) onSearch);

  /**
   * 87. 對應 9.2：預載入帳本圖標
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 預載入帳本類型圖標和狀態圖標
   */
  static Future<void> preloadLedgerIcons(BuildContext context);

  /**
   * 88. 對應 9.2：優化無限滾動
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 優化無限滾動載入機制
   */
  static bool shouldLoadMore(ScrollController controller, {double threshold = 200});
}
```

---

## 10.0 錯誤處理機制

### 10.1 統一錯誤處理
```dart
/**
 * 89. 對應 10.1：帳本清單頁面錯誤處理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 根據P016_帳本清單頁面_SRS.md規範的具體錯誤情境進行處理
 */
class LedgerListErrorHandler {

  /**
   * 90. 對應 10.1：處理載入帳本錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 處理載入帳本清單錯誤
   */
  static String handleLoadLedgersError(dynamic error);

  /**
   * 91. 對應 10.1：處理建立帳本錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 處理建立新帳本錯誤
   */
  static String handleCreateLedgerError(dynamic error);

  /**
   * 92. 對應 10.1：處理批量操作錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 處理批量歸檔或刪除錯誤
   */
  static String handleBatchOperationError(dynamic error);

  /**
   * 93. 對應 10.1：處理同步錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 處理帳本同步錯誤
   */
  static String handleSyncError(dynamic error);

  /**
   * 94. 對應 10.1：顯示錯誤對話框
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 顯示錯誤訊息對話框
   */
  static Future<void> showErrorDialog(
    BuildContext context,
    String message,
    LedgerListErrorType errorType,
    {VoidCallback? onRetry, VoidCallback? onDismiss}
  );

  /**
   * 95. 對應 10.1：處理網路連線錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 處理網路連線錯誤
   */
  static String handleNetworkError(DioError error);

  /**
   * 96. 對應 10.1：處理權限錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 處理帳本操作權限錯誤
   */
  static String handlePermissionError(dynamic error);
}
```

### 10.2 錯誤復原機制
```dart
/**
 * 97. 對應 10.2：帳本清單錯誤復原策略
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 22:00:00
 * @description 提供帳本清單各種錯誤復原機制
 */
class LedgerListErrorRecovery {

  /**
   * 98. 對應 10.2：自動重試載入機制
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 針對網路錯誤等臨時性問題的自動重試機制
   */
  static Future<T> retryLoadLedgers<T>(
    Future<T> Function() loadOperation, {
    int maxRetries = 3,
    Duration initialDelay = const Duration(seconds: 1),
  });

  /**
   * 99. 對應 10.2：離線狀態處理
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 處理離線狀態的帳本清單界面
   */
  static Widget buildOfflineLedgerListWidget({
    List<LedgerModel>? cachedLedgers,
    VoidCallback? onRetry,
    String? message,
  });

  /**
   * 100. 對應 10.2：恢復本地快取數據
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 22:00:00
   * @description 從本地快取恢復帳本清單數據
   */
  static Future<List<LedgerModel>?> recoverFromCache();
}
```

---

## 遵循0055設計原則

### 階層架構對應
- 本TDD文件基於P016_帳本清單頁面_SRS.md的需求規格
- 與9005. Flutter_Presentation layer.md的產品規格保持一致
- 所有函數節點編號對應上層文件的功能條目

### 設計原則遵循
1. **階層架構邏輯**: 嚴格基於SRS文件定義的功能條目設計
2. **同層級依賴關係**: 考量與8815帳本切換頁面和8812設定頁面的依賴關係
3. **代碼節點編號**: 採用不加前綴的節點編號格式（如2.1、3.2）
4. **命名一致性**: 保持與使用者介面操作流程的名稱同步
5. **可讀性設計**: 採用模組化設計，遵循低耦合、高內聚原則

## 文件維護記錄

| 版本 | 日期 | 修改者 | 修改內容 |
|------|------|--------|----------|
| **v1.0.0** | **2025-08-06 22:00:00 +08:00** | **LCAS SA Team** | **初版建立：完整P016帳本清單頁面TDD規格，支援多維度篩選排序、批量操作管理、狀態同步監控，實作四模式差異化設計，完整錯誤處理機制，遵循0055設計原則，對應P016_帳本清單頁面_SRS規格** |

---

## 備註

- **純函數宣告**: 完全移除所有實作代碼，僅保留函數名稱、參數定義和註解
- **多檢視支援**: 支援列表、卡片、分類三種檢視模式，響應式佈局適配
- **四模式差異化**: 根據使用者模式提供不同的帳本管理界面複雜度和功能
- **效能優先**: 完整的Widget快取、無限滾動、搜尋防抖、數據預載入機制
- **完整技術架構**: Widget分層、狀態管理、API整合、響應式設計、無障礙支援、效能最佳化
- **規範完全對齊**: 與0055、P016_帳本清單頁面_SRS、9005文件完全一致
- **節點編號標準化**: 統一編號01-100，遵循0055規範格式
- **版本統一升級**: 所有組件版本統一為v1.0.0
