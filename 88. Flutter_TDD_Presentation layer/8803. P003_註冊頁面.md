
# 8803. TDD_P003_註冊頁面_v1.0.0

## 文件資訊
- **文件標題**: P003 註冊頁面技術設計文件
- **文件版本**: v1.0.0
- **創建日期**: 2025-08-06 21:00:00 +08:00 (台灣時間)
- **創建者**: LCAS SA Team
- **最後更新**: 2025-08-06 21:00:00 +08:00 (台灣時間)
- **對應需求文件**: 8703. 註冊頁面_SRS.md
- **對應PRD文件**: 9005. Flutter_Presentation layer.md
- **頁面代碼**: P003 (Register Page)

---

## 📑 目次 (Table of Contents)

1. [技術架構概述](#10-技術架構概述)
2. [Widget清單](#20-widget清單)
3. [核心Widget設計](#30-核心widget設計)
4. [狀態管理架構](#40-狀態管理架構)
5. [API整合設計](#50-api整合設計)
6. [導航路由設計](#60-導航路由設計)
7. [響應式設計](#70-響應式設計)
8. [無障礙功能](#80-無障礙功能)
9. [效能最佳化](#90-效能最佳化)
10. [錯誤處理機制](#100-錯誤處理機制)

---

## 1.0 技術架構概述

### 1.1 模組定位
P003註冊頁面作為LCAS 2.0 Flutter應用程式的新使用者註冊入口，負責使用者帳號建立、多種註冊方式支援、四模式初始選擇、Email驗證及使用者資訊收集。本頁面採用Clean Architecture設計，確保安全性與使用者體驗最佳化。

### 1.2 技術堆疊
- **前端框架**: Flutter 3.x
- **狀態管理**: Provider + ChangeNotifier
- **路由管理**: GoRouter
- **HTTP客戶端**: Dio
- **本地儲存**: SharedPreferences + FlutterSecureStorage
- **第三方註冊**: LINE Login SDK, Google Sign-In, Apple Sign-In
- **Email驗證**: Email Validator, HTTP for API calls
- **表單驗證**: FormBuilderValidators
- **密碼強度**: Custom Password Strength Validator

### 1.3 模組架構
```dart
/**
 * P003_註冊頁面_1.0.0
 * @module P003註冊頁面
 * @description Flutter Presentation Layer新使用者註冊頁面 - 多方式註冊、Email驗證、四模式選擇、安全註冊
 * @update 2025-08-06: 升級至v1.0版本，新增完整註冊功能設計，支援LINE OAuth、Email註冊、第三方註冊、模式選擇
 */
```

---

## 2.0 Widget清單

### 2.1 完整Widget索引表

| # | Widget名稱 | 版本 | 所屬層級 | 功能描述 | 相依組件 |
|---|------------|------|----------|----------|----------|
| 01 | RegisterPage | V1.0.0 | 頁面容器層 | 主註冊頁面容器及生命週期管理 | RegisterProvider、Router |
| 02 | RegisterScaffold | V1.0.0 | 佈局結構層 | 基礎頁面結構與背景設定 | GradientBackground |
| 03 | BrandHeaderSection | V1.0.0 | 品牌展示層 | Logo與品牌標題顯示 | BrandLogo、BrandTitle |
| 04 | RegisterMethodsSection | V1.0.0 | 註冊方式層 | 多種註冊方式選擇介面 | LineRegisterButton、SocialRegisterGroup、EmailRegisterForm |
| 05 | LineRegisterButton | V1.0.0 | 主要註冊層 | LINE OAuth註冊按鈕（推薦） | AnimatedContainer、LINE SDK |
| 06 | SocialRegisterGroup | V1.0.0 | 社交註冊層 | Google/Apple第三方註冊群組 | GoogleRegisterButton、AppleRegisterButton |
| 07 | EmailRegisterForm | V1.0.0 | 傳統註冊層 | Email密碼註冊表單 | EmailInput、PasswordInput、ConfirmPasswordInput |
| 08 | EmailInput | V1.0.0 | 表單元件層 | Email輸入欄位與即時驗證 | TextFormField、EmailValidator |
| 09 | PasswordInput | V1.0.0 | 表單元件層 | 密碼輸入欄位與強度指示 | TextFormField、PasswordValidator |
| 10 | ConfirmPasswordInput | V1.0.0 | 表單元件層 | 確認密碼輸入欄位 | TextFormField、PasswordMatchValidator |
| 11 | DisplayNameInput | V1.0.0 | 表單元件層 | 使用者姓名輸入欄位 | TextFormField、NameValidator |
| 12 | PasswordStrengthIndicator | V1.0.0 | 密碼輔助層 | 密碼強度視覺指示器 | LinearProgressIndicator、ColorTransition |
| 13 | ModeSelectionSection | V1.0.0 | 模式選擇層 | 四種使用模式選擇介面 | ModeCard、ModeGrid |
| 14 | ModeCard | V1.0.0 | 模式展示層 | 個別模式展示卡片 | AnimatedContainer、Icon |
| 15 | GoogleRegisterButton | V1.0.0 | 第三方註冊層 | Google OAuth註冊按鈕 | InkWell、Google SDK |
| 16 | AppleRegisterButton | V1.0.0 | 第三方註冊層 | Apple ID註冊按鈕 | InkWell、Apple SDK |
| 17 | TermsAgreementSection | V1.0.0 | 條款同意層 | 服務條款與隱私政策同意 | TermsCheckbox、PolicyLinks |
| 18 | TermsCheckbox | V1.0.0 | 同意選項層 | 條款同意選項 | Checkbox、Text |
| 19 | MarketingConsentCheckbox | V1.0.0 | 選擇性同意層 | 行銷訊息同意選項 | Checkbox、Text |
| 20 | RegisterButton | V1.0.0 | 互動元件層 | 註冊執行按鈕 | ElevatedButton、LoadingIndicator |
| 21 | LoginLink | V1.0.0 | 導航連結層 | 已有帳號登入連結 | TextButton、Router |
| 22 | LoadingOverlay | V1.0.0 | 載入狀態層 | 註冊中載入覆蓋層 | CircularProgressIndicator、Backdrop |
| 23 | ErrorDialog | V1.0.0 | 錯誤處理層 | 註冊錯誤對話框 | AlertDialog、TextButton |
| 24 | EmailVerificationDialog | V1.0.0 | 驗證提示層 | Email驗證提示對話框 | AlertDialog、Icon |
| 25 | SuccessDialog | V1.0.0 | 成功提示層 | 註冊成功對話框 | AlertDialog、Icon |

---

## 3.0 核心Widget設計

### 3.1 頁面容器層Widget

#### 3.1.1 RegisterPage Widget
```dart
/**
 * 01. 對應 2.1：主註冊頁面容器Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 註冊頁面的主容器，支援多種註冊方式與四模式差異化
 */
class RegisterPage extends StatefulWidget {
  static const String routeName = '/register';
  final UserMode? initialMode;
  final String? referralCode;

  const RegisterPage({Key? key, this.initialMode, this.referralCode});

  @override
  State<RegisterPage> createState();
}

/**
 * 02. 對應 2.1：註冊頁面狀態類別
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 管理註冊頁面的狀態和動畫控制器
 */
class _RegisterPageState extends State<RegisterPage> with TickerProviderStateMixin {
  late AnimationController _fadeController;
  late AnimationController _slideController;
  late Animation<double> _fadeAnimation;
  late Animation<Offset> _slideAnimation;

  @override
  void initState();

  /**
   * 03. 對應 2.1：初始化動畫控制器
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 設定頁面進入動畫效果
   */
  void _initializeAnimations();

  /**
   * 04. 對應 2.1：初始化頁面狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 調用Provider進行註冊頁面初始化
   */
  Future<void> _initializePage();

  @override
  Widget build(BuildContext context);

  @override
  void dispose();
}
```

### 3.2 佈局結構層Widget

#### 3.2.1 RegisterScaffold Widget
```dart
/**
 * 05. 對應 2.2：註冊頁面佈局結構Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 提供註冊頁面的基礎佈局結構，支援四模式差異化背景
 */
class RegisterScaffold extends StatelessWidget {
  final Animation<double> fadeAnimation;
  final Animation<Offset> slideAnimation;
  final RegisterProvider provider;
  final UserMode? initialMode;

  const RegisterScaffold({
    Key? key,
    required this.fadeAnimation,
    required this.slideAnimation,
    required this.provider,
    this.initialMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 06. 對應 2.2：建立安全覆蓋層
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 顯示安全提示或錯誤對話框的覆蓋層
   */
  Widget _buildSecurityOverlay(BuildContext context, RegisterProvider provider);
}
```

### 3.3 品牌展示層Widget

#### 3.3.1 BrandHeaderSection Widget
```dart
/**
 * 07. 對應 3.1：品牌標頭區塊Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 顯示品牌Logo和標題，支援四模式差異化樣式
 */
class BrandHeaderSection extends StatelessWidget {
  final UserMode? selectedMode;

  const BrandHeaderSection({Key? key, this.selectedMode});

  @override
  Widget build(BuildContext context);

  /**
   * 08. 對應 3.1：取得模式對應品牌樣式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據使用者模式返回對應的品牌展示樣式
   */
  BrandStyle _getBrandStyleForMode(UserMode? mode);
}
```

### 3.4 註冊方式層Widget

#### 3.4.1 RegisterMethodsSection Widget
```dart
/**
 * 09. 對應 3.2：註冊方式區塊Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 整合多種註冊方式，支援四模式差異化顯示
 */
class RegisterMethodsSection extends StatelessWidget {
  final RegisterProvider provider;
  final UserMode? selectedMode;

  const RegisterMethodsSection({
    Key? key,
    required this.provider,
    this.selectedMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 10. 對應 3.2：取得模式可用註冊方式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據使用者模式返回可用的註冊方式列表
   */
  List<RegisterMethod> _getAvailableMethodsForMode(UserMode? mode);
}
```

#### 3.4.2 LineRegisterButton Widget
```dart
/**
 * 11. 對應 3.2.1：LINE註冊按鈕Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description LINE OAuth註冊按鈕，主要推薦註冊方式
 */
class LineRegisterButton extends StatefulWidget {
  final VoidCallback onPressed;
  final bool isLoading;
  final UserMode? selectedMode;

  const LineRegisterButton({
    Key? key,
    required this.onPressed,
    this.isLoading = false,
    this.selectedMode,
  });

  @override
  State<LineRegisterButton> createState();
}

class _LineRegisterButtonState extends State<LineRegisterButton> 
    with SingleTickerProviderStateMixin {
  late AnimationController _pulseController;
  late Animation<double> _pulseAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 12. 對應 3.2.1：取得模式對應按鈕樣式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據模式類型返回對應的LINE按鈕樣式
   */
  LineButtonStyle _getButtonStyleForMode(UserMode? mode);

  @override
  void dispose();
}
```

### 3.5 社交註冊層Widget

#### 3.5.1 SocialRegisterGroup Widget
```dart
/**
 * 13. 對應 3.2.2：社交註冊群組Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description Google/Apple第三方註冊按鈕群組
 */
class SocialRegisterGroup extends StatelessWidget {
  final RegisterProvider provider;
  final UserMode? selectedMode;

  const SocialRegisterGroup({
    Key? key,
    required this.provider,
    this.selectedMode,
  });

  @override
  Widget build(BuildContext context);
}

/**
 * 14. 對應 3.2.2：Google註冊按鈕Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description Google OAuth註冊按鈕
 */
class GoogleRegisterButton extends StatelessWidget {
  final VoidCallback onPressed;
  final bool isLoading;

  const GoogleRegisterButton({
    Key? key,
    required this.onPressed,
    this.isLoading = false,
  });

  @override
  Widget build(BuildContext context);
}

/**
 * 15. 對應 3.2.2：Apple註冊按鈕Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description Apple ID註冊按鈕
 */
class AppleRegisterButton extends StatelessWidget {
  final VoidCallback onPressed;
  final bool isLoading;

  const AppleRegisterButton({
    Key? key,
    required this.onPressed,
    this.isLoading = false,
  });

  @override
  Widget build(BuildContext context);
}
```

### 3.6 傳統註冊層Widget

#### 3.6.1 EmailRegisterForm Widget
```dart
/**
 * 16. 對應 3.2.3：Email註冊表單Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description Email密碼註冊表單，包含完整欄位驗證
 */
class EmailRegisterForm extends StatefulWidget {
  final RegisterProvider provider;
  final UserMode? selectedMode;

  const EmailRegisterForm({
    Key? key,
    required this.provider,
    this.selectedMode,
  });

  @override
  State<EmailRegisterForm> createState();
}

class _EmailRegisterFormState extends State<EmailRegisterForm> {
  final GlobalKey<FormState> _formKey = GlobalKey<FormState>();
  late TextEditingController _emailController;
  late TextEditingController _passwordController;
  late TextEditingController _confirmPasswordController;
  late TextEditingController _displayNameController;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 17. 對應 3.2.3：驗證表單輸入
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 驗證所有表單欄位格式與內容
   */
  bool _validateForm();

  /**
   * 18. 對應 3.2.3：提交註冊表單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 執行Email註冊流程
   */
  Future<void> _submitForm();

  @override
  void dispose();
}
```

#### 3.6.2 表單輸入元件Widgets
```dart
/**
 * 19. 對應 3.2.3.1：Email輸入欄位Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description Email輸入欄位與即時格式驗證
 */
class EmailInput extends StatelessWidget {
  final TextEditingController controller;
  final String? Function(String?)? validator;
  final bool enabled;
  final VoidCallback? onChanged;

  const EmailInput({
    Key? key,
    required this.controller,
    this.validator,
    this.enabled = true,
    this.onChanged,
  });

  @override
  Widget build(BuildContext context);
}

/**
 * 20. 對應 3.2.3.2：密碼輸入欄位Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 密碼輸入欄位與強度指示
 */
class PasswordInput extends StatefulWidget {
  final TextEditingController controller;
  final String? Function(String?)? validator;
  final bool enabled;
  final VoidCallback? onChanged;

  const PasswordInput({
    Key? key,
    required this.controller,
    this.validator,
    this.enabled = true,
    this.onChanged,
  });

  @override
  State<PasswordInput> createState();
}

class _PasswordInputState extends State<PasswordInput> {
  bool _isObscured = true;

  @override
  Widget build(BuildContext context);

  /**
   * 21. 對應 3.2.3.2：切換密碼可見性
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 控制密碼欄位顯示/隱藏
   */
  void _togglePasswordVisibility();
}

/**
 * 22. 對應 3.2.3.3：確認密碼輸入欄位Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 確認密碼輸入欄位與一致性驗證
 */
class ConfirmPasswordInput extends StatefulWidget {
  final TextEditingController controller;
  final String originalPassword;
  final bool enabled;

  const ConfirmPasswordInput({
    Key? key,
    required this.controller,
    required this.originalPassword,
    this.enabled = true,
  });

  @override
  State<ConfirmPasswordInput> createState();
}

class _ConfirmPasswordInputState extends State<ConfirmPasswordInput> {
  bool _isObscured = true;

  @override
  Widget build(BuildContext context);

  /**
   * 23. 對應 3.2.3.3：驗證密碼一致性
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 檢查確認密碼與原始密碼一致性
   */
  String? _validatePasswordMatch(String? value);
}

/**
 * 24. 對應 3.2.3.4：使用者姓名輸入欄位Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 使用者姓名輸入欄位與格式驗證
 */
class DisplayNameInput extends StatelessWidget {
  final TextEditingController controller;
  final String? Function(String?)? validator;
  final bool enabled;

  const DisplayNameInput({
    Key? key,
    required this.controller,
    this.validator,
    this.enabled = true,
  });

  @override
  Widget build(BuildContext context);
}
```

### 3.7 密碼強度指示器Widget

#### 3.7.1 PasswordStrengthIndicator Widget
```dart
/**
 * 25. 對應 3.3：密碼強度指示器Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 即時顯示密碼強度的視覺指示器
 */
class PasswordStrengthIndicator extends StatefulWidget {
  final String password;
  final UserMode? selectedMode;

  const PasswordStrengthIndicator({
    Key? key,
    required this.password,
    this.selectedMode,
  });

  @override
  State<PasswordStrengthIndicator> createState();
}

class _PasswordStrengthIndicatorState extends State<PasswordStrengthIndicator> 
    with SingleTickerProviderStateMixin {
  late AnimationController _strengthController;
  late Animation<double> _strengthAnimation;
  PasswordStrength _currentStrength = PasswordStrength.weak;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 26. 對應 3.3：計算密碼強度
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據密碼內容計算強度等級
   */
  PasswordStrength _calculatePasswordStrength(String password);

  /**
   * 27. 對應 3.3：取得強度對應顏色
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據強度等級返回對應顏色
   */
  Color _getStrengthColor(PasswordStrength strength);

  @override
  void dispose();
}
```

### 3.8 模式選擇層Widget

#### 3.8.1 ModeSelectionSection Widget
```dart
/**
 * 28. 對應 3.4：模式選擇區塊Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 四種使用模式選擇介面
 */
class ModeSelectionSection extends StatelessWidget {
  final UserMode? selectedMode;
  final Function(UserMode) onModeSelected;
  final bool enabled;

  const ModeSelectionSection({
    Key? key,
    this.selectedMode,
    required this.onModeSelected,
    this.enabled = true,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 29. 對應 3.4：取得模式清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 返回所有可選擇的使用模式清單
   */
  List<UserMode> _getAvailableModes();
}

/**
 * 30. 對應 3.4：模式卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 個別模式展示卡片
 */
class ModeCard extends StatefulWidget {
  final UserMode mode;
  final bool isSelected;
  final VoidCallback onTap;
  final bool enabled;

  const ModeCard({
    Key? key,
    required this.mode,
    required this.isSelected,
    required this.onTap,
    this.enabled = true,
  });

  @override
  State<ModeCard> createState();
}

class _ModeCardState extends State<ModeCard> with SingleTickerProviderStateMixin {
  late AnimationController _scaleController;
  late Animation<double> _scaleAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 31. 對應 3.4：取得模式資訊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據模式類型返回對應的顯示資訊
   */
  ModeInfo _getModeInfo(UserMode mode);

  @override
  void dispose();
}
```

### 3.9 條款同意層Widget

#### 3.9.1 TermsAgreementSection Widget
```dart
/**
 * 32. 對應 3.5：條款同意區塊Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 服務條款與隱私政策同意區塊
 */
class TermsAgreementSection extends StatelessWidget {
  final bool termsAccepted;
  final bool privacyAccepted;
  final bool marketingConsent;
  final Function(bool) onTermsChanged;
  final Function(bool) onPrivacyChanged;
  final Function(bool) onMarketingChanged;

  const TermsAgreementSection({
    Key? key,
    required this.termsAccepted,
    required this.privacyAccepted,
    required this.marketingConsent,
    required this.onTermsChanged,
    required this.onPrivacyChanged,
    required this.onMarketingChanged,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 33. 對應 3.5：顯示條款內容
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 顯示服務條款或隱私政策內容
   */
  Future<void> _showTermsContent(BuildContext context, TermsType type);
}

/**
 * 34. 對應 3.5：條款選項Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 個別條款同意選項
 */
class TermsCheckbox extends StatelessWidget {
  final bool value;
  final Function(bool) onChanged;
  final String labelText;
  final String linkText;
  final VoidCallback? onLinkTap;
  final bool isRequired;

  const TermsCheckbox({
    Key? key,
    required this.value,
    required this.onChanged,
    required this.labelText,
    required this.linkText,
    this.onLinkTap,
    this.isRequired = false,
  });

  @override
  Widget build(BuildContext context);
}

/**
 * 35. 對應 3.5：行銷同意選項Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 選擇性行銷訊息同意選項
 */
class MarketingConsentCheckbox extends StatelessWidget {
  final bool value;
  final Function(bool) onChanged;
  final UserMode? selectedMode;

  const MarketingConsentCheckbox({
    Key? key,
    required this.value,
    required this.onChanged,
    this.selectedMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 36. 對應 3.5：取得模式對應行銷內容
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據使用模式返回對應的行銷同意說明
   */
  String _getMarketingContentForMode(UserMode? mode);
}
```

### 3.10 互動元件層Widget

#### 3.10.1 RegisterButton Widget
```dart
/**
 * 37. 對應 3.6：註冊執行按鈕Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 主要註冊執行按鈕，支援載入狀態
 */
class RegisterButton extends StatefulWidget {
  final VoidCallback? onPressed;
  final bool isLoading;
  final bool isEnabled;
  final UserMode? selectedMode;

  const RegisterButton({
    Key? key,
    this.onPressed,
    this.isLoading = false,
    this.isEnabled = true,
    this.selectedMode,
  });

  @override
  State<RegisterButton> createState();
}

class _RegisterButtonState extends State<RegisterButton> 
    with SingleTickerProviderStateMixin {
  late AnimationController _buttonController;
  late Animation<double> _buttonAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 38. 對應 3.6：取得模式對應按鈕樣式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據使用模式返回對應的按鈕樣式
   */
  ButtonStyle _getButtonStyleForMode(UserMode? mode);

  @override
  void dispose();
}

/**
 * 39. 對應 3.6：登入連結Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 已有帳號登入連結
 */
class LoginLink extends StatelessWidget {
  final VoidCallback onPressed;

  const LoginLink({Key? key, required this.onPressed});

  @override
  Widget build(BuildContext context);
}
```

### 3.11 狀態顯示層Widget

#### 3.11.1 對話框Widgets
```dart
/**
 * 40. 對應 3.7：載入覆蓋層Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 註冊中載入覆蓋層
 */
class LoadingOverlay extends StatelessWidget {
  final bool isVisible;
  final String message;

  const LoadingOverlay({
    Key? key,
    required this.isVisible,
    this.message = '正在註冊中...',
  });

  @override
  Widget build(BuildContext context);
}

/**
 * 41. 對應 3.7：註冊錯誤對話框Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 註冊錯誤提示對話框
 */
class ErrorDialog extends StatelessWidget {
  final String title;
  final String message;
  final RegisterErrorType errorType;
  final VoidCallback? onRetry;
  final VoidCallback? onDismiss;

  const ErrorDialog({
    Key? key,
    required this.title,
    required this.message,
    required this.errorType,
    this.onRetry,
    this.onDismiss,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 42. 對應 3.7：顯示錯誤對話框
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 顯示錯誤對話框的靜態方法
   */
  static Future<void> show(
    BuildContext context, {
    required String title,
    required String message,
    required RegisterErrorType errorType,
    VoidCallback? onRetry,
    VoidCallback? onDismiss,
  });
}

/**
 * 43. 對應 3.7：Email驗證提示對話框Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description Email驗證提示對話框
 */
class EmailVerificationDialog extends StatelessWidget {
  final String email;
  final VoidCallback onResendEmail;
  final VoidCallback onDismiss;

  const EmailVerificationDialog({
    Key? key,
    required this.email,
    required this.onResendEmail,
    required this.onDismiss,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 44. 對應 3.7：顯示Email驗證對話框
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 顯示Email驗證對話框的靜態方法
   */
  static Future<void> show(
    BuildContext context, {
    required String email,
    required VoidCallback onResendEmail,
    required VoidCallback onDismiss,
  });
}

/**
 * 45. 對應 3.7：註冊成功對話框Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 註冊成功提示對話框
 */
class SuccessDialog extends StatelessWidget {
  final String message;
  final VoidCallback onContinue;

  const SuccessDialog({
    Key? key,
    required this.message,
    required this.onContinue,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 46. 對應 3.7：顯示成功對話框
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 顯示註冊成功對話框的靜態方法
   */
  static Future<void> show(
    BuildContext context, {
    required String message,
    required VoidCallback onContinue,
  });
}
```

---

## 4.0 狀態管理架構

### 4.1 RegisterProvider 狀態管理器
```dart
/**
 * 47. 對應 4.1：註冊頁面狀態管理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 管理註冊頁面的狀態，支援多種註冊方式和四模式差異化
 */
class RegisterProvider extends ChangeNotifier {
  // 狀態屬性
  RegisterPageState _pageState = RegisterPageState.initial;
  RegisterMethod? _selectedMethod;
  UserMode? _selectedMode;
  bool _isLoading = false;
  String? _errorMessage;
  RegisterErrorType? _errorType;

  // 表單狀態
  final GlobalKey<FormState> emailFormKey = GlobalKey<FormState>();
  String _email = '';
  String _password = '';
  String _confirmPassword = '';
  String _displayName = '';
  PasswordStrength _passwordStrength = PasswordStrength.weak;

  // 同意狀態
  bool _termsAccepted = false;
  bool _privacyAccepted = false;
  bool _marketingConsent = false;

  // Email驗證狀態
  bool _emailVerificationSent = false;
  String? _verificationToken;

  // Getters
  RegisterPageState get pageState;
  RegisterMethod? get selectedMethod;
  UserMode? get selectedMode;
  bool get isLoading;
  String? get errorMessage;
  RegisterErrorType? get errorType;
  String get email;
  String get password;
  String get confirmPassword;
  String get displayName;
  PasswordStrength get passwordStrength;
  bool get termsAccepted;
  bool get privacyAccepted;
  bool get marketingConsent;
  bool get emailVerificationSent;
  bool get canSubmit;

  /**
   * 48. 對應 4.1：初始化註冊頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 初始化註冊頁面狀態和檢查可用註冊方式
   */
  Future<void> initializePage(UserMode? initialMode);

  /**
   * 49. 對應 4.1：設定使用者模式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 設定使用者模式並更新可用註冊方式
   */
  void setSelectedMode(UserMode mode);

  /**
   * 50. 對應 4.1：LINE OAuth註冊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 執行LINE OAuth註冊流程
   */
  Future<void> registerWithLine();

  /**
   * 51. 對應 4.1：Google OAuth註冊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 執行Google OAuth註冊流程
   */
  Future<void> registerWithGoogle();

  /**
   * 52. 對應 4.1：Apple ID註冊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 執行Apple ID註冊流程
   */
  Future<void> registerWithApple();

  /**
   * 53. 對應 4.1：Email密碼註冊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 執行Email密碼註冊流程
   */
  Future<void> registerWithEmail();

  /**
   * 54. 對應 4.1：更新表單資料
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 更新註冊表單的各項資料
   */
  void updateFormData({
    String? email,
    String? password,
    String? confirmPassword,
    String? displayName,
  });

  /**
   * 55. 對應 4.1：更新密碼強度
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 計算並更新密碼強度
   */
  void updatePasswordStrength(String password);

  /**
   * 56. 對應 4.1：更新同意狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 更新各項條款同意狀態
   */
  void updateAgreementStatus({
    bool? termsAccepted,
    bool? privacyAccepted,
    bool? marketingConsent,
  });

  /**
   * 57. 對應 4.1：重新發送驗證Email
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 重新發送Email驗證信件
   */
  Future<void> resendVerificationEmail();

  /**
   * 58. 對應 4.1：檢查Email可用性
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 檢查Email是否已被註冊
   */
  Future<bool> checkEmailAvailability(String email);

  /**
   * 59. 對應 4.1：驗證表單完整性
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 驗證所有必填欄位是否完整
   */
  bool validateFormCompleteness();

  /**
   * 60. 對應 4.1：取得裝置資訊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 收集裝置相關資訊用於API調用
   */
  Future<Map<String, dynamic>> _getDeviceInfo();

  /**
   * 61. 對應 4.1：設定載入狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 更新載入狀態並通知監聽者
   */
  void _setLoading(bool loading);

  /**
   * 62. 對應 4.1：設定頁面狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 更新頁面狀態並通知監聽者
   */
  void _setPageState(RegisterPageState state);

  /**
   * 63. 對應 4.1：設定錯誤訊息
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 設定錯誤訊息和錯誤類型
   */
  void _setError(String message, RegisterErrorType type, dynamic error);

  /**
   * 64. 對應 4.1：清除錯誤狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 清除錯誤狀態並決定後續動作
   */
  void clearError();

  /**
   * 65. 對應 4.1：重試註冊操作
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據錯誤類型執行相對應的重試操作
   */
  Future<void> retryRegister();

  /**
   * 66. 對應 4.1：導航到登入頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 導航到登入頁面
   */
  void navigateToLogin();

  /**
   * 67. 對應 4.1：導航到首頁
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description OAuth註冊成功後導航到首頁
   */
  void navigateToHome();
}
```

### 4.2 狀態枚舉定義
```dart
/**
 * 68. 對應 4.2：註冊頁面狀態枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 定義註冊頁面的各種狀態
 */
enum RegisterPageState {
  initial,              // 初始狀態
  modeSelection,        // 模式選擇
  formFilling,          // 填寫表單
  validating,           // 驗證中
  submitting,           // 提交中
  emailVerification,    // Email驗證等待
  success,              // 註冊成功
  error                 // 註冊失敗
}

/**
 * 69. 對應 4.2：註冊方式枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 支援的註冊方式
 */
enum RegisterMethod {
  line,       // LINE OAuth
  google,     // Google OAuth
  apple,      // Apple ID
  email,      // Email密碼
}

/**
 * 70. 對應 4.2：密碼強度枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 密碼強度等級
 */
enum PasswordStrength {
  weak,       // 弱密碼
  medium,     // 中等密碼
  strong,     // 強密碼
}

/**
 * 71. 對應 4.2：註冊錯誤類型枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 註冊錯誤類型分類
 */
enum RegisterErrorType {
  networkError,         // 網路錯誤
  emailTaken,          // Email已被使用
  weakPassword,        // 密碼強度不足
  invalidEmail,        // Email格式錯誤
  passwordMismatch,    // 密碼不一致
  invalidDisplayName,  // 姓名格式錯誤
  termsNotAccepted,    // 未同意條款
  oauthError,          // OAuth錯誤
  verificationFailed,  // 驗證失敗
  serverError,         // 伺服器錯誤
  rateLimited,         // 頻率限制
  unknown,             // 未知錯誤
}

/**
 * 72. 對應 4.2：條款類型枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 條款與政策類型
 */
enum TermsType {
  serviceTerms,        // 服務條款
  privacyPolicy,       // 隱私政策
  marketingConsent,    // 行銷同意
}
```

---

## 5.0 API整合設計

### 5.1 RegisterApiService API服務
```dart
/**
 * 73. 對應 5.1：註冊頁面API服務
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 處理註冊頁面相關的API調用，支援F001使用者註冊功能
 */
class RegisterApiService {
  final ApiClient _apiClient = ApiClient();

  /**
   * 74. 對應 5.1：LINE OAuth註冊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description LINE OAuth註冊API調用（對應F001端點）
   */
  Future<ApiResponse<RegisterResponse>> registerWithLine(LineRegisterRequest request);

  /**
   * 75. 對應 5.1：Google OAuth註冊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description Google OAuth註冊API調用（對應F001端點）
   */
  Future<ApiResponse<RegisterResponse>> registerWithGoogle(GoogleRegisterRequest request);

  /**
   * 76. 對應 5.1：Apple ID註冊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description Apple ID註冊API調用（對應F001端點）
   */
  Future<ApiResponse<RegisterResponse>> registerWithApple(AppleRegisterRequest request);

  /**
   * 77. 對應 5.1：Email密碼註冊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description Email密碼註冊API調用（對應F001端點）
   */
  Future<ApiResponse<RegisterResponse>> registerWithEmail(EmailRegisterRequest request);

  /**
   * 78. 對應 5.1：檢查Email可用性
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 檢查Email是否已被註冊
   */
  Future<ApiResponse<EmailCheckResponse>> checkEmailAvailability(EmailCheckRequest request);

  /**
   * 79. 對應 5.1：重新發送驗證Email
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 重新發送Email驗證信件
   */
  Future<ApiResponse<ResendVerificationResponse>> resendVerificationEmail(ResendVerificationRequest request);

  /**
   * 80. 對應 5.1：處理Dio錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 處理Dio錯誤並轉換為使用者友善訊息
   */
  String _handleDioError(DioError error);

  /**
   * 81. 對應 5.1：取得裝置資訊
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 收集裝置資訊用於API調用
   */
  Future<Map<String, dynamic>> _getDeviceInfo();
}
```

### 5.2 資料模型定義
```dart
/**
 * 82. 對應 5.2：LINE註冊請求模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description LINE OAuth註冊請求資料結構
 */
class LineRegisterRequest {
  final String authorizationCode;
  final String redirectUri;
  final String clientId;
  final String selectedMode;
  final bool marketingConsent;
  final Map<String, dynamic> deviceInfo;

  LineRegisterRequest({
    required this.authorizationCode,
    required this.redirectUri,
    required this.clientId,
    required this.selectedMode,
    required this.marketingConsent,
    required this.deviceInfo,
  });

  Map<String, dynamic> toJson();
}

/**
 * 83. 對應 5.2：Email註冊請求模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description Email密碼註冊請求資料結構
 */
class EmailRegisterRequest {
  final String email;
  final String password;
  final String displayName;
  final String selectedMode;
  final bool termsAccepted;
  final bool privacyAccepted;
  final bool marketingConsent;
  final Map<String, dynamic> deviceInfo;

  EmailRegisterRequest({
    required this.email,
    required this.password,
    required this.displayName,
    required this.selectedMode,
    required this.termsAccepted,
    required this.privacyAccepted,
    required this.marketingConsent,
    required this.deviceInfo,
  });

  Map<String, dynamic> toJson();
}

/**
 * 84. 對應 5.2：註冊回應模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 統一的註冊回應資料結構
 */
class RegisterResponse {
  final bool success;
  final String? accessToken;
  final String? refreshToken;
  final UserModel? user;
  final int? expiresIn;
  final bool? verificationEmailSent;
  final String message;
  final String? nextAction;

  RegisterResponse({
    required this.success,
    this.accessToken,
    this.refreshToken,
    this.user,
    this.expiresIn,
    this.verificationEmailSent,
    required this.message,
    this.nextAction,
  });

  factory RegisterResponse.fromJson(Map<String, dynamic> json);
  Map<String, dynamic> toJson();
}

/**
 * 85. 對應 5.2：Email檢查回應模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description Email可用性檢查回應資料結構
 */
class EmailCheckResponse {
  final bool available;
  final String message;

  EmailCheckResponse({
    required this.available,
    required this.message,
  });

  factory EmailCheckResponse.fromJson(Map<String, dynamic> json);
}
```

---

## 6.0 導航路由設計

### 6.1 路由配置
```dart
/**
 * 86. 對應 6.1：註冊頁面路由配置
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 定義註冊頁面的路由規則與導航邏輯
 */
class RegisterRoutes {
  static const String register = '/register';
  static const String emailVerification = '/email-verification';
  static const String login = '/login';
  static const String home = '/home';
  static const String welcome = '/welcome';

  /**
   * 87. 對應 6.1：取得註冊頁面路由
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 建立註冊頁面的GoRoute配置
   */
  static GoRoute getRegisterRoute();

  /**
   * 88. 對應 6.1：取得Email驗證頁面路由
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 建立Email驗證頁面的GoRoute配置
   */
  static GoRoute getEmailVerificationRoute();
}

/**
 * 89. 對應 6.1：註冊導航服務
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 處理註冊相關的導航邏輯
 */
class RegisterNavigationService {
  static final GoRouter _router;
  static GoRouter get router;

  /**
   * 90. 對應 6.1：導航到首頁
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description OAuth註冊成功後導航到首頁
   */
  Future<void> navigateToHome(UserModel user);

  /**
   * 91. 對應 6.1：導航到登入頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 導航到登入頁面
   */
  Future<void> navigateToLogin();

  /**
   * 92. 對應 6.1：導航到Email驗證頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description Email註冊後導航到驗證提示頁面
   */
  Future<void> navigateToEmailVerification(String email);

  /**
   * 93. 對應 6.1：返回歡迎頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 返回到歡迎頁面
   */
  void navigateToWelcome();

  /**
   * 94. 對應 6.1：處理深層連結
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 處理來自深層連結的註冊請求
   */
  Future<void> handleDeepLink(String link, Map<String, String> params);
}
```

---

## 7.0 響應式設計

### 7.1 響應式輔助工具
```dart
/**
 * 95. 對應 7.1：註冊頁面響應式設計輔助工具
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 遵循9005文件的響應式設計規範，提供註冊頁面專用的佈局管理
 */
class RegisterResponsiveHelper {
  final BuildContext context;
  late final MediaQueryData _mediaQuery;
  late final Size _screenSize;

  RegisterResponsiveHelper.of(BuildContext context);

  // 斷點判斷屬性（9005規範）
  bool get isMobile;
  bool get isTablet;
  bool get isSmallDesktop;
  bool get isLargeDesktop;
  DeviceType get deviceType;

  // 註冊頁面專用尺寸屬性
  double get registerCardWidth;
  double get registerCardMaxWidth;
  EdgeInsets get registerCardPadding;
  double get modeCardSize;
  double get formFieldHeight;
  double get socialButtonSize;
  double get brandLogoSize;

  /**
   * 96. 對應 7.1：取得四模式卡片尺寸
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據使用者模式和裝置類型返回適當模式卡片尺寸
   */
  Size getModeCardSizeForMode(UserMode mode);

  /**
   * 97. 對應 7.1：取得模式對應字型大小
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據使用者模式返回適當字型大小
   */
  double getFontSizeForMode(UserMode? mode, TextStyle baseStyle);

  /**
   * 98. 對應 7.1：取得表單佈局模式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 根據螢幕尺寸決定表單佈局方式
   */
  FormLayoutMode getFormLayoutMode();
}
```

---

## 8.0 無障礙功能

### 8.1 語意化支援
```dart
/**
 * 99. 對應 8.1：註冊頁面無障礙語意化Widget包裝器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 提供WCAG 2.1 AA標準的完整無障礙支援（9005規範）
 */
class RegisterAccessibilityWrapper {

  /**
   * 100. 對應 8.1：為註冊按鈕提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 為各種註冊按鈕添加適當的語意化標籤
   */
  static Widget wrapRegisterButton(Widget child, RegisterMethod method, bool isLoading);
}
```

---

## 9.0 效能最佳化

### 9.1 Widget快取策略
```dart
/**
 * 101. 對應 9.1：註冊頁面Widget快取管理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 優化註冊頁面Widget重建效能，減少不必要的重建
 */
class RegisterWidgetCache {
  static final Map<String, Widget> _cache = {};
  static final Map<String, DateTime> _cacheTimestamps = {};
  static const Duration _cacheValidDuration = Duration(minutes: 10);

  /**
   * 102. 對應 9.1：取得快取的品牌標頭或建立新的
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 從快取中獲取品牌標頭Widget或創建新的
   */
  static Widget getBrandHeaderOrCreate(UserMode? mode, Widget Function() creator);
}
```

---

## 10.0 錯誤處理機制

### 10.1 統一錯誤處理
```dart
/**
 * 103. 對應 10.1：註冊頁面錯誤處理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 21:00:00
 * @description 根據8703規範的具體錯誤情境進行詳細處理
 */
class RegisterErrorHandler {

  /**
   * 104. 對應 10.1：處理LINE OAuth錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 處理LINE OAuth註冊錯誤（8703規範）
   */
  static String handleLineOAuthError(dynamic error);

  /**
   * 105. 對應 10.1：處理Email註冊錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 21:00:00
   * @description 處理Email註冊錯誤（8703規範）
   */
  static String handleEmailRegisterError(dynamic error);
}
```

---

## 遵循0055設計原則

### 階層架構對應
- 本TDD文件基於8703. 註冊頁面_SRS.md的需求規格
- 與9005. Flutter_Presentation layer.md的產品規格保持一致
- 所有函數節點編號對應上層文件的功能條目

### 設計原則遵循
1. **階層架構邏輯**: 嚴格基於SRS文件定義的功能條目設計
2. **同層級依賴關係**: 考量與歡迎頁面、登入頁面的依賴關係
3. **代碼節點編號**: 採用不加前綴的節點編號格式（如2.1、3.2）
4. **命名一致性**: 保持與使用者介面操作流程的名稱同步
5. **可讀性設計**: 採用模組化設計，遵循低耦合、高內聚原則

## 文件維護記錄

| 版本 | 日期 | 修改者 | 修改內容 |
|------|------|--------|----------|
| **v1.0.0** | **2025-08-06 21:00:00 +08:00** | **LCAS SA Team** | **初版建立：完整P003註冊頁面TDD規格，支援多種註冊方式（LINE OAuth、Email、Google、Apple），實作四模式選擇、Email驗證、密碼強度檢查，完整條款同意機制，遵循0055設計原則，對應8703 SRS規格** |

---

## 備註

- **純函數宣告**: 完全移除所有實作代碼，僅保留函數名稱、參數定義和註解
- **多方式註冊支援**: 支援LINE OAuth（主推）、Email、Google、Apple等多種註冊方式
- **四模式差異化**: 根據使用者模式提供不同的註冊界面複雜度和引導
- **Email驗證流程**: 完整的Email驗證註冊流程與重發機制
- **安全性優先**: 完整的表單驗證、密碼強度檢查、條款同意機制
- **完整技術架構**: Widget分層、狀態管理、API整合、響應式設計、無障礙支援、效能最佳化
- **規範完全對齊**: 與0055、8703、9005文件完全一致
- **節點編號標準化**: 統一編號01-105，遵循0055規範格式
- **版本統一升級**: 所有組件版本統一為v1.0.0
- **密碼強度指示**: 即時密碼強度視覺指示器與安全建議
- **模式選擇**: 完整的四種使用模式選擇介面與差異化處理
