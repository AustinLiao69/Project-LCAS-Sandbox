
# 8815. TDD_P015_帳本切換頁面_v1.0.0

## 文件資訊
- **文件標題**: P015 帳本切換頁面技術設計文件
- **文件版本**: v1.0.0
- **創建日期**: 2025-08-06 20:00:00 +08:00 (台灣時間)
- **創建者**: LCAS SA Team
- **最後更新**: 2025-08-06 20:00:00 +08:00 (台灣時間)
- **對應需求文件**: P015_帳本切換頁面_SRS.md
- **對應PRD文件**: 9005. Flutter_Presentation layer.md
- **頁面代碼**: P015 (Ledger Switch Page)

---

## 📑 目次 (Table of Contents)

1. [技術架構概述](#10-技術架構概述)
2. [Widget清單](#20-widget清單)
3. [核心Widget設計](#30-核心widget設計)
4. [狀態管理架構](#40-狀態管理架構)
5. [API整合設計](#50-api整合設計)
6. [導航路由設計](#60-導航路由設計)
7. [響應式設計](#70-響應式設計)
8. [無障礙功能](#80-無障礙功能)
9. [效能最佳化](#90-效能最佳化)
10. [錯誤處理機制](#100-錯誤處理機制)

---

## 1.0 技術架構概述

### 1.1 模組定位
P015帳本切換頁面作為LCAS 2.0 Flutter應用程式的多帳本管理核心，負責使用者在不同帳本間的流暢切換、權限驗證、狀態同步及四模式差異化帳本管理體驗。本頁面採用Clean Architecture設計，確保多帳本架構的穩定性與易用性。

### 1.2 技術堆疊
- **前端框架**: Flutter 3.x
- **狀態管理**: Provider + ChangeNotifier
- **路由管理**: GoRouter
- **HTTP客戶端**: Dio
- **本地儲存**: SharedPreferences + FlutterSecureStorage
- **快取管理**: flutter_cache_manager
- **動畫框架**: AnimationController + Tween
- **搜尋功能**: flutter_typeahead

### 1.3 模組架構
```dart
/**
 * P015_帳本切換頁面_1.0.0
 * @module P015帳本切換頁面
 * @description Flutter Presentation Layer多帳本切換頁面 - 帳本管理、快速切換、權限驗證、四模式差異化
 * @update 2025-08-06: 升級至v1.0版本，新增完整帳本切換功能設計，支援多類型帳本、權限管理、智能排序
 */
```

---

## 2.0 Widget清單

### 2.1 完整Widget索引表

| # | Widget名稱 | 版本 | 所屬層級 | 功能描述 | 相依組件 |
|---|------------|------|----------|----------|----------|
| 01 | LedgerSwitchPage | V1.0.0 | 頁面容器層 | 主帳本切換頁面容器及生命週期管理 | LedgerSwitchProvider、Router |
| 02 | LedgerSwitchScaffold | V1.0.0 | 佈局結構層 | 基礎頁面結構與搜尋列配置 | AppBar、SearchBar |
| 03 | LedgerListHeader | V1.0.0 | 標頭區域層 | 當前帳本顯示與切換按鈕 | CurrentLedgerCard、QuickSwitchButton |
| 04 | LedgerSearchBar | V1.0.0 | 搜尋功能層 | 帳本搜尋與篩選功能 | TextFormField、FilterButton |
| 05 | LedgerCategoryTabs | V1.0.0 | 分類標籤層 | 帳本類型分類標籤 | TabBar、TabController |
| 06 | LedgerListView | V1.0.0 | 清單顯示層 | 帳本清單主要顯示區域 | ListView、LedgerCard |
| 07 | LedgerCard | V1.0.0 | 帳本卡片層 | 單一帳本資訊卡片 | Container、Column、Row |
| 08 | CurrentLedgerCard | V1.0.0 | 當前帳本層 | 當前選中帳本詳細資訊 | LedgerIcon、LedgerInfo、StatusIndicator |
| 09 | LedgerIcon | V1.0.0 | 圖示元件層 | 帳本類型圖示顯示 | Icon、CircleAvatar |
| 10 | LedgerInfo | V1.0.0 | 資訊顯示層 | 帳本基本資訊文字顯示 | Text、Column |
| 11 | StatusIndicator | V1.0.0 | 狀態指示層 | 帳本同步與權限狀態指示 | Icon、Tooltip |
| 12 | MemberBadge | V1.0.0 | 成員標識層 | 協作帳本成員數量顯示 | Badge、Text |
| 13 | LedgerTypeChip | V1.0.0 | 類型標籤層 | 帳本類型標籤顯示 | Chip、Text |
| 14 | RecentLedgersList | V1.0.0 | 最近使用層 | 最近使用帳本快速存取 | Wrap、LedgerQuickCard |
| 15 | LedgerQuickCard | V1.0.0 | 快速卡片層 | 最近使用帳本簡化卡片 | Container、Column |
| 16 | SwitchConfirmDialog | V1.0.0 | 確認對話框層 | 帳本切換確認對話框 | AlertDialog、TextButton |
| 17 | CreateLedgerButton | V1.0.0 | 建立按鈕層 | 建立新帳本浮動按鈕 | FloatingActionButton、Icon |
| 18 | LedgerOptionsMenu | V1.0.0 | 選項選單層 | 帳本操作選項選單 | PopupMenuButton、PopupMenuItem |
| 19 | SyncStatusIndicator | V1.0.0 | 同步狀態層 | 帳本資料同步狀態指示 | CircularProgressIndicator、Icon |
| 20 | PermissionIndicator | V1.0.0 | 權限指示層 | 使用者在帳本中的權限顯示 | Icon、Text |
| 21 | LoadingOverlay | V1.0.0 | 載入覆蓋層 | 帳本切換載入狀態覆蓋 | CircularProgressIndicator、Backdrop |
| 22 | ErrorStateWidget | V1.0.0 | 錯誤狀態層 | 載入失敗錯誤狀態顯示 | Icon、Text、ElevatedButton |
| 23 | EmptyStateWidget | V1.0.0 | 空狀態層 | 無帳本時的空狀態顯示 | Icon、Text、OutlinedButton |

---

## 3.0 核心Widget設計

### 3.1 頁面容器層Widget

#### 3.1.1 LedgerSwitchPage Widget
```dart
/**
 * 01. 對應 2.1：主帳本切換頁面容器Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本切換頁面的主容器，支援多類型帳本切換與四模式差異化
 */
class LedgerSwitchPage extends StatefulWidget {
  static const String routeName = '/ledger-switch';
  final UserMode? userMode;
  final String? currentLedgerId;

  const LedgerSwitchPage({Key? key, this.userMode, this.currentLedgerId});

  @override
  State<LedgerSwitchPage> createState();
}

/**
 * 02. 對應 2.1：帳本切換頁面狀態類別
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 管理帳本切換頁面的狀態和動畫控制器
 */
class _LedgerSwitchPageState extends State<LedgerSwitchPage> 
    with TickerProviderStateMixin {
  late AnimationController _fadeController;
  late AnimationController _slideController;
  late TabController _tabController;
  late Animation<double> _fadeAnimation;
  late Animation<Offset> _slideAnimation;

  @override
  void initState();

  /**
   * 03. 對應 2.1：初始化動畫控制器
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 設定頁面動畫效果和標籤控制器
   */
  void _initializeAnimations();

  /**
   * 04. 對應 2.1：初始化頁面狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 調用Provider載入帳本清單
   */
  Future<void> _initializePage();

  @override
  Widget build(BuildContext context);

  @override
  void dispose();
}
```

### 3.2 佈局結構層Widget

#### 3.2.1 LedgerSwitchScaffold Widget
```dart
/**
 * 05. 對應 2.2：帳本切換頁面佈局結構Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 提供帳本切換頁面的基礎佈局結構，支援四模式差異化背景
 */
class LedgerSwitchScaffold extends StatelessWidget {
  final Animation<double> fadeAnimation;
  final Animation<Offset> slideAnimation;
  final TabController tabController;
  final LedgerSwitchProvider provider;
  final UserMode? userMode;

  const LedgerSwitchScaffold({
    Key? key,
    required this.fadeAnimation,
    required this.slideAnimation,
    required this.tabController,
    required this.provider,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 06. 對應 2.2：建立應用程式列
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立包含搜尋功能的應用程式列
   */
  PreferredSizeWidget _buildAppBar(BuildContext context, LedgerSwitchProvider provider);
}
```

### 3.3 標頭區域層Widget

#### 3.3.1 LedgerListHeader Widget
```dart
/**
 * 07. 對應 3.1：帳本清單標頭區塊Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 顯示當前帳本資訊和快速切換選項，支援四模式差異化樣式
 */
class LedgerListHeader extends StatelessWidget {
  final LedgerModel currentLedger;
  final VoidCallback? onQuickSwitch;
  final UserMode? userMode;

  const LedgerListHeader({
    Key? key,
    required this.currentLedger,
    this.onQuickSwitch,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 08. 對應 3.1：取得模式對應標頭樣式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者模式返回對應的標頭展示樣式
   */
  HeaderStyle _getHeaderStyleForMode(UserMode? mode);
}
```

### 3.4 搜尋功能層Widget

#### 3.4.1 LedgerSearchBar Widget
```dart
/**
 * 09. 對應 3.2：帳本搜尋列Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本搜尋與篩選功能，支援即時搜尋與高級篩選
 */
class LedgerSearchBar extends StatefulWidget {
  final String searchQuery;
  final Function(String) onSearchChanged;
  final VoidCallback? onFilterPressed;
  final LedgerFilterOptions? currentFilter;

  const LedgerSearchBar({
    Key? key,
    required this.searchQuery,
    required this.onSearchChanged,
    this.onFilterPressed,
    this.currentFilter,
  });

  @override
  State<LedgerSearchBar> createState();
}

class _LedgerSearchBarState extends State<LedgerSearchBar> {
  late TextEditingController _searchController;
  late FocusNode _searchFocusNode;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 10. 對應 3.2：建立搜尋建議清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據輸入文字顯示搜尋建議
   */
  Widget _buildSearchSuggestions(BuildContext context, String query);

  @override
  void dispose();
}
```

### 3.5 分類標籤層Widget

#### 3.5.1 LedgerCategoryTabs Widget
```dart
/**
 * 11. 對應 3.3：帳本分類標籤Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本類型分類標籤，支援個人、協作、專案帳本分類
 */
class LedgerCategoryTabs extends StatelessWidget {
  final TabController tabController;
  final Function(int) onTabChanged;
  final Map<LedgerType, int> ledgerCounts;
  final UserMode? userMode;

  const LedgerCategoryTabs({
    Key? key,
    required this.tabController,
    required this.onTabChanged,
    required this.ledgerCounts,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 12. 對應 3.3：建立分類標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據帳本類型建立對應的分類標籤
   */
  List<Tab> _buildCategoryTabs(BuildContext context);

  /**
   * 13. 對應 3.3：取得模式可見分類
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者模式返回可見的帳本分類
   */
  List<LedgerType> _getVisibleCategoriesForMode(UserMode? mode);
}
```

### 3.6 清單顯示層Widget

#### 3.6.1 LedgerListView Widget
```dart
/**
 * 14. 對應 3.4：帳本清單顯示Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本清單主要顯示區域，支援分頁載入與下拉重新整理
 */
class LedgerListView extends StatefulWidget {
  final List<LedgerModel> ledgers;
  final LedgerModel? currentLedger;
  final Function(LedgerModel) onLedgerSelected;
  final VoidCallback? onRefresh;
  final bool isLoading;
  final UserMode? userMode;

  const LedgerListView({
    Key? key,
    required this.ledgers,
    this.currentLedger,
    required this.onLedgerSelected,
    this.onRefresh,
    this.isLoading = false,
    this.userMode,
  });

  @override
  State<LedgerListView> createState();
}

class _LedgerListViewState extends State<LedgerListView> {
  late ScrollController _scrollController;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 15. 對應 3.4：建立帳本卡片
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為每個帳本建立對應的卡片Widget
   */
  Widget _buildLedgerCard(BuildContext context, LedgerModel ledger, int index);

  /**
   * 16. 對應 3.4：處理載入更多
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理清單滾動到底部時載入更多帳本
   */
  void _handleLoadMore();

  @override
  void dispose();
}
```

#### 3.6.2 LedgerCard Widget
```dart
/**
 * 17. 對應 3.4.1：帳本卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 單一帳本資訊卡片，顯示帳本詳細資訊與操作選項
 */
class LedgerCard extends StatefulWidget {
  final LedgerModel ledger;
  final bool isSelected;
  final VoidCallback onTap;
  final VoidCallback? onLongPress;
  final UserMode? userMode;

  const LedgerCard({
    Key? key,
    required this.ledger,
    this.isSelected = false,
    required this.onTap,
    this.onLongPress,
    this.userMode,
  });

  @override
  State<LedgerCard> createState();
}

class _LedgerCardState extends State<LedgerCard> 
    with SingleTickerProviderStateMixin {
  late AnimationController _animationController;
  late Animation<double> _scaleAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 18. 對應 3.4.1：建立卡片內容
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立帳本卡片的詳細內容佈局
   */
  Widget _buildCardContent(BuildContext context);

  /**
   * 19. 對應 3.4.1：建立狀態指示器
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立帳本同步和權限狀態指示器
   */
  Widget _buildStatusIndicators(BuildContext context, LedgerModel ledger);

  /**
   * 20. 對應 3.4.1：取得卡片樣式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據選中狀態和模式取得卡片樣式
   */
  CardStyle _getCardStyle(bool isSelected, UserMode? mode);

  @override
  void dispose();
}
```

### 3.7 資訊顯示組件Widget

#### 3.7.1 CurrentLedgerCard & LedgerInfo Widgets
```dart
/**
 * 21. 對應 3.5：當前帳本卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 當前選中帳本的詳細資訊顯示卡片
 */
class CurrentLedgerCard extends StatelessWidget {
  final LedgerModel ledger;
  final UserMode? userMode;

  const CurrentLedgerCard({Key? key, required this.ledger, this.userMode});

  @override
  Widget build(BuildContext context);
}

/**
 * 22. 對應 3.5.1：帳本資訊Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本基本資訊文字顯示組件
 */
class LedgerInfo extends StatelessWidget {
  final LedgerModel ledger;
  final bool showDetails;

  const LedgerInfo({
    Key? key,
    required this.ledger,
    this.showDetails = true,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 23. 對應 3.5.1：格式化最後使用時間
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 格式化並顯示帳本最後使用時間
   */
  String _formatLastUsed(DateTime? lastUsed);
}

/**
 * 24. 對應 3.5.2：帳本圖示Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本類型圖示顯示組件
 */
class LedgerIcon extends StatelessWidget {
  final LedgerType ledgerType;
  final double size;
  final Color? color;

  const LedgerIcon({
    Key? key,
    required this.ledgerType,
    this.size = 24.0,
    this.color,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 25. 對應 3.5.2：取得類型對應圖示
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據帳本類型返回對應的圖示
   */
  IconData _getIconForLedgerType(LedgerType type);
}
```

### 3.8 狀態指示組件Widget

#### 3.8.1 StatusIndicator & MemberBadge Widgets
```dart
/**
 * 26. 對應 3.6：狀態指示器Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本同步與權限狀態指示器
 */
class StatusIndicator extends StatelessWidget {
  final SyncStatus syncStatus;
  final UserRole userRole;
  final bool showTooltip;

  const StatusIndicator({
    Key? key,
    required this.syncStatus,
    required this.userRole,
    this.showTooltip = true,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 27. 對應 3.6：取得同步狀態圖示
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據同步狀態返回對應的圖示和顏色
   */
  IconData _getSyncStatusIcon(SyncStatus status);

  /**
   * 28. 對應 3.6：取得權限角色文字
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者角色返回對應的權限文字
   */
  String _getRoleText(UserRole role);
}

/**
 * 29. 對應 3.7：成員標識Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 協作帳本成員數量顯示標識
 */
class MemberBadge extends StatelessWidget {
  final int memberCount;
  final List<MemberModel>? members;
  final VoidCallback? onTap;

  const MemberBadge({
    Key? key,
    required this.memberCount,
    this.members,
    this.onTap,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 30. 對應 3.7：建立成員頭像群組
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立成員頭像的堆疊顯示
   */
  Widget _buildMemberAvatars(BuildContext context, List<MemberModel> members);
}
```

### 3.9 輔助功能組件Widget

#### 3.9.1 RecentLedgersList & CreateLedgerButton Widgets
```dart
/**
 * 31. 對應 3.8：最近使用帳本清單Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 最近使用帳本快速存取清單
 */
class RecentLedgersList extends StatelessWidget {
  final List<LedgerModel> recentLedgers;
  final Function(LedgerModel) onLedgerSelected;
  final UserMode? userMode;

  const RecentLedgersList({
    Key? key,
    required this.recentLedgers,
    required this.onLedgerSelected,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);
}

/**
 * 32. 對應 3.8.1：快速帳本卡片Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 最近使用帳本的簡化卡片顯示
 */
class LedgerQuickCard extends StatelessWidget {
  final LedgerModel ledger;
  final VoidCallback onTap;

  const LedgerQuickCard({Key? key, required this.ledger, required this.onTap});

  @override
  Widget build(BuildContext context);
}

/**
 * 33. 對應 3.9：建立帳本按鈕Widget
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 建立新帳本的浮動操作按鈕
 */
class CreateLedgerButton extends StatefulWidget {
  final VoidCallback onPressed;
  final UserMode? userMode;

  const CreateLedgerButton({Key? key, required this.onPressed, this.userMode});

  @override
  State<CreateLedgerButton> createState();
}

class _CreateLedgerButtonState extends State<CreateLedgerButton>
    with SingleTickerProviderStateMixin {
  late AnimationController _animationController;
  late Animation<double> _scaleAnimation;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 34. 對應 3.9：取得模式對應按鈕樣式
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者模式返回對應的按鈕樣式
   */
  ButtonStyle _getButtonStyleForMode(UserMode? mode);

  @override
  void dispose();
}
```

---

## 4.0 狀態管理架構

### 4.1 LedgerSwitchProvider 狀態管理器
```dart
/**
 * 35. 對應 4.1：帳本切換頁面狀態管理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 管理帳本切換頁面的狀態，支援多類型帳本管理和四模式差異化
 */
class LedgerSwitchProvider extends ChangeNotifier {
  // 狀態屬性
  LedgerSwitchPageState _pageState = LedgerSwitchPageState.initial;
  List<LedgerModel> _allLedgers = [];
  List<LedgerModel> _filteredLedgers = [];
  List<LedgerModel> _recentLedgers = [];
  LedgerModel? _currentLedger;
  LedgerModel? _selectedLedger;
  
  // 搜尋與篩選狀態
  String _searchQuery = '';
  LedgerType? _selectedCategory;
  LedgerFilterOptions _filterOptions = LedgerFilterOptions();
  
  // 載入狀態
  bool _isLoading = false;
  bool _isSwitching = false;
  String? _errorMessage;
  LedgerSwitchErrorType? _errorType;
  
  // 使用者模式
  UserMode? _userMode;

  // Getters
  LedgerSwitchPageState get pageState;
  List<LedgerModel> get allLedgers;
  List<LedgerModel> get filteredLedgers;
  List<LedgerModel> get recentLedgers;
  LedgerModel? get currentLedger;
  LedgerModel? get selectedLedger;
  String get searchQuery;
  LedgerType? get selectedCategory;
  LedgerFilterOptions get filterOptions;
  bool get isLoading;
  bool get isSwitching;
  String? get errorMessage;
  LedgerSwitchErrorType? get errorType;
  UserMode? get userMode;

  /**
   * 36. 對應 4.1：初始化帳本切換頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 初始化頁面狀態並載入帳本清單
   */
  Future<void> initializePage(UserMode? mode, String? currentLedgerId);

  /**
   * 37. 對應 4.1：載入帳本清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 從API載入使用者可存取的帳本清單
   */
  Future<void> loadLedgers();

  /**
   * 38. 對應 4.1：載入最近使用帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 載入使用者最近使用的帳本清單
   */
  Future<void> loadRecentLedgers();

  /**
   * 39. 對應 4.1：更新搜尋查詢
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新搜尋文字並過濾帳本清單
   */
  void updateSearchQuery(String query);

  /**
   * 40. 對應 4.1：設定帳本分類篩選
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 設定帳本類型分類篩選
   */
  void setSelectedCategory(LedgerType? category);

  /**
   * 41. 對應 4.1：更新篩選選項
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新進階篩選選項
   */
  void updateFilterOptions(LedgerFilterOptions options);

  /**
   * 42. 對應 4.1：篩選帳本清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據搜尋和篩選條件過濾帳本清單
   */
  void _filterLedgers();

  /**
   * 43. 對應 4.1：選擇帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 選擇要切換的目標帳本
   */
  void selectLedger(LedgerModel ledger);

  /**
   * 44. 對應 4.1：確認切換帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 執行帳本切換操作
   */
  Future<void> confirmSwitchLedger();

  /**
   * 45. 對應 4.1：快速切換到最近帳本
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 快速切換到最近使用的帳本
   */
  Future<void> quickSwitchToRecent(LedgerModel ledger);

  /**
   * 46. 對應 4.1：重新整理帳本清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 重新整理帳本清單數據
   */
  Future<void> refreshLedgers();

  /**
   * 47. 對應 4.1：取得帳本統計數據
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 取得各類型帳本的統計數據
   */
  Map<LedgerType, int> getLedgerCounts();

  /**
   * 48. 對應 4.1：檢查帳本切換權限
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 檢查使用者對目標帳本的切換權限
   */
  Future<bool> checkSwitchPermission(LedgerModel ledger);

  /**
   * 49. 對應 4.1：更新帳本使用記錄
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新帳本使用時間記錄
   */
  Future<void> updateLedgerUsageRecord(String ledgerId);

  /**
   * 50. 對應 4.1：設定載入狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新載入狀態並通知監聽者
   */
  void _setLoading(bool loading);

  /**
   * 51. 對應 4.1：設定切換狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新切換狀態並通知監聽者
   */
  void _setSwitching(bool switching);

  /**
   * 52. 對應 4.1：設定頁面狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新頁面狀態並通知監聽者
   */
  void _setPageState(LedgerSwitchPageState state);

  /**
   * 53. 對應 4.1：設定錯誤訊息
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 設定錯誤訊息和錯誤類型
   */
  void _setError(String message, LedgerSwitchErrorType type, dynamic error);

  /**
   * 54. 對應 4.1：清除錯誤狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 清除錯誤狀態並決定後續動作
   */
  void clearError();

  /**
   * 55. 對應 4.1：重試操作
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據錯誤類型執行相對應的重試操作
   */
  Future<void> retryOperation();
}
```

### 4.2 狀態枚舉定義
```dart
/**
 * 56. 對應 4.2：帳本切換頁面狀態枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 定義帳本切換頁面的各種狀態
 */
enum LedgerSwitchPageState {
  initial,      // 初始狀態
  loading,      // 載入中
  loaded,       // 載入完成
  switching,    // 切換中
  success,      // 切換成功
  error,        // 錯誤狀態
}

/**
 * 57. 對應 4.2：帳本類型枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 支援的帳本類型
 */
enum LedgerType {
  personal,     // 個人帳本
  shared,       // 協作帳本
  project,      // 專案帳本
  category,     // 分類帳本
}

/**
 * 58. 對應 4.2：同步狀態枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本資料同步狀態
 */
enum SyncStatus {
  synced,       // 已同步
  syncing,      // 同步中
  offline,      // 離線
  conflict,     // 衝突
  error,        // 同步錯誤
}

/**
 * 59. 對應 4.2：使用者角色枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 使用者在帳本中的角色
 */
enum UserRole {
  owner,        // 擁有者
  admin,        // 管理員
  member,       // 成員
  viewer,       // 檢視者
}

/**
 * 60. 對應 4.2：帳本切換錯誤類型枚舉
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本切換錯誤類型分類
 */
enum LedgerSwitchErrorType {
  networkError,       // 網路錯誤
  permissionDenied,   // 權限不足
  ledgerNotFound,     // 帳本不存在
  syncConflict,       // 同步衝突
  quotaExceeded,      // 配額超限
  unknown,           // 未知錯誤
}
```

---

## 5.0 API整合設計

### 5.1 LedgerSwitchApiService API服務
```dart
/**
 * 61. 對應 5.1：帳本切換頁面API服務
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 處理帳本切換頁面相關的API調用，支援F015多帳本切換功能
 */
class LedgerSwitchApiService {
  final ApiClient _apiClient = ApiClient();

  /**
   * 62. 對應 5.1：取得可存取帳本清單
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 取得使用者可存取的帳本清單（對應F015端點）
   */
  Future<ApiResponse<LedgerListResponse>> getAccessibleLedgers(LedgerListRequest request);

  /**
   * 63. 對應 5.1：執行帳本切換
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 執行帳本切換操作（對應F015端點）
   */
  Future<ApiResponse<SwitchLedgerResponse>> switchLedger(SwitchLedgerRequest request);

  /**
   * 64. 對應 5.1：取得帳本狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 取得特定帳本的狀態資訊
   */
  Future<ApiResponse<LedgerStatusResponse>> getLedgerStatus(String ledgerId);

  /**
   * 65. 對應 5.1：更新最近使用記錄
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 更新帳本使用記錄
   */
  Future<ApiResponse<UpdateUsageResponse>> updateRecentLedgers(UpdateUsageRequest request);

  /**
   * 66. 對應 5.1：檢查切換權限
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 檢查帳本切換權限
   */
  Future<ApiResponse<PermissionCheckResponse>> checkSwitchPermission(PermissionCheckRequest request);

  /**
   * 67. 對應 5.1：處理Dio錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理Dio錯誤並轉換為使用者友善訊息
   */
  String _handleDioError(DioError error);
}
```

### 5.2 資料模型定義
```dart
/**
 * 68. 對應 5.2：帳本清單請求模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本清單請求資料結構
 */
class LedgerListRequest {
  final String userId;
  final bool includeArchived;
  final LedgerType? ledgerType;
  final String? searchKeyword;
  final int page;
  final int limit;

  LedgerListRequest({
    required this.userId,
    this.includeArchived = false,
    this.ledgerType,
    this.searchKeyword,
    this.page = 1,
    this.limit = 20,
  });

  Map<String, dynamic> toJson();
}

/**
 * 69. 對應 5.2：帳本切換請求模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本切換請求資料結構
 */
class SwitchLedgerRequest {
  final String targetLedgerId;
  final String? currentLedgerId;
  final bool saveCurrentState;
  final String platform;
  final DateTime switchTime;

  SwitchLedgerRequest({
    required this.targetLedgerId,
    this.currentLedgerId,
    this.saveCurrentState = true,
    required this.platform,
    required this.switchTime,
  });

  Map<String, dynamic> toJson();
}

/**
 * 70. 對應 5.2：帳本清單回應模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本清單回應資料結構
 */
class LedgerListResponse {
  final bool success;
  final LedgerModel? currentLedger;
  final List<LedgerModel> availableLedgers;
  final List<String> recentLedgers;
  final int totalCount;
  final String? message;

  LedgerListResponse({
    required this.success,
    this.currentLedger,
    required this.availableLedgers,
    required this.recentLedgers,
    required this.totalCount,
    this.message,
  });

  factory LedgerListResponse.fromJson(Map<String, dynamic> json);
}

/**
 * 71. 對應 5.2：帳本模型
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 帳本資料模型
 */
class LedgerModel {
  final String ledgerId;
  final String name;
  final LedgerType type;
  final UserRole role;
  final int memberCount;
  final List<MemberModel>? members;
  final DateTime? lastUsed;
  final SyncStatus syncStatus;
  final bool isArchived;
  final String currency;
  final int totalEntries;
  final DateTime? lastEntryDate;
  final String? description;
  final String? iconUrl;

  LedgerModel({
    required this.ledgerId,
    required this.name,
    required this.type,
    required this.role,
    required this.memberCount,
    this.members,
    this.lastUsed,
    required this.syncStatus,
    this.isArchived = false,
    required this.currency,
    required this.totalEntries,
    this.lastEntryDate,
    this.description,
    this.iconUrl,
  });

  factory LedgerModel.fromJson(Map<String, dynamic> json);
  Map<String, dynamic> toJson();
}
```

---

## 6.0 導航路由設計

### 6.1 路由配置
```dart
/**
 * 72. 對應 6.1：帳本切換頁面路由配置
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 定義帳本切換頁面的路由規則與導航邏輯
 */
class LedgerSwitchRoutes {
  static const String ledgerSwitch = '/ledger-switch';
  static const String createLedger = '/create-ledger';
  static const String ledgerSettings = '/ledger-settings';
  static const String memberManagement = '/member-management';

  /**
   * 73. 對應 6.1：取得帳本切換頁面路由
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 建立帳本切換頁面的GoRoute配置
   */
  static GoRoute getLedgerSwitchRoute();
}

/**
 * 74. 對應 6.1：帳本切換導航服務
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 處理帳本切換相關的導航邏輯
 */
class LedgerSwitchNavigationService {
  static final GoRouter _router;
  static GoRouter get router;

  /**
   * 75. 對應 6.1：導航到首頁
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 切換成功後導航到首頁
   */
  Future<void> navigateToHome();

  /**
   * 76. 對應 6.1：導航到建立帳本頁面
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 導航到建立新帳本頁面
   */
  Future<void> navigateToCreateLedger();

  /**
   * 77. 對應 6.1：導航到帳本設定
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 導航到帳本設定頁面
   */
  Future<void> navigateToLedgerSettings(String ledgerId);

  /**
   * 78. 對應 6.1：導航到成員管理
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 導航到成員管理頁面
   */
  Future<void> navigateToMemberManagement(String ledgerId);

  /**
   * 79. 對應 6.1：返回上一頁
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 返回到上一個頁面
   */
  void navigateBack();
}
```

---

## 7.0 響應式設計

### 7.1 響應式輔助工具
```dart
/**
 * 80. 對應 7.1：帳本切換頁面響應式設計輔助工具
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 遵循9005文件的響應式設計規範，提供帳本切換頁面專用的佈局管理
 */
class LedgerSwitchResponsiveHelper {
  final BuildContext context;
  late final MediaQueryData _mediaQuery;
  late final Size _screenSize;

  LedgerSwitchResponsiveHelper.of(BuildContext context);

  // 斷點判斷屬性（9005規範）
  bool get isMobile;
  bool get isTablet;
  bool get isSmallDesktop;
  bool get isLargeDesktop;
  DeviceType get deviceType;

  // 帳本切換頁面專用尺寸屬性
  double get ledgerCardHeight;
  double get ledgerCardSpacing;
  EdgeInsets get ledgerListPadding;
  double get searchBarHeight;
  double get headerHeight;
  int get crossAxisCount;

  /**
   * 81. 對應 7.1：取得四模式卡片尺寸
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者模式和裝置類型返回適當卡片尺寸
   */
  double getLedgerCardHeightForMode(UserMode? mode);

  /**
   * 82. 對應 7.1：取得模式對應間距
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據使用者模式返回適當間距設定
   */
  EdgeInsets getSpacingForMode(UserMode? mode);

  /**
   * 83. 對應 7.1：取得列表列數
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 根據螢幕尺寸返回網格列數
   */
  int getCrossAxisCountForScreen();
}
```

---

## 8.0 無障礙功能

### 8.1 語意化支援
```dart
/**
 * 84. 對應 8.1：帳本切換頁面無障礙語意化Widget包裝器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 提供WCAG 2.1 AA標準的完整無障礙支援（9005規範）
 */
class LedgerSwitchAccessibilityWrapper {

  /**
   * 85. 對應 8.1：為帳本卡片提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為帳本卡片添加適當的語意化標籤
   */
  static Widget wrapLedgerCard(Widget child, LedgerModel ledger, bool isSelected);

  /**
   * 86. 對應 8.1：為搜尋欄提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為搜尋欄添加適當的語意化標籤
   */
  static Widget wrapSearchBar(Widget child);

  /**
   * 87. 對應 8.1：為狀態指示器提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為狀態指示器添加適當的語意化標籤
   */
  static Widget wrapStatusIndicator(Widget child, SyncStatus status, UserRole role);

  /**
   * 88. 對應 8.1：為切換確認對話框提供語意化標籤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 為切換確認對話框添加適當的語意化標籤
   */
  static Widget wrapSwitchDialog(Widget child, LedgerModel targetLedger);
}
```

---

## 9.0 效能最佳化

### 9.1 Widget快取策略
```dart
/**
 * 89. 對應 9.1：帳本切換頁面Widget快取管理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 優化帳本切換頁面Widget重建效能，減少不必要的重建
 */
class LedgerSwitchWidgetCache {
  static final Map<String, Widget> _cache = {};
  static final Map<String, DateTime> _cacheTimestamps = {};
  static const Duration _cacheValidDuration = Duration(minutes: 15);

  /**
   * 90. 對應 9.1：取得快取的帳本卡片或建立新的
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 從快取中獲取帳本卡片Widget或創建新的
   */
  static Widget getLedgerCardOrCreate(LedgerModel ledger, Widget Function() creator);

  /**
   * 91. 對應 9.1：取得快取的狀態指示器或建立新的
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 從快取中獲取狀態指示器Widget或創建新的
   */
  static Widget getStatusIndicatorOrCreate(String key, Widget Function() creator);

  /**
   * 92. 對應 9.1：清除帳本切換快取
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 清除帳本切換頁面相關的Widget快取
   */
  static void clearLedgerSwitchCache();
}
```

### 9.2 帳本載入優化
```dart
/**
 * 93. 對應 9.2：帳本載入效能優化器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 優化帳本載入效能，提升使用者體驗
 */
class LedgerLoadingOptimizer {
  static final Map<String, Timer> _debounceTimers = {};
  static const Duration _debounceDelay = Duration(milliseconds: 300);

  /**
   * 94. 對應 9.2：防抖動搜尋請求
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 防止使用者快速輸入造成重複搜尋請求
   */
  static Future<void> debounceSearch(String key, Future<void> Function() searchFunction);

  /**
   * 95. 對應 9.2：預載入帳本圖示
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 預載入帳本圖示資源
   */
  static Future<void> preloadLedgerIcons(BuildContext context, List<LedgerModel> ledgers);

  /**
   * 96. 對應 9.2：批量載入帳本狀態
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 批量載入帳本狀態以減少API調用次數
   */
  static Future<Map<String, SyncStatus>> batchLoadLedgerStatus(List<String> ledgerIds);
}
```

---

## 10.0 錯誤處理機制

### 10.1 統一錯誤處理
```dart
/**
 * 97. 對應 10.1：帳本切換頁面錯誤處理器
 * @version 2025-08-06-V1.0.0
 * @date 2025-08-06 20:00:00
 * @description 根據P015規範的具體錯誤情境進行詳細處理
 */
class LedgerSwitchErrorHandler {

  /**
   * 98. 對應 10.1：處理帳本載入錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理帳本清單載入錯誤（P015規範）
   */
  static String handleLedgerLoadError(dynamic error);

  /**
   * 99. 對應 10.1：處理帳本切換錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理帳本切換操作錯誤（P015規範）
   */
  static String handleSwitchError(dynamic error);

  /**
   * 100. 對應 10.1：處理權限錯誤
   * @version 2025-08-06-V1.0.0
   * @date 2025-08-06 20:00:00
   * @description 處理帳本存取權限錯誤（P015規範）
   */
  static String handlePermissionError(dynamic error);
}
```

---

## 遵循0055設計原則

### 階層架構對應
- 本TDD文件基於P015_帳本切換頁面_SRS.md的需求規格
- 與9005. Flutter_Presentation layer.md的產品規格保持一致
- 所有函數節點編號對應上層文件的功能條目

### 設計原則遵循
1. **階層架構邏輯**: 嚴格基於SRS文件定義的功能條目設計
2. **同層級依賴關係**: 考量與首頁儀表板和設定頁面的依賴關係
3. **代碼節點編號**: 採用不加前綴的節點編號格式（如2.1、3.2）
4. **命名一致性**: 保持與使用者介面操作流程的名稱同步
5. **可讀性設計**: 採用模組化設計，遵循低耦合、高內聚原則

## 文件維護記錄

| 版本 | 日期 | 修改者 | 修改內容 |
|------|------|--------|----------|
| **v1.0.0** | **2025-08-06 20:00:00 +08:00** | **LCAS SA Team** | **初版建立：完整P015帳本切換頁面TDD規格，支援多類型帳本切換（個人、協作、專案、分類），實作四模式差異化設計，完整權限驗證機制，智能搜尋與篩選功能，遵循0055設計原則，對應P015 SRS規格** |

---

## 備註

- **純函數宣告**: 完全移除所有實作代碼，僅保留函數名稱、參數定義和註解
- **多帳本支援**: 支援個人、協作、專案、分類等多種帳本類型的統一切換管理
- **四模式差異化**: 根據使用者模式提供不同的帳本切換界面複雜度和功能選項
- **權限安全**: 完整的權限驗證、狀態同步、切換記錄機制
- **智能功能**: 搜尋建議、智能排序、最近使用記錄、離線支援
- **完整技術架構**: Widget分層、狀態管理、API整合、響應式設計、無障礙支援、效能最佳化
- **規範完全對齊**: 與0055、P015 SRS、9005文件完全一致
- **節點編號標準化**: 統一編號01-100，遵循0055規範格式
- **版本統一升級**: 所有組件版本統一為v1.0.0
