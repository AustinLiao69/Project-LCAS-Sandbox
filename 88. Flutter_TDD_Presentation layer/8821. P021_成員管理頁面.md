
# 8821. TDD_P021_成員管理頁面_v1.0.0

## 文件資訊
- **文件標題**: P021 成員管理頁面技術設計文件
- **文件版本**: v1.0.0
- **創建日期**: 2025-01-26 22:00:00 +08:00 (台灣時間)
- **創建者**: LCAS SA Team
- **最後更新**: 2025-01-26 22:00:00 +08:00 (台灣時間)
- **對應需求文件**: P021_成員管理頁面_SRS.md
- **對應PRD文件**: 9005. Flutter_Presentation layer.md
- **頁面代碼**: P021 (Member Management Page)

---

## 📑 目次 (Table of Contents)

1. [技術架構概述](#10-技術架構概述)
2. [Widget清單](#20-widget清單)
3. [核心Widget設計](#30-核心widget設計)
4. [狀態管理架構](#40-狀態管理架構)
5. [API整合設計](#50-api整合設計)
6. [導航路由設計](#60-導航路由設計)
7. [響應式設計](#70-響應式設計)
8. [無障礙功能](#80-無障礙功能)
9. [效能最佳化](#90-效能最佳化)
10. [錯誤處理機制](#100-錯誤處理機制)

---

## 1.0 技術架構概述

### 1.1 模組定位
P021成員管理頁面作為LCAS 2.0多帳本系統的協作管理中心，負責帳本成員的完整管理功能，包含成員邀請、角色權限設定、成員活動監控、權限調整等核心功能。本頁面採用Clean Architecture設計，確保協作管理的安全性與易用性平衡。

### 1.2 技術堆疊
- **前端框架**: Flutter 3.x
- **狀態管理**: Provider + ChangeNotifier
- **路由管理**: GoRouter
- **HTTP客戶端**: Dio
- **本地儲存**: SharedPreferences + FlutterSecureStorage
- **即時通訊**: WebSocket + Socket.IO
- **權限管理**: Permission Handler
- **表單驗證**: FormBuilderValidators
- **下拉重新整理**: RefreshIndicator

### 1.3 模組架構
```dart
/**
 * P021_成員管理頁面_1.0.0
 * @module P021成員管理頁面
 * @description Flutter Presentation Layer協作管理頁面 - 成員管理、權限控制、邀請審核、活動監控
 * @update 2025-01-26: 升級至v1.0版本，新增完整成員管理功能設計，支援角色權限管理、邀請審核、即時狀態監控
 */
```

---

## 2.0 Widget清單

### 2.1 完整Widget索引表

| # | Widget名稱 | 版本 | 所屬層級 | 功能描述 | 相依組件 |
|---|------------|------|----------|----------|----------|
| 01 | MemberManagementPage | V1.0.0 | 頁面容器層 | 主成員管理頁面容器及生命週期管理 | MemberProvider、Router |
| 02 | MemberScaffold | V1.0.0 | 佈局結構層 | 基礎頁面結構與導航設定 | AppBar、RefreshIndicator |
| 03 | MemberHeaderSection | V1.0.0 | 統計摘要層 | 成員統計摘要卡片區域 | StatisticsCard、MemberCounter |
| 04 | SearchAndFilterBar | V1.0.0 | 搜尋篩選層 | 成員搜尋與篩選控制介面 | SearchField、FilterDropdown |
| 05 | MemberListSection | V1.0.0 | 成員列表層 | 成員清單主要顯示區域 | MemberCard、PaginationController |
| 06 | MemberCard | V1.0.0 | 成員項目層 | 個別成員資訊展示卡片 | UserAvatar、RoleTag、ActivityIndicator |
| 07 | MemberAvatar | V1.0.0 | 頭像顯示層 | 成員頭像與線上狀態顯示 | CachedNetworkImage、StatusIndicator |
| 08 | RoleTag | V1.0.0 | 角色標籤層 | 成員角色標籤與權限顯示 | Chip、Icon |
| 09 | ActivityIndicator | V1.0.0 | 活動狀態層 | 成員活動狀態與最後活動時間 | StatusDot、TimeDisplay |
| 10 | MemberActionsMenu | V1.0.0 | 操作選單層 | 成員操作選單（編輯、移除等） | PopupMenuButton、IconButton |
| 11 | InvitationSection | V1.0.0 | 邀請管理層 | 待處理邀請清單顯示區域 | InvitationCard、InviteButton |
| 12 | InvitationCard | V1.0.0 | 邀請項目層 | 個別邀請資訊展示卡片 | EmailDisplay、StatusBadge |
| 13 | InviteMemberDialog | V1.0.0 | 邀請對話框層 | 邀請新成員對話框 | EmailInput、RoleSelector |
| 14 | EmailInputField | V1.0.0 | 輸入元件層 | Email輸入欄位與驗證 | TextFormField、EmailValidator |
| 15 | RoleSelector | V1.0.0 | 角色選擇層 | 角色選擇下拉選單 | DropdownButtonFormField、RoleItem |
| 16 | PermissionsDisplay | V1.0.0 | 權限顯示層 | 角色權限詳細說明 | PermissionList、InfoCard |
| 17 | EditMemberDialog | V1.0.0 | 編輯對話框層 | 編輯成員角色與權限對話框 | RoleEditor、PermissionEditor |
| 18 | RemoveMemberDialog | V1.0.0 | 移除確認層 | 移除成員確認對話框 | AlertDialog、WarningText |
| 19 | LoadingOverlay | V1.0.0 | 載入狀態層 | 操作進行中載入覆蓋層 | CircularProgressIndicator、Backdrop |
| 20 | ErrorSnackBar | V1.0.0 | 錯誤提示層 | 錯誤訊息提示條 | SnackBar、IconButton |
| 21 | EmptyStateWidget | V1.0.0 | 空狀態層 | 無成員時的空狀態顯示 | IllustrationWidget、ActionButton |

---

## 3.0 核心Widget設計

### 3.1 頁面容器層Widget

#### 3.1.1 MemberManagementPage Widget
```dart
/**
 * 01. 對應 2.1：主成員管理頁面容器Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 成員管理頁面的主容器，支援多角色權限管理與即時狀態監控
 */
class MemberManagementPage extends StatefulWidget {
  static const String routeName = '/member-management';
  final String ledgerId;
  final UserMode? userMode;

  const MemberManagementPage({
    Key? key,
    required this.ledgerId,
    this.userMode,
  });

  @override
  State<MemberManagementPage> createState();
}

/**
 * 02. 對應 2.1：成員管理頁面狀態類別
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 管理成員管理頁面的狀態和WebSocket連線
 */
class _MemberManagementPageState extends State<MemberManagementPage> 
    with TickerProviderStateMixin {
  late AnimationController _refreshController;
  late AnimationController _fabController;
  late Animation<double> _refreshAnimation;
  late Animation<double> _fabAnimation;

  @override
  void initState();

  /**
   * 03. 對應 2.1：初始化動畫控制器
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 設定頁面動畫效果
   */
  void _initializeAnimations();

  /**
   * 04. 對應 2.1：初始化頁面資料
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 載入成員清單和邀請資料
   */
  Future<void> _initializeData();

  /**
   * 05. 對應 2.1：建立WebSocket連線
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立即時成員狀態更新連線
   */
  void _setupRealtimeConnection();

  @override
  Widget build(BuildContext context);

  @override
  void dispose();
}
```

### 3.2 佈局結構層Widget

#### 3.2.1 MemberScaffold Widget
```dart
/**
 * 06. 對應 2.2：成員管理頁面佈局結構Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 提供成員管理頁面的基礎佈局結構，支援下拉重新整理
 */
class MemberScaffold extends StatelessWidget {
  final String ledgerId;
  final MemberProvider provider;
  final UserMode? userMode;
  final VoidCallback onRefresh;
  final VoidCallback onInviteMember;

  const MemberScaffold({
    Key? key,
    required this.ledgerId,
    required this.provider,
    this.userMode,
    required this.onRefresh,
    required this.onInviteMember,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 07. 對應 2.2：建立應用程式列
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立包含標題和操作按鈕的應用程式列
   */
  PreferredSizeWidget _buildAppBar(BuildContext context);

  /**
   * 08. 對應 2.2：建立浮動操作按鈕
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立邀請成員的浮動操作按鈕
   */
  Widget _buildFloatingActionButton(BuildContext context);
}
```

### 3.3 統計摘要層Widget

#### 3.3.1 MemberHeaderSection Widget
```dart
/**
 * 09. 對應 3.1：成員統計摘要區塊Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 顯示成員統計資訊摘要卡片
 */
class MemberHeaderSection extends StatelessWidget {
  final MemberStatistics statistics;
  final UserMode? userMode;

  const MemberHeaderSection({
    Key? key,
    required this.statistics,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 10. 對應 3.1：建立統計卡片
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立個別統計項目卡片
   */
  Widget _buildStatCard(
    BuildContext context,
    String title,
    String value,
    IconData icon,
    Color color,
  );

  /**
   * 11. 對應 3.1：取得模式對應樣式
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據使用者模式返回對應的卡片樣式
   */
  CardStyle _getCardStyleForMode(UserMode? mode);
}
```

### 3.4 搜尋篩選層Widget

#### 3.4.1 SearchAndFilterBar Widget
```dart
/**
 * 12. 對應 3.2：搜尋與篩選控制列Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 提供成員搜尋和篩選功能控制
 */
class SearchAndFilterBar extends StatefulWidget {
  final String searchKeyword;
  final String roleFilter;
  final String statusFilter;
  final ValueChanged<String> onSearchChanged;
  final ValueChanged<String> onRoleFilterChanged;
  final ValueChanged<String> onStatusFilterChanged;
  final VoidCallback onClearFilters;

  const SearchAndFilterBar({
    Key? key,
    required this.searchKeyword,
    required this.roleFilter,
    required this.statusFilter,
    required this.onSearchChanged,
    required this.onRoleFilterChanged,
    required this.onStatusFilterChanged,
    required this.onClearFilters,
  });

  @override
  State<SearchAndFilterBar> createState();
}

class _SearchAndFilterBarState extends State<SearchAndFilterBar> {
  late TextEditingController _searchController;
  late Timer? _searchDebouncer;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 13. 對應 3.2：建立搜尋輸入欄位
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立成員姓名或Email搜尋欄位
   */
  Widget _buildSearchField();

  /**
   * 14. 對應 3.2：建立篩選下拉選單
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立角色和狀態篩選下拉選單
   */
  Widget _buildFilterDropdowns();

  /**
   * 15. 對應 3.2：處理搜尋防抖動
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 防止搜尋輸入過於頻繁
   */
  void _handleSearchDebounce(String query);

  @override
  void dispose();
}
```

### 3.5 成員列表層Widget

#### 3.5.1 MemberListSection Widget
```dart
/**
 * 16. 對應 3.3：成員清單區塊Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 顯示成員清單主要內容區域
 */
class MemberListSection extends StatelessWidget {
  final List<LedgerMember> members;
  final bool isLoading;
  final bool hasMore;
  final VoidCallback onLoadMore;
  final Function(LedgerMember) onMemberTap;
  final Function(LedgerMember) onEditMember;
  final Function(LedgerMember) onRemoveMember;
  final UserMode? userMode;

  const MemberListSection({
    Key? key,
    required this.members,
    required this.isLoading,
    required this.hasMore,
    required this.onLoadMore,
    required this.onMemberTap,
    required this.onEditMember,
    required this.onRemoveMember,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 17. 對應 3.3：建立成員清單視圖
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立可滾動的成員清單視圖
   */
  Widget _buildMemberListView();

  /**
   * 18. 對應 3.3：建立載入更多指示器
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示載入更多成員的指示器
   */
  Widget _buildLoadMoreIndicator();
}

/**
 * 19. 對應 3.3：成員卡片Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 個別成員資訊展示卡片
 */
class MemberCard extends StatelessWidget {
  final LedgerMember member;
  final VoidCallback onTap;
  final VoidCallback onEdit;
  final VoidCallback onRemove;
  final bool canEdit;
  final UserMode? userMode;

  const MemberCard({
    Key? key,
    required this.member,
    required this.onTap,
    required this.onEdit,
    required this.onRemove,
    required this.canEdit,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 20. 對應 3.3：建立成員基本資訊
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示成員頭像、姓名、Email等基本資訊
   */
  Widget _buildMemberInfo();

  /**
   * 21. 對應 3.3：建立成員統計資訊
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示成員貢獻統計和活動狀態
   */
  Widget _buildMemberStats();

  /**
   * 22. 對應 3.3：建立操作按鈕區域
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立編輯、移除等操作按鈕
   */
  Widget _buildActionButtons();
}
```

### 3.6 成員項目細部元件

#### 3.6.1 MemberAvatar & RoleTag Widgets
```dart
/**
 * 23. 對應 3.4：成員頭像與狀態Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 顯示成員頭像與線上狀態
 */
class MemberAvatar extends StatelessWidget {
  final String? avatarUrl;
  final String displayName;
  final bool isOnline;
  final double size;

  const MemberAvatar({
    Key? key,
    this.avatarUrl,
    required this.displayName,
    required this.isOnline,
    this.size = 50.0,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 24. 對應 3.4：建立狀態指示器
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 在頭像右下角顯示線上/離線狀態
   */
  Widget _buildStatusIndicator();
}

/**
 * 25. 對應 3.4：角色標籤Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 顯示成員角色標籤
 */
class RoleTag extends StatelessWidget {
  final String role;
  final List<String> permissions;
  final UserMode? userMode;

  const RoleTag({
    Key? key,
    required this.role,
    required this.permissions,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 26. 對應 3.4：取得角色顏色
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據角色類型返回對應顏色
   */
  Color _getRoleColor(String role);

  /**
   * 27. 對應 3.4：取得角色圖示
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據角色類型返回對應圖示
   */
  IconData _getRoleIcon(String role);
}

/**
 * 28. 對應 3.4：活動指示器Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 顯示成員活動狀態和最後活動時間
 */
class ActivityIndicator extends StatelessWidget {
  final DateTime lastActive;
  final int activityScore;

  const ActivityIndicator({
    Key? key,
    required this.lastActive,
    required this.activityScore,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 29. 對應 3.4：格式化活動時間
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 將最後活動時間格式化為易讀格式
   */
  String _formatActivityTime(DateTime dateTime);

  /**
   * 30. 對應 3.4：取得活動等級
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據活動分數判斷活動等級
   */
  ActivityLevel _getActivityLevel(int score);
}
```

### 3.7 邀請管理層Widget

#### 3.7.1 InvitationSection Widget
```dart
/**
 * 31. 對應 3.5：邀請管理區塊Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 顯示待處理邀請清單
 */
class InvitationSection extends StatelessWidget {
  final List<LedgerInvitation> invitations;
  final Function(String) onResendInvitation;
  final Function(String) onCancelInvitation;
  final bool canManageInvitations;

  const InvitationSection({
    Key? key,
    required this.invitations,
    required this.onResendInvitation,
    required this.onCancelInvitation,
    required this.canManageInvitations,
  });

  @override
  Widget build(BuildContext context);
}

/**
 * 32. 對應 3.5：邀請卡片Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 個別邀請資訊展示卡片
 */
class InvitationCard extends StatelessWidget {
  final LedgerInvitation invitation;
  final VoidCallback onResend;
  final VoidCallback onCancel;
  final bool canManage;

  const InvitationCard({
    Key? key,
    required this.invitation,
    required this.onResend,
    required this.onCancel,
    required this.canManage,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 33. 對應 3.5：建立邀請狀態標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示邀請狀態（已發送、待回應、已過期等）
   */
  Widget _buildStatusBadge();

  /**
   * 34. 對應 3.5：計算剩餘有效時間
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 計算邀請剩餘有效時間
   */
  String _calculateRemainingTime();
}
```

### 3.8 對話框元件層

#### 3.8.1 InviteMemberDialog Widget
```dart
/**
 * 35. 對應 3.6：邀請成員對話框Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 邀請新成員對話框
 */
class InviteMemberDialog extends StatefulWidget {
  final Function(List<String>, String, String?, int) onInvite;
  final List<String> availableRoles;

  const InviteMemberDialog({
    Key? key,
    required this.onInvite,
    required this.availableRoles,
  });

  @override
  State<InviteMemberDialog> createState();
}

class _InviteMemberDialogState extends State<InviteMemberDialog> {
  final GlobalKey<FormState> _formKey = GlobalKey<FormState>();
  final List<TextEditingController> _emailControllers = [];
  late String _selectedRole;
  late String? _personalMessage;
  late int _expiryDays;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 36. 對應 3.6：建立Email輸入區域
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 可動態新增多個Email輸入欄位
   */
  Widget _buildEmailInputSection();

  /**
   * 37. 對應 3.6：建立角色選擇區域
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 選擇新成員預設角色
   */
  Widget _buildRoleSelectionSection();

  /**
   * 38. 對應 3.6：建立進階選項區域
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 個人訊息和邀請有效期設定
   */
  Widget _buildAdvancedOptionsSection();

  /**
   * 39. 對應 3.6：新增Email輸入欄位
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 動態新增Email輸入欄位
   */
  void _addEmailField();

  /**
   * 40. 對應 3.6：移除Email輸入欄位
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 移除指定的Email輸入欄位
   */
  void _removeEmailField(int index);

  /**
   * 41. 對應 3.6：驗證並提交邀請
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 驗證表單並執行邀請操作
   */
  void _validateAndSubmit();

  @override
  void dispose();
}
```

#### 3.8.2 EditMemberDialog Widget
```dart
/**
 * 42. 對應 3.7：編輯成員對話框Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 編輯成員角色與權限對話框
 */
class EditMemberDialog extends StatefulWidget {
  final LedgerMember member;
  final List<String> availableRoles;
  final Function(String, List<String>, String?) onUpdate;

  const EditMemberDialog({
    Key? key,
    required this.member,
    required this.availableRoles,
    required this.onUpdate,
  });

  @override
  State<EditMemberDialog> createState();
}

class _EditMemberDialogState extends State<EditMemberDialog> {
  late String _selectedRole;
  late List<String> _selectedPermissions;
  late String? _changeReason;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 43. 對應 3.7：建立成員資訊顯示
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示被編輯成員的基本資訊
   */
  Widget _buildMemberInfoDisplay();

  /**
   * 44. 對應 3.7：建立角色編輯區域
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 角色選擇和權限調整區域
   */
  Widget _buildRoleEditSection();

  /**
   * 45. 對應 3.7：建立權限詳細設定
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 詳細權限設定開關列表
   */
  Widget _buildPermissionSettings();

  /**
   * 46. 對應 3.7：驗證權限變更規則
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 驗證權限變更是否符合業務規則
   */
  bool _validatePermissionChanges();

  /**
   * 47. 對應 3.7：提交權限變更
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 提交成員權限變更
   */
  void _submitChanges();
}
```

---

## 4.0 狀態管理架構

### 4.1 MemberProvider 狀態管理器
```dart
/**
 * 48. 對應 4.1：成員管理頁面狀態管理器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 管理成員管理頁面的狀態，支援成員CRUD、邀請管理、即時狀態更新
 */
class MemberProvider extends ChangeNotifier {
  // 基本狀態屬性
  MemberPageState _pageState = MemberPageState.initial;
  List<LedgerMember> _members = [];
  List<LedgerInvitation> _invitations = [];
  MemberStatistics? _statistics;
  bool _isLoading = false;
  String? _errorMessage;
  String _ledgerId = '';

  // 搜尋與篩選狀態
  String _searchKeyword = '';
  String _roleFilter = 'all';
  String _statusFilter = 'all';
  int _currentPage = 1;
  bool _hasMoreMembers = true;

  // UI狀態
  bool _showInviteDialog = false;
  bool _showEditDialog = false;
  bool _showRemoveDialog = false;
  LedgerMember? _selectedMember;

  // 操作狀態
  bool _isInviting = false;
  bool _isUpdatingMember = false;
  bool _isRemovingMember = false;

  // WebSocket狀態
  bool _isRealtimeConnected = false;

  // Getters
  MemberPageState get pageState => _pageState;
  List<LedgerMember> get members => _members;
  List<LedgerInvitation> get invitations => _invitations;
  MemberStatistics? get statistics => _statistics;
  bool get isLoading => _isLoading;
  String? get errorMessage => _errorMessage;
  String get searchKeyword => _searchKeyword;
  String get roleFilter => _roleFilter;
  String get statusFilter => _statusFilter;
  bool get hasMoreMembers => _hasMoreMembers;
  bool get showInviteDialog => _showInviteDialog;
  bool get showEditDialog => _showEditDialog;
  bool get showRemoveDialog => _showRemoveDialog;
  LedgerMember? get selectedMember => _selectedMember;
  bool get isInviting => _isInviting;
  bool get isUpdatingMember => _isUpdatingMember;
  bool get isRemovingMember => _isRemovingMember;
  bool get isRealtimeConnected => _isRealtimeConnected;

  /**
   * 49. 對應 4.1：初始化成員管理頁面
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 初始化成員管理頁面狀態和載入初始資料
   */
  Future<void> initializePage(String ledgerId);

  /**
   * 50. 對應 4.1：載入成員清單
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 載入成員清單和統計資訊
   */
  Future<void> loadMembers({bool refresh = false});

  /**
   * 51. 對應 4.1：載入更多成員
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 分頁載入更多成員
   */
  Future<void> loadMoreMembers();

  /**
   * 52. 對應 4.1：搜尋成員
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據關鍵字搜尋成員
   */
  void searchMembers(String keyword);

  /**
   * 53. 對應 4.1：篩選成員
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據角色和狀態篩選成員
   */
  void filterMembers({String? role, String? status});

  /**
   * 54. 對應 4.1：邀請新成員
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 邀請新成員加入帳本
   */
  Future<void> inviteMembers(
    List<String> emails,
    String defaultRole,
    String? personalMessage,
    int expiryDays,
  );

  /**
   * 55. 對應 4.1：更新成員角色
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 更新成員的角色和權限
   */
  Future<void> updateMemberRole(
    String memberId,
    String newRole,
    List<String> permissions,
    String? reason,
  );

  /**
   * 56. 對應 4.1：移除成員
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 從帳本移除成員
   */
  Future<void> removeMember(String memberId, String? reason);

  /**
   * 57. 對應 4.1：重新發送邀請
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 重新發送邀請郵件
   */
  Future<void> resendInvitation(String inviteId);

  /**
   * 58. 對應 4.1：取消邀請
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 取消待處理的邀請
   */
  Future<void> cancelInvitation(String inviteId);

  /**
   * 59. 對應 4.1：建立即時連線
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立WebSocket連線監聽成員狀態變化
   */
  void connectRealtime();

  /**
   * 60. 對應 4.1：處理即時狀態更新
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 處理來自WebSocket的成員狀態更新
   */
  void handleRealtimeUpdate(RealtimeEvent event);

  /**
   * 61. 對應 4.1：顯示邀請對話框
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示邀請成員對話框
   */
  void showInviteDialog();

  /**
   * 62. 對應 4.1：顯示編輯對話框
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示編輯成員對話框
   */
  void showEditDialog(LedgerMember member);

  /**
   * 63. 對應 4.1：顯示移除確認對話框
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示移除成員確認對話框
   */
  void showRemoveDialog(LedgerMember member);

  /**
   * 64. 對應 4.1：隱藏所有對話框
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 隱藏所有對話框
   */
  void hideDialogs();

  // 私有方法
  void _setLoading(bool loading);
  void _setPageState(MemberPageState state);
  void _setError(String? error);
  void _updateMemberInList(LedgerMember updatedMember);
  void _removeMemberFromList(String memberId);
  void _addInvitationToList(LedgerInvitation invitation);
  void _updateInvitationInList(LedgerInvitation updatedInvitation);
  void _removeInvitationFromList(String inviteId);
}
```

### 4.2 狀態枚舉定義
```dart
/**
 * 65. 對應 4.2：成員管理頁面狀態枚舉
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 定義成員管理頁面的各種狀態
 */
enum MemberPageState {
  initial,          // 初始狀態
  loading,          // 載入中
  loaded,           // 載入完成
  refreshing,       // 重新整理中
  loadingMore,      // 載入更多中
  inviting,         // 邀請中
  updating,         // 更新中
  removing,         // 移除中
  error,            // 錯誤狀態
}

/**
 * 66. 對應 4.2：成員操作類型枚舉
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 成員相關操作類型
 */
enum MemberOperation {
  invite,           // 邀請成員
  updateRole,       // 更新角色
  remove,           // 移除成員
  resendInvite,     // 重新發送邀請
  cancelInvite,     // 取消邀請
}

/**
 * 67. 對應 4.2：即時事件類型枚舉
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 即時狀態更新事件類型
 */
enum RealtimeEventType {
  memberJoined,     // 成員加入
  memberLeft,       // 成員離開
  memberOnline,     // 成員上線
  memberOffline,    // 成員離線
  roleUpdated,      // 角色更新
  inviteAccepted,   // 邀請接受
  inviteDeclined,   // 邀請拒絕
}

/**
 * 68. 對應 4.2：活動等級枚舉
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 成員活動等級分類
 */
enum ActivityLevel {
  high,      // 高活躍度
  medium,    // 中等活躍度
  low,       // 低活躍度
  inactive,  // 不活躍
}
```

---

## 5.0 API整合設計

### 5.1 MemberApiService API服務
```dart
/**
 * 69. 對應 5.1：成員管理頁面API服務
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 處理成員管理相關的API調用，對應ProjectLedgerService和CollaborationService
 */
class MemberApiService {
  final ApiClient _apiClient = ApiClient();

  /**
   * 70. 對應 5.1：載入帳本成員清單
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 載入帳本成員清單（對應F014-F015端點）
   */
  Future<ApiResponse<MemberListResponse>> getLedgerMembers(MemberQueryRequest request);

  /**
   * 71. 對應 5.1：邀請帳本成員
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 邀請新成員加入帳本（對應F014端點）
   */
  Future<ApiResponse<InviteResponse>> inviteMembers(InviteMembersRequest request);

  /**
   * 72. 對應 5.1：更新成員角色
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 更新成員角色和權限（對應F020端點）
   */
  Future<ApiResponse<MemberUpdateResponse>> updateMemberRole(UpdateMemberRoleRequest request);

  /**
   * 73. 對應 5.1：移除帳本成員
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 從帳本移除成員（對應F015端點）
   */
  Future<ApiResponse<void>> removeMember(RemoveMemberRequest request);

  /**
   * 74. 對應 5.1：重新發送邀請
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 重新發送邀請郵件
   */
  Future<ApiResponse<void>> resendInvitation(ResendInviteRequest request);

  /**
   * 75. 對應 5.1：取消邀請
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 取消待處理邀請
   */
  Future<ApiResponse<void>> cancelInvitation(CancelInviteRequest request);

  /**
   * 76. 對應 5.1：建立即時連線
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立WebSocket連線監聽成員狀態（對應F021端點）
   */
  Stream<RealtimeEvent> connectRealtimeUpdates(String ledgerId);

  /**
   * 77. 對應 5.1：處理Dio錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 處理Dio錯誤並轉換為使用者友善訊息
   */
  String _handleDioError(DioError error);
}
```

### 5.2 資料模型定義
```dart
/**
 * 78. 對應 5.2：成員查詢請求模型
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 成員清單查詢請求資料結構
 */
class MemberQueryRequest {
  final String ledgerId;
  final String? search;
  final String? role;
  final String? status;
  final int page;
  final int limit;

  MemberQueryRequest({
    required this.ledgerId,
    this.search,
    this.role,
    this.status,
    required this.page,
    required this.limit,
  });

  Map<String, dynamic> toJson();
}

/**
 * 79. 對應 5.2：成員清單回應模型
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 成員清單查詢回應資料結構
 */
class MemberListResponse {
  final bool success;
  final List<LedgerMember> members;
  final List<LedgerInvitation> invitations;
  final MemberStatistics stats;
  final PaginationInfo pagination;

  MemberListResponse({
    required this.success,
    required this.members,
    required this.invitations,
    required this.stats,
    required this.pagination,
  });

  factory MemberListResponse.fromJson(Map<String, dynamic> json);
}

/**
 * 80. 對應 5.2：邀請成員請求模型
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 邀請成員請求資料結構
 */
class InviteMembersRequest {
  final String ledgerId;
  final List<String> emails;
  final String defaultRole;
  final String? personalMessage;
  final int expiryDays;

  InviteMembersRequest({
    required this.ledgerId,
    required this.emails,
    required this.defaultRole,
    this.personalMessage,
    required this.expiryDays,
  });

  Map<String, dynamic> toJson();
}

/**
 * 81. 對應 5.2：更新成員角色請求模型
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 更新成員角色請求資料結構
 */
class UpdateMemberRoleRequest {
  final String ledgerId;
  final String memberId;
  final String newRole;
  final List<String> permissions;
  final String? reason;

  UpdateMemberRoleRequest({
    required this.ledgerId,
    required this.memberId,
    required this.newRole,
    required this.permissions,
    this.reason,
  });

  Map<String, dynamic> toJson();
}
```

---

## 6.0 導航路由設計

### 6.1 路由配置
```dart
/**
 * 82. 對應 6.1：成員管理頁面路由配置
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 定義成員管理頁面的路由規則與導航邏輯
 */
class MemberRoutes {
  static const String memberManagement = '/member-management';
  static const String memberDetail = '/member-detail';
  static const String permissionSettings = '/permission-settings';
  static const String inviteHistory = '/invite-history';

  /**
   * 83. 對應 6.1：取得成員管理頁面路由
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 建立成員管理頁面的GoRoute配置
   */
  static GoRoute getMemberManagementRoute();
}

/**
 * 84. 對應 6.1：成員管理導航服務
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 處理成員管理相關的導航邏輯
 */
class MemberNavigationService {
  static final GoRouter _router;
  static GoRouter get router;

  /**
   * 85. 對應 6.1：導航到成員詳情
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 導航到成員詳情頁面
   */
  Future<void> navigateToMemberDetail(String memberId);

  /**
   * 86. 對應 6.1：導航到權限設定
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 導航到權限設定頁面
   */
  Future<void> navigateToPermissionSettings(String ledgerId);

  /**
   * 87. 對應 6.1：導航到邀請歷史
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 導航到邀請歷史頁面
   */
  Future<void> navigateToInviteHistory(String ledgerId);

  /**
   * 88. 對應 6.1：返回帳本詳情
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 返回到帳本詳情頁面
   */
  void navigateBack();
}
```

---

## 7.0 響應式設計

### 7.1 響應式輔助工具
```dart
/**
 * 89. 對應 7.1：成員管理頁面響應式設計輔助工具
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 遵循9005文件的響應式設計規範，提供成員管理頁面專用的佈局管理
 */
class MemberResponsiveHelper {
  final BuildContext context;
  late final MediaQueryData _mediaQuery;
  late final Size _screenSize;

  MemberResponsiveHelper.of(BuildContext context);

  // 斷點判斷屬性（9005規範）
  bool get isMobile;
  bool get isTablet;
  bool get isSmallDesktop;
  bool get isLargeDesktop;
  DeviceType get deviceType;

  // 成員管理頁面專用尺寸屬性
  double get memberCardHeight;
  double get memberCardPadding;
  double get avatarSize;
  double get invitationCardHeight;
  EdgeInsets get sectionPadding;
  double get dialogWidth;
  int get membersPerRow;

  /**
   * 90. 對應 7.1：取得四模式佈局配置
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據使用者模式和裝置類型返回適當佈局配置
   */
  LayoutConfiguration getLayoutForMode(UserMode? mode);

  /**
   * 91. 對應 7.1：取得成員卡片配置
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據螢幕尺寸返回成員卡片配置
   */
  CardConfiguration getMemberCardConfig();

  /**
   * 92. 對應 7.1：取得對話框尺寸
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 根據螢幕尺寸返回適當對話框尺寸
   */
  DialogSize getDialogSize(DialogType type);
}
```

---

## 8.0 無障礙功能

### 8.1 語意化支援
```dart
/**
 * 93. 對應 8.1：成員管理頁面無障礙語意化Widget包裝器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 提供WCAG 2.1 AA標準的完整無障礙支援（9005規範）
 */
class MemberAccessibilityWrapper {

  /**
   * 94. 對應 8.1：為成員卡片提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 為成員卡片添加適當的語意化標籤
   */
  static Widget wrapMemberCard(Widget child, LedgerMember member);

  /**
   * 95. 對應 8.1：為角色標籤提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 為角色標籤添加適當的語意化標籤
   */
  static Widget wrapRoleTag(Widget child, String role, List<String> permissions);

  /**
   * 96. 對應 8.1：為操作按鈕提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 為成員操作按鈕添加適當的語意化標籤
   */
  static Widget wrapActionButton(Widget child, MemberOperation operation, String memberName);

  /**
   * 97. 對應 8.1：為邀請卡片提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 為邀請卡片添加適當的語意化標籤
   */
  static Widget wrapInvitationCard(Widget child, LedgerInvitation invitation);

  /**
   * 98. 對應 8.1：為狀態指示器提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 為成員活動狀態指示器添加適當的語意化標籤
   */
  static Widget wrapStatusIndicator(Widget child, bool isOnline, DateTime lastActive);
}
```

---

## 9.0 效能最佳化

### 9.1 Widget快取策略
```dart
/**
 * 99. 對應 9.1：成員管理頁面Widget快取管理器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 優化成員管理頁面Widget重建效能，減少不必要的重建
 */
class MemberWidgetCache {
  static final Map<String, Widget> _cache = {};
  static final Map<String, DateTime> _cacheTimestamps = {};
  static const Duration _cacheValidDuration = Duration(minutes: 15);

  /**
   * 100. 對應 9.1：取得快取的成員卡片或建立新的
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 從快取中獲取成員卡片Widget或創建新的
   */
  static Widget getMemberCardOrCreate(LedgerMember member, Widget Function() creator);
}
```

---

## 10.0 錯誤處理機制

### 10.1 統一錯誤處理
```dart
/**
 * 101. 對應 10.1：成員管理頁面錯誤處理器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 22:00:00
 * @description 統一處理成員管理相關錯誤
 */
class MemberErrorHandler {

  /**
   * 102. 對應 10.1：處理邀請錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 處理邀請成員錯誤
   */
  static String handleInviteError(dynamic error);

  /**
   * 103. 對應 10.1：處理權限變更錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 處理成員權限變更錯誤
   */
  static String handlePermissionError(dynamic error);

  /**
   * 104. 對應 10.1：處理成員移除錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 處理成員移除錯誤
   */
  static String handleRemovalError(dynamic error);

  /**
   * 105. 對應 10.1：顯示錯誤對話框
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 22:00:00
   * @description 顯示成員管理錯誤對話框
   */
  static Future<void> showErrorDialog(
    BuildContext context,
    String message,
    MemberErrorType errorType,
    {VoidCallback? onRetry}
  );
}
```

---

## 遵循0055設計原則

### 階層架構對應
- 本TDD文件基於P021_成員管理頁面_SRS.md的需求規格
- 與9005. Flutter_Presentation layer.md的產品規格保持一致
- 所有函數節點編號對應上層文件的功能條目

### 設計原則遵循
1. **階層架構邏輯**: 嚴格基於SRS文件定義的功能條目設計
2. **同層級依賴關係**: 考量與帳本詳情頁面和權限設定頁面的依賴關係
3. **代碼節點編號**: 採用不加前綴的節點編號格式（如2.1、3.2）
4. **命名一致性**: 保持與使用者介面操作流程的名稱同步
5. **可讀性設計**: 採用模組化設計，遵循低耦合、高內聚原則

## 文件維護記錄

| 版本 | 日期 | 修改者 | 修改內容 |
|------|------|--------|----------|
| **v1.0.0** | **2025-01-26 22:00:00 +08:00** | **LCAS SA Team** | **初版建立：完整P021成員管理頁面TDD規格，支援成員邀請、角色權限設定、成員活動監控、權限調整、即時狀態更新，實作四模式差異化設計，完整錯誤處理機制，遵循0055設計原則，對應P021 SRS規格** |

---

## 備註

- **純函數宣告**: 完全移除所有實作代碼，僅保留函數名稱、參數定義和註解
- **協作管理功能**: 支援成員邀請、角色權限管理、活動監控、即時狀態更新等完整協作功能
- **四模式差異化**: 根據使用者模式提供不同的成員管理界面複雜度和操作權限
- **安全性優先**: 完整的權限驗證、操作記錄、角色階層限制機制
- **即時性支援**: WebSocket即時狀態更新、線上離線監控、活動追蹤
- **完整技術架構**: Widget分層、狀態管理、API整合、響應式設計、無障礙支援、效能最佳化
- **規範完全對齊**: 與0055、P021 SRS、9005文件完全一致
- **節點編號標準化**: 統一編號01-105，遵循0055規範格式
- **版本統一升級**: 所有組件版本統一為v1.0.0
