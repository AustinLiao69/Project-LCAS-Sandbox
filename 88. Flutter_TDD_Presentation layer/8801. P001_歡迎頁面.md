
# TDD_P001_歡迎頁面_v1.1.0

## 文件資訊
- **文件標題**: P001 歡迎頁面 TDD 測試設計文件
- **文件版本**: v1.1.0
- **創建日期**: 2025-01-26 15:00:00 +08:00 (台灣時間)
- **創建者**: LCAS SA Team
- **最後更新**: 2025-01-26 15:00:00 +08:00 (台灣時間)
- **對應需求文件**: 8701. 歡迎頁面_SRS.md
- **對應PRD文件**: 9005. Flutter_Presentation layer.md
- **頁面代碼**: P001 (Welcome Page)

---

## 📑 目次 (Table of Contents)

1. 測試概述
   - 1.1 測試目標
   - 1.2 測試範圍
   - 1.3 測試架構

2. 功能測試設計
   - 2.1 頁面初始化測試
   - 2.2 模式選擇功能測試
   - 2.3 導航流程測試
   - 2.4 四模式差異化測試

3. UI元件測試設計
   - 3.1 視覺元件測試
   - 3.2 互動元件測試
   - 3.3 響應式佈局測試

4. API整合測試設計
   - 4.1 初始化API測試
   - 4.2 模式設定API測試
   - 4.3 錯誤處理測試

5. 效能測試設計
   - 5.1 載入效能測試
   - 5.2 記憶體使用測試
   - 5.3 動畫效能測試

6. 無障礙測試設計
   - 6.1 螢幕閱讀器測試
   - 6.2 鍵盤導航測試
   - 6.3 色彩對比度測試

7. 跨平台測試設計
   - 7.1 Android平台測試
   - 7.2 iOS平台測試
   - 7.3 不同螢幕尺寸測試

8. 四模式整合測試
   - 8.1 精準控制者模式測試
   - 8.2 紀錄習慣者模式測試
   - 8.3 轉型挑戰者模式測試
   - 8.4 潛在覺醒者模式測試

9. 測試資料與Mock設計

10. 測試執行策略

## 1.0 測試概述

### 1.1 測試目標
P001歡迎頁面作為LCAS 2.0 Flutter應用程式的入口頁面，測試目標如下：
- **功能完整性**：確保模式選擇、登入導航等核心功能正常運作
- **視覺一致性**：驗證四種使用者模式的視覺差異化設計
- **使用者體驗**：保證流暢的操作體驗和直觀的互動流程
- **API整合性**：確保與F004、F009等API端點的正確整合
- **效能標準**：頁面載入時間 < 2秒，動畫流暢度 ≥ 60fps

### 1.2 測試範圍
```
P001歡迎頁面測試範圍：
├── 📱 頁面功能測試
│   ├── 首次啟動流程
│   ├── 再次啟動流程
│   ├── 模式選擇機制
│   └── 導航邏輯
├── 🎨 UI視覺測試
│   ├── 品牌Logo顯示
│   ├── 漸層背景效果
│   ├── 四模式卡片設計
│   └── 按鈕互動效果
├── 🔌 API整合測試
│   ├── F004 帳號刪除功能
│   ├── F009 使用者設定功能
│   └── 初始化檢查API
├── ⚡ 效能測試
│   ├── 頁面載入速度
│   ├── 記憶體使用量
│   └── 動畫流暢度
└── ♿ 無障礙測試
    ├── 螢幕閱讀器支援
    ├── 高對比度模式
    └── 大字體模式
```

### 1.3 測試架構
```dart
// 測試架構設計
class WelcomePageTestSuite {
  // 主要測試分類
  static final List<TestCategory> categories = [
    PageInitializationTests(),      // 頁面初始化測試
    ModeSelectionTests(),          // 模式選擇測試
    NavigationFlowTests(),         // 導航流程測試
    UIComponentTests(),            // UI元件測試
    APIIntegrationTests(),         // API整合測試
    PerformanceTests(),            // 效能測試
    AccessibilityTests(),          // 無障礙測試
    CrossPlatformTests(),          // 跨平台測試
    FourModeTests()               // 四模式差異化測試
  ];
}
```

---

## 2.0 功能測試設計

### 2.1 頁面初始化測試

#### TC-P001-001: 首次啟動初始化測試
```dart
testWidgets('首次啟動應顯示完整歡迎流程', (WidgetTester tester) async {
  // 準備測試資料
  when(mockInitializationAPI.checkFirstLaunch())
      .thenAnswer((_) async => ApiResponse(
        success: true,
        data: {
          'isFirstLaunch': true,
          'userAuthenticated': false,
          'availableModes': ['controller', 'logger', 'struggler', 'sleeper']
        }
      ));

  // 執行測試
  await tester.pumpWidget(TestApp(home: WelcomePage()));
  await tester.pump(); // 等待初始化完成

  // 驗證結果
  expect(find.text('歡迎使用 LCAS 2.0'), findsOneWidget);
  expect(find.byType(ModeSelectionGrid), findsOneWidget);
  expect(find.byType(ModeCard), findsNWidgets(4)); // 四種模式卡片
  expect(find.text('精準控制者'), findsOneWidget);
  expect(find.text('紀錄習慣者'), findsOneWidget);
  expect(find.text('轉型挑戰者'), findsOneWidget);
  expect(find.text('潛在覺醒者'), findsOneWidget);
});
```

#### TC-P001-002: 再次啟動自動跳轉測試
```dart
testWidgets('已登入使用者應自動跳轉至首頁', (WidgetTester tester) async {
  // 準備測試資料
  when(mockInitializationAPI.checkFirstLaunch())
      .thenAnswer((_) async => ApiResponse(
        success: true,
        data: {
          'isFirstLaunch': false,
          'userAuthenticated': true,
          'userId': 'test_user_001'
        }
      ));

  // 執行測試
  await tester.pumpWidget(TestApp(home: WelcomePage()));
  await tester.pump();
  await tester.pump(Duration(seconds: 2)); // 等待自動跳轉

  // 驗證結果
  expect(find.byType(HomePage), findsOneWidget);
  verify(mockNavigationService.pushReplacement('/home')).called(1);
});
```

### 2.2 模式選擇功能測試

#### TC-P001-003: 模式選擇互動測試
```dart
testWidgets('使用者可以選擇並確認模式', (WidgetTester tester) async {
  // 準備測試環境
  await _setupWelcomePage(tester);

  // 測試模式選擇
  await tester.tap(find.text('精準控制者'));
  await tester.pump();

  // 驗證選擇狀態
  final selectedCard = tester.widget<ModeCard>(
    find.ancestor(
      of: find.text('精準控制者'),
      matching: find.byType(ModeCard)
    )
  );
  expect(selectedCard.isSelected, true);

  // 測試確認按鈕
  expect(find.text('開始使用'), findsOneWidget);
  await tester.tap(find.text('開始使用'));
  await tester.pump();

  // 驗證API調用
  verify(mockModeSelectionAPI.setUserMode(
    userId: anyNamed('userId'),
    selectedMode: 'controller',
    deviceInfo: anyNamed('deviceInfo')
  )).called(1);
});
```

#### TC-P001-004: 模式切換測試
```dart
testWidgets('使用者可以在模式間切換選擇', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 先選擇一個模式
  await tester.tap(find.text('紀錄習慣者'));
  await tester.pump();

  // 切換到另一個模式
  await tester.tap(find.text('轉型挑戰者'));
  await tester.pump();

  // 驗證只有最後選擇的模式被選中
  final loggerCard = _findModeCard(tester, '紀錄習慣者');
  final strugglerCard = _findModeCard(tester, '轉型挑戰者');
  
  expect(loggerCard.isSelected, false);
  expect(strugglerCard.isSelected, true);
});
```

### 2.3 導航流程測試

#### TC-P001-005: 導航路徑驗證測試
```dart
testWidgets('模式選擇完成後應導航至正確頁面', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 模擬成功的模式設定API回應
  when(mockModeSelectionAPI.setUserMode(any))
      .thenAnswer((_) async => ApiResponse(
        success: true,
        data: {
          'modeConfig': {
            'modeName': 'controller',
            'uiTheme': {'primaryColor': '#1976D2'},
            'featureFlags': {'advancedMode': true}
          },
          'nextRoute': '/login'
        }
      ));

  // 執行模式選擇流程
  await tester.tap(find.text('精準控制者'));
  await tester.pump();
  await tester.tap(find.text('開始使用'));
  await tester.pump();

  // 驗證導航
  verify(mockNavigationService.pushReplacement('/login')).called(1);
});
```

### 2.4 四模式差異化測試

#### TC-P001-006: 四模式視覺差異驗證
```dart
testWidgets('四種模式應顯示不同的視覺設計', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 檢查精準控制者模式
  final controllerCard = _findModeCard(tester, '精準控制者');
  expect(controllerCard.primaryColor, Color(0xFF1976D2)); // 專業藍
  expect(controllerCard.description, contains('精準控制'));

  // 檢查紀錄習慣者模式
  final loggerCard = _findModeCard(tester, '紀錄習慣者');
  expect(loggerCard.primaryColor, Color(0xFF6A1B9A)); // 優雅紫
  expect(loggerCard.description, contains('美感記帳'));

  // 檢查轉型挑戰者模式
  final strugglerCard = _findModeCard(tester, '轉型挑戰者');
  expect(strugglerCard.primaryColor, Color(0xFFFF6B35)); // 活力橘
  expect(strugglerCard.description, contains('習慣養成'));

  // 檢查潛在覺醒者模式
  final sleeperCard = _findModeCard(tester, '潛在覺醒者');
  expect(sleeperCard.primaryColor, Color(0xFF4CAF50)); // 溫和綠
  expect(sleeperCard.description, contains('簡單開始'));
});
```

---

## 3.0 UI元件測試設計

### 3.1 視覺元件測試

#### TC-P001-007: 品牌Logo顯示測試
```dart
testWidgets('應正確顯示品牌Logo', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 檢查Logo元件
  final logoWidget = find.byType(BrandLogo);
  expect(logoWidget, findsOneWidget);

  // 檢查Logo尺寸
  final RenderBox logoBox = tester.renderObject(logoWidget);
  expect(logoBox.size.width, 120.0);
  expect(logoBox.size.height, 120.0);

  // 檢查Logo位置
  final Offset logoPosition = tester.getTopLeft(logoWidget);
  expect(logoPosition.dy, lessThan(200.0)); // Logo應在頁面上方
});
```

#### TC-P001-008: 漸層背景效果測試
```dart
testWidgets('應顯示正確的漸層背景', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 找到背景容器
  final backgroundContainer = find.byKey(Key('welcome_background'));
  expect(backgroundContainer, findsOneWidget);

  // 驗證漸層設定
  final Container container = tester.widget<Container>(backgroundContainer);
  final BoxDecoration decoration = container.decoration as BoxDecoration;
  final LinearGradient gradient = decoration.gradient as LinearGradient;

  expect(gradient.begin, Alignment.topLeft);
  expect(gradient.end, Alignment.bottomRight);
  expect(gradient.colors.length, 2);
  expect(gradient.colors[0], Color(0xFF667eea));
  expect(gradient.colors[1], Color(0xFF764ba2));
});
```

### 3.2 互動元件測試

#### TC-P001-009: 按鈕互動效果測試
```dart
testWidgets('按鈕應有正確的互動效果', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 選擇一個模式以啟用按鈕
  await tester.tap(find.text('精準控制者'));
  await tester.pump();

  // 找到開始使用按鈕
  final startButton = find.text('開始使用');
  expect(startButton, findsOneWidget);

  // 測試按鈕點擊效果
  await tester.press(startButton);
  await tester.pump(Duration(milliseconds: 100));

  // 驗證按鈕縮放效果
  final RenderTransform buttonTransform = tester.renderObject(
    find.ancestor(of: startButton, matching: find.byType(Transform))
  );
  expect(buttonTransform.transform.getMaxScaleOnAxis(), lessThan(1.0));

  // 釋放按鈕
  await tester.pumpAndSettle();
});
```

### 3.3 響應式佈局測試

#### TC-P001-010: 不同螢幕尺寸佈局測試
```dart
testWidgets('應適應不同螢幕尺寸', (WidgetTester tester) async {
  // 測試手機尺寸 (360x640)
  await tester.binding.setSurfaceSize(Size(360, 640));
  await _setupWelcomePage(tester);

  // 驗證手機佈局
  final mobileGrid = find.byType(ModeSelectionGrid);
  final GridView mobileGridWidget = tester.widget<GridView>(mobileGrid);
  expect(mobileGridWidget.gridDelegate.crossAxisCount, 2); // 2x2網格

  // 測試平板尺寸 (768x1024)
  await tester.binding.setSurfaceSize(Size(768, 1024));
  await tester.pump();

  // 驗證平板佈局
  final tabletGridWidget = tester.widget<GridView>(mobileGrid);
  expect(tabletGridWidget.gridDelegate.crossAxisCount, 4); // 4x1網格

  // 恢復預設尺寸
  await tester.binding.setSurfaceSize(null);
});
```

---

## 4.0 API整合測試設計

### 4.1 初始化API測試

#### TC-P001-011: F004帳號刪除功能檢查測試
```dart
testWidgets('應檢查帳號刪除狀態', (WidgetTester tester) async {
  // 模擬帳號待刪除狀態
  when(mockInitializationAPI.checkFirstLaunch())
      .thenAnswer((_) async => ApiResponse(
        success: true,
        data: {
          'isFirstLaunch': false,
          'userAuthenticated': false,
          'accountDeletionRequired': true,
          'deletionPendingInfo': {
            'scheduledDate': '2025-01-30',
            'reason': 'User requested deletion'
          }
        }
      ));

  await tester.pumpWidget(TestApp(home: WelcomePage()));
  await tester.pump();

  // 驗證顯示帳號刪除警告
  expect(find.text('帳號刪除通知'), findsOneWidget);
  expect(find.text('您的帳號將於 2025-01-30 被刪除'), findsOneWidget);
  expect(find.text('如需取消刪除，請聯繫客服'), findsOneWidget);
});
```

#### TC-P001-012: F009使用者設定功能整合測試
```dart
testWidgets('模式選擇應正確調用使用者設定API', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 準備API回應
  when(mockUserSettingsAPI.updateUserSettings(any))
      .thenAnswer((_) async => ApiResponse(
        success: true,
        data: {'updated': true}
      ));

  // 執行模式選擇
  await tester.tap(find.text('轉型挑戰者'));
  await tester.pump();
  await tester.tap(find.text('開始使用'));
  await tester.pump();

  // 驗證API調用
  final captured = verify(mockUserSettingsAPI.updateUserSettings(
    captureAny
  )).captured;

  final requestData = captured[0] as Map<String, dynamic>;
  expect(requestData['selectedMode'], 'struggler');
  expect(requestData['modeConfig'], isNotNull);
  expect(requestData['timestamp'], isNotNull);
});
```

### 4.2 錯誤處理測試

#### TC-P001-013: API錯誤處理測試
```dart
testWidgets('應正確處理API錯誤', (WidgetTester tester) async {
  // 模擬API錯誤
  when(mockModeSelectionAPI.setUserMode(any))
      .thenThrow(ApiException('網路連線失敗'));

  await _setupWelcomePage(tester);

  // 執行會觸發錯誤的操作
  await tester.tap(find.text('潛在覺醒者'));
  await tester.pump();
  await tester.tap(find.text('開始使用'));
  await tester.pump();

  // 驗證錯誤處理
  expect(find.byType(ErrorDialog), findsOneWidget);
  expect(find.text('網路連線失敗'), findsOneWidget);
  expect(find.text('重試'), findsOneWidget);
  expect(find.text('取消'), findsOneWidget);
});
```

#### TC-P001-014: 網路離線處理測試
```dart
testWidgets('離線狀態應顯示適當提示', (WidgetTester tester) async {
  // 模擬離線狀態
  when(mockConnectivityService.isConnected)
      .thenReturn(false);

  await tester.pumpWidget(TestApp(home: WelcomePage()));
  await tester.pump();

  // 驗證離線提示
  expect(find.byType(OfflineBanner), findsOneWidget);
  expect(find.text('目前處於離線狀態'), findsOneWidget);
  expect(find.text('某些功能可能無法使用'), findsOneWidget);

  // 驗證離線模式選項
  expect(find.text('離線模式'), findsOneWidget);
});
```

---

## 5.0 效能測試設計

### 5.1 載入效能測試

#### TC-P001-015: 頁面載入時間測試
```dart
testWidgets('頁面載入時間應小於2秒', (WidgetTester tester) async {
  final stopwatch = Stopwatch()..start();

  await tester.pumpWidget(TestApp(home: WelcomePage()));
  await tester.pump(); // 第一次渲染
  
  // 等待所有異步操作完成
  await tester.pumpAndSettle();
  
  stopwatch.stop();
  
  // 驗證載入時間
  expect(stopwatch.elapsedMilliseconds, lessThan(2000));
  
  // 驗證頁面完全載入
  expect(find.byType(WelcomePage), findsOneWidget);
  expect(find.byType(LoadingIndicator), findsNothing);
});
```

### 5.2 記憶體使用測試

#### TC-P001-016: 記憶體洩漏測試
```dart
testWidgets('不應有記憶體洩漏', (WidgetTester tester) async {
  // 記錄初始記憶體使用
  final initialMemory = await _getMemoryUsage();

  // 重複建立和銷毀頁面
  for (int i = 0; i < 10; i++) {
    await tester.pumpWidget(TestApp(home: WelcomePage()));
    await tester.pump();
    await tester.pumpWidget(Container()); // 銷毀頁面
    await tester.pump();
  }

  // 強制垃圾回收
  await _forceGarbageCollection();
  
  // 檢查記憶體使用
  final finalMemory = await _getMemoryUsage();
  final memoryIncrease = finalMemory - initialMemory;
  
  // 記憶體增長不應超過50MB
  expect(memoryIncrease, lessThan(50 * 1024 * 1024));
});
```

### 5.3 動畫效能測試

#### TC-P001-017: 動畫流暢度測試
```dart
testWidgets('動畫應維持60fps', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 開始監控幀率
  final frameMonitor = FrameRateMonitor();
  frameMonitor.start();

  // 執行動畫操作
  await tester.tap(find.text('紀錄習慣者'));
  await tester.pump();
  await tester.pump(Duration(milliseconds: 500)); // 等待動畫完成

  frameMonitor.stop();

  // 驗證幀率
  expect(frameMonitor.averageFPS, greaterThanOrEqualTo(60.0));
  expect(frameMonitor.droppedFrames, lessThan(3));
});
```

---

## 6.0 無障礙測試設計

### 6.1 螢幕閱讀器測試

#### TC-P001-018: 語意化標籤測試
```dart
testWidgets('應提供正確的語意化標籤', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 檢查主標題語意
  final titleSemantic = find.byWidgetPredicate((widget) =>
    widget is Semantics &&
    widget.properties.label == '歡迎使用 LCAS 2.0' &&
    widget.properties.header == true
  );
  expect(titleSemantic, findsOneWidget);

  // 檢查模式卡片語意
  final controllerCardSemantic = find.byWidgetPredicate((widget) =>
    widget is Semantics &&
    widget.properties.label!.contains('精準控制者模式') &&
    widget.properties.button == true
  );
  expect(controllerCardSemantic, findsOneWidget);
});
```

#### TC-P001-019: 螢幕閱讀器導航測試
```dart
testWidgets('螢幕閱讀器應能正確導航', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 啟用螢幕閱讀器模式
  tester.binding.defaultBinaryMessenger.setMockMethodCallHandler(
    SystemChannels.accessibility,
    (call) async {
      if (call.method == 'announce') {
        return null;
      }
    }
  );

  // 模擬螢幕閱讀器焦點移動
  await tester.sendKeyEvent(LogicalKeyboardKey.tab);
  await tester.pump();

  // 驗證焦點位置
  final focusedWidget = tester.binding.focusManager.primaryFocus;
  expect(focusedWidget, isNotNull);
});
```

### 6.2 高對比度模式測試

#### TC-P001-020: 高對比度主題測試
```dart
testWidgets('高對比度模式應正確顯示', (WidgetTester tester) async {
  // 啟用高對比度模式
  await tester.pumpWidget(TestApp(
    theme: ThemeData(
      brightness: Brightness.dark,
      colorScheme: ColorScheme.highContrast(),
    ),
    home: WelcomePage()
  ));
  await tester.pump();

  // 檢查高對比度色彩
  final modeCard = _findModeCard(tester, '精準控制者');
  expect(modeCard.borderColor, Color(0xFFFFFFFF)); // 白色邊框
  expect(modeCard.textColor, Color(0xFFFFFFFF)); // 白色文字

  // 檢查背景對比
  final background = find.byKey(Key('welcome_background'));
  final Container backgroundContainer = tester.widget<Container>(background);
  expect(backgroundContainer.color, Color(0xFF000000)); // 黑色背景
});
```

---

## 7.0 跨平台測試設計

### 7.1 Android平台測試

#### TC-P001-021: Android特定功能測試
```dart
testWidgets('Android平台應正確處理返回鍵', (WidgetTester tester) async {
  // 設定Android平台
  debugDefaultTargetPlatformOverride = TargetPlatform.android;

  await _setupWelcomePage(tester);

  // 模擬Android返回鍵
  await tester.binding.defaultBinaryMessenger.handlePlatformMessage(
    'flutter/navigation',
    const StandardMethodCodec().encodeMethodCall(MethodCall('routePopped')),
    (data) {}
  );
  await tester.pump();

  // 驗證應用程式退出確認
  expect(find.byType(ExitConfirmDialog), findsOneWidget);
  expect(find.text('確定要離開應用程式嗎？'), findsOneWidget);

  // 清理
  debugDefaultTargetPlatformOverride = null;
});
```

### 7.2 iOS平台測試

#### TC-P001-022: iOS特定功能測試
```dart
testWidgets('iOS平台應使用Cupertino設計風格', (WidgetTester tester) async {
  // 設定iOS平台
  debugDefaultTargetPlatformOverride = TargetPlatform.iOS;

  await tester.pumpWidget(TestApp(
    home: WelcomePage(),
    cupertinoMode: true
  ));
  await tester.pump();

  // 檢查iOS風格按鈕
  expect(find.byType(CupertinoButton), findsAtLeastNWidgets(1));

  // 檢查iOS風格導航
  expect(find.byType(CupertinoPageRoute), findsOneWidget);

  // 清理
  debugDefaultTargetPlatformOverride = null;
});
```

---

## 8.0 四模式整合測試

### 8.1 精準控制者模式測試

#### TC-P001-023: Controller模式完整流程測試
```dart
testWidgets('精準控制者模式應啟用完整功能', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 選擇精準控制者模式
  await tester.tap(find.text('精準控制者'));
  await tester.pump();

  // 檢查模式特定UI
  final controllerCard = _findModeCard(tester, '精準控制者');
  expect(controllerCard.description, contains('深度分析'));
  expect(controllerCard.featureList, contains('進階功能預覽'));

  // 模擬成功設定
  when(mockModeSelectionAPI.setUserMode(any))
      .thenAnswer((_) async => ApiResponse(
        success: true,
        data: {
          'modeConfig': {
            'modeName': 'controller',
            'featureFlags': {
              'advancedMode': true,
              'professionalUI': true,
              'fullFeatureAccess': true
            }
          }
        }
      ));

  await tester.tap(find.text('開始使用'));
  await tester.pump();

  // 驗證Controller模式設定
  final captured = verify(mockModeSelectionAPI.setUserMode(
    captureAny
  )).captured;
  expect(captured[0]['selectedMode'], 'controller');
});
```

### 8.2 紀錄習慣者模式測試

#### TC-P001-024: Logger模式視覺優化測試
```dart
testWidgets('紀錄習慣者模式應優化視覺體驗', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 選擇紀錄習慣者模式
  await tester.tap(find.text('紀錄習慣者'));
  await tester.pump();

  // 檢查視覺優化
  final loggerCard = _findModeCard(tester, '紀錄習慣者');
  expect(loggerCard.primaryColor, Color(0xFF6A1B9A)); // 優雅紫
  expect(loggerCard.description, contains('美感記帳'));

  // 檢查動畫效果
  expect(find.byType(AnimatedContainer), findsAtLeastNWidgets(1));
  expect(find.byType(FadeTransition), findsAtLeastNWidgets(1));
});
```

### 8.3 轉型挑戰者模式測試

#### TC-P001-025: Struggler模式激勵元素測試
```dart
testWidgets('轉型挑戰者模式應包含激勵元素', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 選擇轉型挑戰者模式
  await tester.tap(find.text('轉型挑戰者'));
  await tester.pump();

  // 檢查激勵元素
  final strugglerCard = _findModeCard(tester, '轉型挑戰者');
  expect(strugglerCard.primaryColor, Color(0xFFFF6B35)); // 活力橘
  expect(strugglerCard.description, contains('目標達成'));
  expect(strugglerCard.motivationalText, isNotNull);

  // 檢查習慣養成功能
  expect(find.text('習慣養成'), findsOneWidget);
  expect(find.text('目標追蹤'), findsOneWidget);
});
```

### 8.4 潛在覺醒者模式測試

#### TC-P001-026: Sleeper模式簡化介面測試
```dart
testWidgets('潛在覺醒者模式應提供極簡介面', (WidgetTester tester) async {
  await _setupWelcomePage(tester);

  // 選擇潛在覺醒者模式
  await tester.tap(find.text('潛在覺醒者'));
  await tester.pump();

  // 檢查簡化設計
  final sleeperCard = _findModeCard(tester, '潛在覺醒者');
  expect(sleeperCard.primaryColor, Color(0xFF4CAF50)); // 溫和綠
  expect(sleeperCard.description, contains('輕鬆記帳'));

  // 檢查按鈕大小（Sleeper模式按鈕更大）
  final startButton = find.text('開始使用');
  final RenderBox buttonBox = tester.renderObject(startButton);
  expect(buttonBox.size.height, greaterThanOrEqualTo(72.0)); // 超大按鈕
});
```

---

## 9.0 測試資料與Mock設計

### 9.1 Mock服務設定
```dart
class WelcomePageTestMocks {
  static MockInitializationAPI mockInitializationAPI;
  static MockModeSelectionAPI mockModeSelectionAPI;
  static MockUserSettingsAPI mockUserSettingsAPI;
  static MockNavigationService mockNavigationService;
  static MockConnectivityService mockConnectivityService;

  static void setupMocks() {
    mockInitializationAPI = MockInitializationAPI();
    mockModeSelectionAPI = MockModeSelectionAPI();
    mockUserSettingsAPI = MockUserSettingsAPI();
    mockNavigationService = MockNavigationService();
    mockConnectivityService = MockConnectivityService();

    // 預設回應設定
    _setupDefaultResponses();
  }

  static void _setupDefaultResponses() {
    when(mockConnectivityService.isConnected).thenReturn(true);
    
    when(mockInitializationAPI.checkFirstLaunch())
        .thenAnswer((_) async => ApiResponse(
          success: true,
          data: {
            'isFirstLaunch': true,
            'userAuthenticated': false,
            'availableModes': ['controller', 'logger', 'struggler', 'sleeper'],
            'accountDeletionRequired': false
          }
        ));
  }
}
```

### 9.2 測試資料集
```dart
class WelcomePageTestData {
  static final Map<String, dynamic> controllerModeConfig = {
    'modeName': 'controller',
    'displayName': '精準控制者',
    'description': '適合需要深度分析的進階使用者',
    'primaryColor': '#1976D2',
    'featureFlags': {
      'advancedMode': true,
      'professionalUI': true,
      'fullFeatureAccess': true
    },
    'uiConfig': {
      'density': 'high',
      'complexity': 'advanced'
    }
  };

  static final Map<String, dynamic> loggerModeConfig = {
    'modeName': 'logger',
    'displayName': '紀錄習慣者',
    'description': '重視美觀體驗的記帳愛好者',
    'primaryColor': '#6A1B9A',
    'featureFlags': {
      'elegantUI': true,
      'visualOptimization': true,
      'smoothAnimations': true
    },
    'uiConfig': {
      'density': 'medium',
      'complexity': 'balanced'
    }
  };

  // ... 其他模式配置
}
```

---

## 10.0 測試執行策略

### 10.1 測試分層執行
```
測試執行順序：
1. 單元測試 (Unit Tests)
   ├── 個別元件功能測試
   ├── API整合點測試
   └── 資料處理邏輯測試

2. 整合測試 (Integration Tests)
   ├── 頁面完整流程測試
   ├── 跨元件互動測試
   └── API端到端測試

3. UI測試 (Widget Tests)
   ├── 視覺元件顯示測試
   ├── 使用者互動測試
   └── 響應式佈局測試

4. 效能測試 (Performance Tests)
   ├── 載入速度測試
   ├── 記憶體使用測試
   └── 動畫流暢度測試

5. 平台測試 (Platform Tests)
   ├── Android特定功能測試
   ├── iOS特定功能測試
   └── 跨平台一致性測試
```

### 10.2 測試覆蓋率目標
- **功能覆蓋率**: ≥ 95%
- **程式碼覆蓋率**: ≥ 90%
- **分支覆蓋率**: ≥ 85%
- **UI元件覆蓋率**: ≥ 100%

### 10.3 CI/CD整合
```yaml
# P001歡迎頁面測試管道
welcome_page_tests:
  stage: test
  script:
    - flutter test test/presentation/welcome_page/
    - flutter test integration_test/welcome_page_flow_test.dart
    - flutter test test/performance/welcome_page_performance_test.dart
  coverage: '/lines......: \d+\.\d+%/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage/lcov.info
```

---

## 文件維護記錄

| 版本 | 日期 | 修改者 | 修改內容 |
|------|------|--------|----------|
| **v1.1.0** | **2025-01-26 15:00:00 +08:00** | **LCAS SA Team** | **完整建立P001歡迎頁面TDD測試設計文件，包含功能測試、UI測試、API整合測試、效能測試、無障礙測試、跨平台測試及四模式差異化測試，確保與8701 SRS文件和9005 PRD文件完全一致** |

---

## 備註

- **測試優先級**：核心功能測試 > 視覺測試 > 效能測試 > 平台特定測試
- **測試環境**：Flutter測試框架 + Mockito + Golden測試
- **四模式測試**：每種模式都有專屬的測試案例驗證
- **API整合**：完整覆蓋F004、F009等相關API端點
- **無障礙支援**：WCAG 2.1 AA標準的測試驗證
- **跨平台一致性**：確保Android和iOS平台功能一致
- **持續優化**：依據測試結果持續改進頁面實作
