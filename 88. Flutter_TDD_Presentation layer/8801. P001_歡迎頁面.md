
# 8801. TDD_P001_歡迎頁面_v2.0.0

## 文件資訊
- **文件標題**: P001 歡迎頁面技術設計文件
- **文件版本**: v2.0.0
- **創建日期**: 2025-08-06 12:00:00 +08:00 (台灣時間)
- **創建者**: LCAS SA Team
- **最後更新**: 2025-08-06 12:00:00 +08:00 (台灣時間)
- **對應需求文件**: 8701. 歡迎頁面_SRS.md
- **對應PRD文件**: 9005. Flutter_Presentation layer.md
- **頁面代碼**: P001 (Welcome Page)

---

## 📑 目次 (Table of Contents)

1. [技術架構概述](#10-技術架構概述)
2. [Widget清單](#20-widget清單)
3. [核心Widget設計](#30-核心widget設計)
4. [狀態管理架構](#40-狀態管理架構)
5. [API整合設計](#50-api整合設計)
6. [導航路由設計](#60-導航路由設計)
7. [響應式設計](#70-響應式設計)
8. [無障礙功能](#80-無障礙功能)
9. [效能最佳化](#90-效能最佳化)
10. [錯誤處理機制](#100-錯誤處理機制)

---

## 1.0 技術架構概述

### 1.1 模組定位
P001歡迎頁面作為LCAS 2.0 Flutter應用程式的入口頁面，負責使用者首次體驗、模式選擇、品牌展示及導航路由分發。本頁面採用MVP原則設計，聚焦核心功能實現。

### 1.2 技術堆疊
- **前端框架**: Flutter 3.x
- **狀態管理**: Provider + ChangeNotifier
- **路由管理**: GoRouter
- **HTTP客戶端**: Dio
- **本地儲存**: SharedPreferences
- **動畫處理**: Flutter內建Animation API
- **無障礙支援**: Flutter Semantics API

### 1.3 模組架構
```dart
/**
 * P001_歡迎頁面_2.0.0
 * @module P001歡迎頁面
 * @description Flutter Presentation Layer入口頁面 - 模式選擇、品牌展示、導航分發
 * @update 2025-08-06: 升級至v2.0版本，重構為技術設計文件，聚焦MVP核心功能
 */
```

---

## 2.0 Widget清單

### 2.1 完整Widget索引表

| # | Widget名稱 | 版本 | 所屬層級 | 功能描述 | 相依組件 |
|---|------------|------|----------|----------|----------|
| 01 | WelcomePage | V2.0.0 | 頁面容器層 | 主頁面容器及生命週期管理 | WelcomeProvider、Router |
| 02 | WelcomeScaffold | V2.0.0 | 佈局結構層 | 基礎頁面結構與背景設定 | GradientBackground |
| 03 | BrandSection | V2.0.0 | 品牌展示層 | Logo與品牌標題顯示 | BrandLogo、BrandTitle |
| 04 | ModeSelectionSection | V2.0.0 | 模式選擇層 | 四模式選擇介面 | ModeGrid、ModeCard |
| 05 | NavigationSection | V2.0.0 | 導航操作層 | 開始使用按鈕與導航控制 | ActionButton、LoadingIndicator |
| 06 | BrandLogo | V2.0.0 | 視覺元件層 | 應用程式品牌Logo | Image、AnimatedContainer |
| 07 | BrandTitle | V2.0.0 | 視覺元件層 | 品牌標題與副標題文字 | Text、AnimatedOpacity |
| 08 | ModeGrid | V2.0.0 | 佈局元件層 | 四模式卡片網格佈局 | GridView、ResponsiveWrapper |
| 09 | ModeCard | V2.0.0 | 互動元件層 | 單一模式選擇卡片 | InkWell、AnimatedScale |
| 10 | ActionButton | V2.0.0 | 互動元件層 | 主要操作按鈕（開始使用） | ElevatedButton、AnimatedContainer |
| 11 | GradientBackground | V2.0.0 | 裝飾元件層 | 頁面漸層背景效果 | Container、LinearGradient |
| 12 | LoadingIndicator | V2.0.0 | 狀態元件層 | 載入中指示器 | CircularProgressIndicator |
| 13 | ErrorDialog | V2.0.0 | 錯誤處理層 | 錯誤訊息對話框 | AlertDialog、TextButton |

### 2.2 Widget分層架構

```
P001歡迎頁面 (v2.0.0)
├── 頁面容器層 (1個Widget)
│   └── WelcomePage() - 主頁面容器及狀態管理
├── 佈局結構層 (1個Widget)
│   └── WelcomeScaffold() - 基礎頁面結構
├── 功能區塊層 (3個Widget)
│   ├── BrandSection() - 品牌展示區塊
│   ├── ModeSelectionSection() - 模式選擇區塊
│   └── NavigationSection() - 導航操作區塊
├── 視覺元件層 (2個Widget)
│   ├── BrandLogo() - 品牌Logo
│   └── BrandTitle() - 品牌標題文字
├── 佈局元件層 (1個Widget)
│   └── ModeGrid() - 模式卡片網格
├── 互動元件層 (2個Widget)
│   ├── ModeCard() - 模式選擇卡片
│   └── ActionButton() - 主要操作按鈕
└── 基礎元件層 (3個Widget)
    ├── GradientBackground() - 漸層背景
    ├── LoadingIndicator() - 載入指示器
    └── ErrorDialog() - 錯誤對話框
```

---

## 3.0 核心Widget設計

### 3.1 頁面容器層Widget

#### 3.1.1 WelcomePage Widget
```dart
/**
 * 01. 主頁面容器Widget
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 歡迎頁面的主容器，負責狀態管理、生命週期控制及頁面初始化
 */
class WelcomePage extends StatefulWidget {
  static const String routeName = '/welcome';
  
  const WelcomePage({Key? key}) : super(key: key);

  @override
  State<WelcomePage> createState() => _WelcomePageState();
}

class _WelcomePageState extends State<WelcomePage> 
    with SingleTickerProviderStateMixin {
  
  late AnimationController _animationController;
  late Animation<double> _fadeAnimation;
  
  @override
  void initState() {
    super.initState();
    _initializeAnimations();
    _initializePage();
  }
  
  void _initializeAnimations() {
    _animationController = AnimationController(
      duration: const Duration(milliseconds: 1500),
      vsync: this,
    );
    
    _fadeAnimation = Tween<double>(
      begin: 0.0,
      end: 1.0,
    ).animate(CurvedAnimation(
      parent: _animationController,
      curve: Curves.easeInOut,
    ));
    
    _animationController.forward();
  }
  
  Future<void> _initializePage() async {
    final provider = Provider.of<WelcomeProvider>(context, listen: false);
    await provider.initializePage();
  }
  
  @override
  Widget build(BuildContext context) {
    return ChangeNotifierProvider(
      create: (_) => WelcomeProvider(),
      child: Consumer<WelcomeProvider>(
        builder: (context, provider, child) {
          return WelcomeScaffold(
            fadeAnimation: _fadeAnimation,
            provider: provider,
          );
        },
      ),
    );
  }
  
  @override
  void dispose() {
    _animationController.dispose();
    super.dispose();
  }
}
```

### 3.2 佈局結構層Widget

#### 3.2.1 WelcomeScaffold Widget
```dart
/**
 * 02. 頁面佈局結構Widget
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 提供歡迎頁面的基礎佈局結構與響應式設計
 */
class WelcomeScaffold extends StatelessWidget {
  final Animation<double> fadeAnimation;
  final WelcomeProvider provider;
  
  const WelcomeScaffold({
    Key? key,
    required this.fadeAnimation,
    required this.provider,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final responsive = ResponsiveHelper.of(context);
    
    return Scaffold(
      body: Stack(
        children: [
          // 漸層背景
          const GradientBackground(),
          
          // 主要內容
          FadeTransition(
            opacity: fadeAnimation,
            child: SafeArea(
              child: Padding(
                padding: EdgeInsets.symmetric(
                  horizontal: responsive.horizontalPadding,
                  vertical: responsive.verticalPadding,
                ),
                child: Column(
                  children: [
                    // 品牌展示區塊
                    Expanded(
                      flex: 2,
                      child: BrandSection(),
                    ),
                    
                    // 模式選擇區塊
                    Expanded(
                      flex: 3,
                      child: ModeSelectionSection(provider: provider),
                    ),
                    
                    // 導航操作區塊
                    Expanded(
                      flex: 1,
                      child: NavigationSection(provider: provider),
                    ),
                  ],
                ),
              ),
            ),
          ),
          
          // 載入覆蓋層
          if (provider.isLoading)
            Container(
              color: Colors.black54,
              child: const Center(
                child: LoadingIndicator(),
              ),
            ),
        ],
      ),
    );
  }
}
```

### 3.3 功能區塊層Widget

#### 3.3.1 BrandSection Widget
```dart
/**
 * 03. 品牌展示區塊Widget
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 顯示應用程式Logo與品牌標題
 */
class BrandSection extends StatelessWidget {
  const BrandSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        // Logo
        const BrandLogo(),
        
        SizedBox(height: 24.h),
        
        // 標題與副標題
        const BrandTitle(),
      ],
    );
  }
}
```

#### 3.3.2 ModeSelectionSection Widget
```dart
/**
 * 04. 模式選擇區塊Widget
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 四模式選擇介面，支援MVP精簡模式
 */
class ModeSelectionSection extends StatelessWidget {
  final WelcomeProvider provider;
  
  const ModeSelectionSection({
    Key? key,
    required this.provider,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // 選擇提示文字
        Text(
          '選擇適合您的使用模式',
          style: TextStyle(
            fontSize: 18.sp,
            color: Colors.white,
            fontWeight: FontWeight.w500,
          ),
          textAlign: TextAlign.center,
        ),
        
        SizedBox(height: 24.h),
        
        // 模式選擇網格
        Expanded(
          child: ModeGrid(
            selectedMode: provider.selectedMode,
            onModeSelected: provider.selectMode,
          ),
        ),
      ],
    );
  }
}
```

### 3.4 互動元件層Widget

#### 3.4.1 ModeCard Widget
```dart
/**
 * 09. 模式選擇卡片Widget
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 單一模式選擇卡片，支援四模式差異化設計
 */
class ModeCard extends StatefulWidget {
  final UserMode mode;
  final bool isSelected;
  final VoidCallback onTap;
  
  const ModeCard({
    Key? key,
    required this.mode,
    required this.isSelected,
    required this.onTap,
  }) : super(key: key);

  @override
  State<ModeCard> createState() => _ModeCardState();
}

class _ModeCardState extends State<ModeCard> 
    with SingleTickerProviderStateMixin {
  
  late AnimationController _scaleController;
  late Animation<double> _scaleAnimation;
  
  @override
  void initState() {
    super.initState();
    _scaleController = AnimationController(
      duration: const Duration(milliseconds: 200),
      vsync: this,
    );
    _scaleAnimation = Tween<double>(
      begin: 1.0,
      end: 1.05,
    ).animate(CurvedAnimation(
      parent: _scaleController,
      curve: Curves.easeInOut,
    ));
  }

  @override
  Widget build(BuildContext context) {
    final modeConfig = _getModeConfig(widget.mode);
    
    return AnimatedBuilder(
      animation: _scaleAnimation,
      builder: (context, child) {
        return Transform.scale(
          scale: widget.isSelected ? _scaleAnimation.value : 1.0,
          child: InkWell(
            onTap: () {
              _scaleController.forward().then((_) {
                _scaleController.reverse();
              });
              widget.onTap();
            },
            child: AnimatedContainer(
              duration: const Duration(milliseconds: 300),
              padding: EdgeInsets.all(16.w),
              decoration: BoxDecoration(
                color: widget.isSelected 
                    ? modeConfig.selectedColor 
                    : modeConfig.normalColor,
                borderRadius: BorderRadius.circular(16.r),
                border: Border.all(
                  color: widget.isSelected 
                      ? Colors.white 
                      : Colors.transparent,
                  width: 2.w,
                ),
                boxShadow: widget.isSelected ? [
                  BoxShadow(
                    color: Colors.black26,
                    blurRadius: 8.r,
                    offset: Offset(0, 4.h),
                  ),
                ] : null,
              ),
              child: Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  // 模式圖示
                  Icon(
                    modeConfig.icon,
                    size: 32.sp,
                    color: Colors.white,
                  ),
                  
                  SizedBox(height: 8.h),
                  
                  // 模式名稱
                  Text(
                    modeConfig.displayName,
                    style: TextStyle(
                      fontSize: 14.sp,
                      fontWeight: FontWeight.w600,
                      color: Colors.white,
                    ),
                    textAlign: TextAlign.center,
                  ),
                  
                  SizedBox(height: 4.h),
                  
                  // 模式描述
                  Text(
                    modeConfig.shortDescription,
                    style: TextStyle(
                      fontSize: 10.sp,
                      color: Colors.white70,
                    ),
                    textAlign: TextAlign.center,
                    maxLines: 2,
                    overflow: TextOverflow.ellipsis,
                  ),
                ],
              ),
            ),
          ),
        );
      },
    );
  }
  
  ModeConfig _getModeConfig(UserMode mode) {
    switch (mode) {
      case UserMode.controller:
        return ModeConfig(
          displayName: '精準控制者',
          shortDescription: '深度分析\n專業操作',
          icon: Icons.analytics,
          normalColor: const Color(0x331976D2),
          selectedColor: const Color(0x661976D2),
        );
      case UserMode.logger:
        return ModeConfig(
          displayName: '紀錄習慣者',
          shortDescription: '優雅美觀\n習慣記錄',
          icon: Icons.auto_awesome,
          normalColor: const Color(0x336A1B9A),
          selectedColor: const Color(0x666A1B9A),
        );
      case UserMode.struggler:
        return ModeConfig(
          displayName: '轉型挑戰者',
          shortDescription: '目標導向\n習慣養成',
          icon: Icons.trending_up,
          normalColor: const Color(0x33FF6B35),
          selectedColor: const Color(0x66FF6B35),
        );
      case UserMode.sleeper:
        return ModeConfig(
          displayName: '潛在覺醒者',
          shortDescription: '簡單易用\n輕鬆開始',
          icon: Icons.eco,
          normalColor: const Color(0x334CAF50),
          selectedColor: const Color(0x664CAF50),
        );
    }
  }
  
  @override
  void dispose() {
    _scaleController.dispose();
    super.dispose();
  }
}
```

---

## 4.0 狀態管理架構

### 4.1 WelcomeProvider 狀態管理器
```dart
/**
 * 歡迎頁面狀態管理器
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 管理歡迎頁面的狀態、模式選擇、API調用等邏輯
 */
class WelcomeProvider extends ChangeNotifier {
  // 頁面狀態
  WelcomePageState _pageState = WelcomePageState.loading;
  WelcomePageState get pageState => _pageState;
  
  // 使用者選擇的模式
  UserMode? _selectedMode;
  UserMode? get selectedMode => _selectedMode;
  
  // 載入狀態
  bool _isLoading = false;
  bool get isLoading => _isLoading;
  
  // 錯誤訊息
  String? _errorMessage;
  String? get errorMessage => _errorMessage;
  
  // 是否為首次啟動
  bool _isFirstLaunch = true;
  bool get isFirstLaunch => _isFirstLaunch;
  
  // API服務
  final WelcomeApiService _apiService = WelcomeApiService();
  final NavigationService _navigationService = NavigationService();
  final PreferencesService _preferencesService = PreferencesService();

  /// 初始化頁面
  Future<void> initializePage() async {
    try {
      _setLoading(true);
      
      // 檢查是否為首次啟動
      _isFirstLaunch = await _preferencesService.isFirstLaunch();
      
      if (!_isFirstLaunch) {
        // 檢查登入狀態
        final isLoggedIn = await _preferencesService.isLoggedIn();
        if (isLoggedIn) {
          // 已登入用戶直接跳轉
          await _navigationService.navigateToHome();
          return;
        }
      }
      
      // 檢查帳號刪除狀態 (對應F004)
      await _checkAccountDeletionStatus();
      
      _setPageState(WelcomePageState.modeSelection);
      
    } catch (e) {
      _setError('頁面初始化失敗: ${e.toString()}');
      _setPageState(WelcomePageState.error);
    } finally {
      _setLoading(false);
    }
  }

  /// 檢查帳號刪除狀態
  Future<void> _checkAccountDeletionStatus() async {
    try {
      final response = await _apiService.checkInitialization();
      
      if (response.data['accountDeletionRequired'] == true) {
        final deletionInfo = response.data['deletionPendingInfo'];
        _setError('您的帳號將於 ${deletionInfo['scheduledDate']} 被刪除\n如需取消刪除，請聯繫客服');
      }
    } catch (e) {
      // 檢查失敗不影響主流程
      debugPrint('帳號刪除狀態檢查失敗: $e');
    }
  }

  /// 選擇模式
  void selectMode(UserMode mode) {
    if (_selectedMode == mode) {
      _selectedMode = null;
    } else {
      _selectedMode = mode;
    }
    notifyListeners();
  }

  /// 開始使用 - 模式設定與導航
  Future<void> startApp() async {
    if (_selectedMode == null) {
      _setError('請先選擇一個使用模式');
      return;
    }
    
    try {
      _setLoading(true);
      
      // 調用模式設定API (對應F009)
      await _setUserMode(_selectedMode!);
      
      // 設定首次啟動標記
      await _preferencesService.setFirstLaunchCompleted();
      
      // 導航到登入頁面
      await _navigationService.navigateToLogin();
      
    } catch (e) {
      _setError('模式設定失敗: ${e.toString()}');
    } finally {
      _setLoading(false);
    }
  }

  /// 設定使用者模式
  Future<void> _setUserMode(UserMode mode) async {
    final request = ModeSelectionRequest(
      selectedMode: mode.name,
      deviceInfo: await _getDeviceInfo(),
    );
    
    final response = await _apiService.setUserMode(request);
    
    if (!response.success) {
      throw Exception('模式設定失敗');
    }
    
    // 儲存模式配置到本地
    await _preferencesService.saveModeConfig(response.data['modeConfig']);
  }

  /// 取得裝置資訊
  Future<Map<String, dynamic>> _getDeviceInfo() async {
    return {
      'platform': Platform.isAndroid ? 'android' : 'ios',
      'version': await _getAppVersion(),
      'deviceId': await _getDeviceId(),
    };
  }

  /// 設定載入狀態
  void _setLoading(bool loading) {
    _isLoading = loading;
    notifyListeners();
  }

  /// 設定頁面狀態
  void _setPageState(WelcomePageState state) {
    _pageState = state;
    notifyListeners();
  }

  /// 設定錯誤訊息
  void _setError(String message) {
    _errorMessage = message;
    notifyListeners();
  }

  /// 清除錯誤
  void clearError() {
    _errorMessage = null;
    notifyListeners();
  }
}
```

### 4.2 狀態枚舉定義
```dart
/// 歡迎頁面狀態枚舉
enum WelcomePageState {
  loading,        // 載入中
  modeSelection,  // 模式選擇
  error,          // 錯誤狀態
}

/// 使用者模式枚舉
enum UserMode {
  controller,  // 精準控制者
  logger,      // 紀錄習慣者
  struggler,   // 轉型挑戰者
  sleeper,     // 潛在覺醒者
}
```

---

## 5.0 API整合設計

### 5.1 WelcomeApiService API服務
```dart
/**
 * 歡迎頁面API服務
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 處理歡迎頁面相關的API調用，對應F004、F009端點
 */
class WelcomeApiService {
  final ApiClient _apiClient = ApiClient();

  /// 檢查應用程式初始化狀態 (對應F004)
  Future<ApiResponse<Map<String, dynamic>>> checkInitialization() async {
    try {
      final response = await _apiClient.get(
        '/app/initialization',
        queryParameters: {
          'deviceInfo': await _getDeviceInfo(),
          'accountDeletionCheck': true,
        },
      );
      
      return ApiResponse.success(response.data);
    } catch (e) {
      return ApiResponse.error(e.toString());
    }
  }

  /// 設定使用者模式 (對應F009)
  Future<ApiResponse<Map<String, dynamic>>> setUserMode(
    ModeSelectionRequest request,
  ) async {
    try {
      final response = await _apiClient.post(
        '/app/user/mode-selection',
        data: request.toJson(),
      );
      
      return ApiResponse.success(response.data['data']);
    } catch (e) {
      return ApiResponse.error(e.toString());
    }
  }

  /// 取得裝置資訊
  Future<Map<String, dynamic>> _getDeviceInfo() async {
    final deviceInfo = DeviceInfoPlugin();
    
    if (Platform.isAndroid) {
      final androidInfo = await deviceInfo.androidInfo;
      return {
        'platform': 'android',
        'model': androidInfo.model,
        'version': androidInfo.version.release,
        'deviceId': androidInfo.androidId,
      };
    } else {
      final iosInfo = await deviceInfo.iosInfo;
      return {
        'platform': 'ios',
        'model': iosInfo.model,
        'version': iosInfo.systemVersion,
        'deviceId': iosInfo.identifierForVendor,
      };
    }
  }
}
```

### 5.2 資料模型定義
```dart
/// 模式選擇請求模型
class ModeSelectionRequest {
  final String selectedMode;
  final Map<String, dynamic> deviceInfo;

  ModeSelectionRequest({
    required this.selectedMode,
    required this.deviceInfo,
  });

  Map<String, dynamic> toJson() => {
    'selectedMode': selectedMode,
    'deviceInfo': deviceInfo,
  };
}

/// API回應統一格式
class ApiResponse<T> {
  final bool success;
  final T? data;
  final String? error;

  ApiResponse.success(this.data) : success = true, error = null;
  ApiResponse.error(this.error) : success = false, data = null;
}
```

---

## 6.0 導航路由設計

### 6.1 路由配置
```dart
/**
 * 歡迎頁面路由配置
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 定義歡迎頁面的路由規則與導航邏輯
 */
class WelcomeRoutes {
  static const String welcome = '/welcome';
  static const String login = '/login';
  static const String home = '/home';

  static GoRoute getWelcomeRoute() {
    return GoRoute(
      path: welcome,
      name: 'welcome',
      builder: (context, state) => const WelcomePage(),
    );
  }
}

/// 導航服務
class NavigationService {
  static final GoRouter _router = GoRouter(
    initialLocation: '/welcome',
    routes: [
      WelcomeRoutes.getWelcomeRoute(),
      LoginRoutes.getLoginRoute(),
      HomeRoutes.getHomeRoute(),
    ],
  );

  static GoRouter get router => _router;

  /// 導航到登入頁面
  Future<void> navigateToLogin() async {
    _router.pushReplacement('/login');
  }

  /// 導航到首頁
  Future<void> navigateToHome() async {
    _router.pushReplacement('/home');
  }

  /// 返回上一頁
  void goBack() {
    _router.pop();
  }
}
```

---

## 7.0 響應式設計

### 7.1 響應式輔助工具
```dart
/**
 * 響應式設計輔助工具
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 提供響應式設計的尺寸計算與佈局調整
 */
class ResponsiveHelper {
  final BuildContext context;
  late final MediaQueryData _mediaQuery;
  late final Size _screenSize;

  ResponsiveHelper.of(BuildContext context) : context = context {
    _mediaQuery = MediaQuery.of(context);
    _screenSize = _mediaQuery.size;
  }

  /// 是否為手機尺寸
  bool get isMobile => _screenSize.width < 600;

  /// 是否為平板尺寸
  bool get isTablet => _screenSize.width >= 600 && _screenSize.width < 1024;

  /// 是否為桌面尺寸
  bool get isDesktop => _screenSize.width >= 1024;

  /// 水平內邊距
  double get horizontalPadding {
    if (isMobile) return 16.w;
    if (isTablet) return 32.w;
    return 48.w;
  }

  /// 垂直內邊距
  double get verticalPadding {
    if (isMobile) return 24.h;
    if (isTablet) return 32.h;
    return 40.h;
  }

  /// 模式網格列數
  int get modeGridCrossAxisCount {
    if (isMobile) return 2; // 2x2
    if (isTablet) return 4; // 4x1
    return 4; // 4x1
  }

  /// 模式卡片高度
  double get modeCardHeight {
    if (isMobile) return 120.h;
    if (isTablet) return 100.h;
    return 90.h;
  }
}
```

### 7.2 斷點管理
```dart
/// 響應式斷點常數
class Breakpoints {
  static const double mobile = 600;
  static const double tablet = 1024;
  static const double desktop = 1440;

  /// 取得目前裝置類型
  static DeviceType getDeviceType(double width) {
    if (width < mobile) return DeviceType.mobile;
    if (width < tablet) return DeviceType.tablet;
    return DeviceType.desktop;
  }
}

enum DeviceType { mobile, tablet, desktop }
```

---

## 8.0 無障礙功能

### 8.1 語意化支援
```dart
/**
 * 無障礙語意化Widget包裝器
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 為歡迎頁面提供完整的無障礙支援
 */
class AccessibilityWrapper {
  
  /// 為模式卡片提供語意化標籤
  static Widget wrapModeCard(Widget child, UserMode mode) {
    return Semantics(
      button: true,
      enabled: true,
      label: _getModeSemanticLabel(mode),
      hint: '雙擊選擇此模式',
      child: child,
    );
  }

  /// 為品牌Logo提供語意化標籤
  static Widget wrapBrandLogo(Widget child) {
    return Semantics(
      image: true,
      label: 'LCAS 2.0 應用程式標誌',
      child: child,
    );
  }

  /// 為開始按鈕提供語意化標籤
  static Widget wrapStartButton(Widget child, {required bool enabled}) {
    return Semantics(
      button: true,
      enabled: enabled,
      label: '開始使用',
      hint: enabled ? '雙擊開始使用應用程式' : '請先選擇一個使用模式',
      child: child,
    );
  }

  /// 取得模式的語意化標籤
  static String _getModeSemanticLabel(UserMode mode) {
    switch (mode) {
      case UserMode.controller:
        return '精準控制者模式，適合需要深度分析和專業操作的使用者';
      case UserMode.logger:
        return '紀錄習慣者模式，重視優雅美觀體驗的記帳愛好者';
      case UserMode.struggler:
        return '轉型挑戰者模式，專注目標導向和習慣養成的使用者';
      case UserMode.sleeper:
        return '潛在覺醒者模式，偏好簡單易用功能的入門使用者';
    }
  }
}
```

### 8.2 色彩對比度支援
```dart
/// 高對比度主題支援
class HighContrastTheme {
  
  /// 檢查是否啟用高對比度模式
  static bool isHighContrastEnabled(BuildContext context) {
    return MediaQuery.of(context).highContrast;
  }

  /// 取得高對比度色彩
  static Color getContrastColor(BuildContext context, Color normalColor) {
    if (isHighContrastEnabled(context)) {
      return normalColor.computeLuminance() > 0.5 
          ? Colors.black 
          : Colors.white;
    }
    return normalColor;
  }

  /// 取得高對比度背景色
  static Color getContrastBackgroundColor(BuildContext context) {
    if (isHighContrastEnabled(context)) {
      return Colors.white;
    }
    return Colors.transparent;
  }
}
```

---

## 9.0 效能最佳化

### 9.1 Widget快取策略
```dart
/**
 * Widget快取管理器
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 優化Widget重建效能，減少不必要的重建
 */
class WidgetCache {
  static final Map<String, Widget> _cache = {};

  /// 取得快取的Widget
  static Widget getOrCreate(String key, Widget Function() creator) {
    if (_cache.containsKey(key)) {
      return _cache[key]!;
    }
    
    final widget = creator();
    _cache[key] = widget;
    return widget;
  }

  /// 清除快取
  static void clearCache() {
    _cache.clear();
  }
}
```

### 9.2 圖片預載入
```dart
/// 圖片預載入管理器
class ImagePreloader {
  static final Set<String> _preloadedImages = {};

  /// 預載入品牌Logo
  static Future<void> preloadBrandAssets(BuildContext context) async {
    const assets = [
      'assets/images/logo.png',
      'assets/images/brand_background.png',
    ];

    for (final asset in assets) {
      if (!_preloadedImages.contains(asset)) {
        await precacheImage(AssetImage(asset), context);
        _preloadedImages.add(asset);
      }
    }
  }
}
```

### 9.3 記憶體管理
```dart
/// 記憶體使用監控
class MemoryMonitor {
  
  /// 監控記憶體使用
  static void monitorMemoryUsage() {
    Timer.periodic(const Duration(seconds: 30), (timer) {
      final usage = ProcessInfo.currentRss;
      if (usage > 100 * 1024 * 1024) { // 100MB
        debugPrint('警告: 記憶體使用量過高: ${usage ~/ 1024 ~/ 1024}MB');
        _performGarbageCollection();
      }
    });
  }

  /// 強制垃圾回收
  static void _performGarbageCollection() {
    WidgetCache.clearCache();
    // 其他清理操作
  }
}
```

---

## 10.0 錯誤處理機制

### 10.1 統一錯誤處理
```dart
/**
 * 歡迎頁面錯誤處理器
 * @version 2025-08-06-V2.0.0
 * @date 2025-08-06 12:00:00
 * @description 統一處理歡迎頁面的各類錯誤情況
 */
class WelcomeErrorHandler {
  
  /// 處理API錯誤
  static String handleApiError(dynamic error) {
    if (error is DioError) {
      switch (error.type) {
        case DioErrorType.connectTimeout:
        case DioErrorType.receiveTimeout:
          return '網路連線逾時，請檢查網路設定';
        case DioErrorType.response:
          return _handleHttpError(error.response?.statusCode);
        case DioErrorType.other:
          return '網路連線失敗，請檢查網路狀態';
        default:
          return '未知網路錯誤';
      }
    }
    
    return error.toString();
  }

  /// 處理HTTP狀態碼錯誤
  static String _handleHttpError(int? statusCode) {
    switch (statusCode) {
      case 400:
        return '請求參數錯誤';
      case 401:
        return '認證失敗，請重新登入';
      case 403:
        return '存取權限不足';
      case 404:
        return '服務不存在';
      case 500:
        return '伺服器內部錯誤';
      case 502:
        return '伺服器暫時無法回應';
      case 503:
        return '服務暫時不可用';
      default:
        return '伺服器回應錯誤 ($statusCode)';
    }
  }

  /// 處理模式選擇錯誤
  static String handleModeSelectionError(dynamic error) {
    if (error.toString().contains('network')) {
      return '網路連線不穩定，請稍後重試';
    }
    
    if (error.toString().contains('validation')) {
      return '模式設定參數錯誤，請重新選擇';
    }
    
    return '模式設定失敗，請稍後重試';
  }

  /// 顯示錯誤對話框
  static void showErrorDialog(BuildContext context, String message) {
    showDialog(
      context: context,
      builder: (context) => AlertDialog(
        title: const Text('錯誤'),
        content: Text(message),
        actions: [
          TextButton(
            onPressed: () => Navigator.of(context).pop(),
            child: const Text('確定'),
          ),
        ],
      ),
    );
  }
}
```

### 10.2 錯誤復原機制
```dart
/// 錯誤復原策略
class ErrorRecovery {
  
  /// 自動重試機制
  static Future<T> retryOperation<T>(
    Future<T> Function() operation, {
    int maxRetries = 3,
    Duration delay = const Duration(seconds: 1),
  }) async {
    Exception? lastException;
    
    for (int attempt = 0; attempt < maxRetries; attempt++) {
      try {
        return await operation();
      } catch (e) {
        lastException = e is Exception ? e : Exception(e.toString());
        
        if (attempt < maxRetries - 1) {
          await Future.delayed(delay * (attempt + 1));
        }
      }
    }
    
    throw lastException!;
  }

  /// 離線模式處理
  static Widget buildOfflineWidget() {
    return Container(
      padding: const EdgeInsets.all(16),
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          const Icon(
            Icons.wifi_off,
            size: 64,
            color: Colors.grey,
          ),
          const SizedBox(height: 16),
          const Text(
            '目前處於離線狀態',
            style: TextStyle(fontSize: 18, color: Colors.grey),
          ),
          const SizedBox(height: 8),
          const Text(
            '請檢查網路連線後重試',
            style: TextStyle(color: Colors.grey),
          ),
          const SizedBox(height: 16),
          ElevatedButton(
            onPressed: () {
              // 重新載入頁面
            },
            child: const Text('重試'),
          ),
        ],
      ),
    );
  }
}
```

---

## 文件維護記錄

| 版本 | 日期 | 修改者 | 修改內容 |
|------|------|--------|----------|
| **v2.0.0** | **2025-08-06 12:00:00 +08:00** | **LCAS SA Team** | **重大版本升級：完全重構為技術設計文件，參照5005文件格式，包含完整的Widget架構設計、狀態管理、API整合、響應式設計、無障礙支援、效能最佳化及錯誤處理機制，確保與8701 SRS文件和9005 PRD文件技術實作一致性** |

---

## 備註

- **重大轉型**：從測試設計文件轉為技術設計文件
- **MVP導向**：聚焦核心功能，避免過度工程
- **模組化設計**：每個Widget職責單一，便於維護與測試
- **狀態管理**：採用Provider模式，確保狀態可預測
- **API整合**：完整對接F004、F009端點
- **響應式設計**：支援手機、平板、桌面三種尺寸
- **無障礙優先**：完整WCAG 2.1 AA標準支援
- **效能最佳化**：Widget快取、圖片預載、記憶體監控
- **錯誤處理**：統一錯誤分類、自動重試、優雅降級
- **技術實作導向**：提供開發團隊具體的實作指引

