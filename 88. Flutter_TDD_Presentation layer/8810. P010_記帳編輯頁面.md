
# 8810. TDD_P010_記帳編輯頁面_v1.0.0

## 文件資訊
- **文件標題**: P010 記帳編輯頁面技術設計文件
- **文件版本**: v1.0.0
- **創建日期**: 2025-01-26 20:00:00 +08:00 (台灣時間)
- **創建者**: LCAS SA Team
- **最後更新**: 2025-01-26 20:00:00 +08:00 (台灣時間)
- **對應需求文件**: P010_記帳編輯頁面_SRS.md
- **對應PRD文件**: 9005. Flutter_Presentation layer.md
- **頁面代碼**: P010 (Transaction Edit Page)

---

## 📑 目次 (Table of Contents)

1. [技術架構概述](#10-技術架構概述)
2. [Widget清單](#20-widget清單)
3. [核心Widget設計](#30-核心widget設計)
4. [狀態管理架構](#40-狀態管理架構)
5. [API整合設計](#50-api整合設計)
6. [導航路由設計](#60-導航路由設計)
7. [響應式設計](#70-響應式設計)
8. [無障礙功能](#80-無障礙功能)
9. [效能最佳化](#90-效能最佳化)
10. [錯誤處理機制](#100-錯誤處理機制)

---

## 1.0 技術架構概述

### 1.1 模組定位
P010記帳編輯頁面作為LCAS 2.0 Flutter應用程式的記帳資料修正核心，負責記帳記錄編輯、表單驗證、資料同步、四模式差異化編輯體驗及衝突處理。本頁面採用Clean Architecture設計，確保資料準確性與編輯流暢性。

### 1.2 技術堆疊
- **前端框架**: Flutter 3.x
- **狀態管理**: Provider + ChangeNotifier
- **路由管理**: GoRouter
- **HTTP客戶端**: Dio
- **本地儲存**: SharedPreferences + Hive
- **表單驗證**: FormBuilderValidators
- **日期時間**: intl
- **圖片處理**: image_picker + image_cropper
- **檔案管理**: file_picker

### 1.3 模組架構
```dart
/**
 * P010_記帳編輯頁面_1.0.0
 * @module P010記帳編輯頁面
 * @description Flutter Presentation Layer記帳編輯頁面 - 完整編輯功能、智慧驗證、四模式差異化、衝突處理
 * @update 2025-01-26: 升級至v1.0版本，新增完整編輯功能設計，支援分割記帳、重複記帳、批次編輯、版本控制
 */
```

---

## 2.0 Widget清單

### 2.1 完整Widget索引表

| # | Widget名稱 | 版本 | 所屬層級 | 功能描述 | 相依組件 |
|---|------------|------|----------|----------|----------|
| 01 | TransactionEditPage | V1.0.0 | 頁面容器層 | 主編輯頁面容器及生命週期管理 | EditProvider、Router |
| 02 | EditScaffold | V1.0.0 | 佈局結構層 | 基礎頁面結構與背景設定 | AppBar、FloatingActionBar |
| 03 | EditAppBar | V1.0.0 | 導航控制層 | 編輯頁面導航與動作按鈕 | BackButton、ActionButtons |
| 04 | EditFormContainer | V1.0.0 | 表單容器層 | 主要編輯表單容器 | FormSections |
| 05 | AmountEditSection | V1.0.0 | 金額編輯層 | 金額輸入與類型切換 | AmountInput、TypeToggle |
| 06 | CategoryEditSection | V1.0.0 | 科目編輯層 | 科目選擇與管理 | CategoryPicker、SubcategoryPicker |
| 07 | DateTimeEditSection | V1.0.0 | 日期時間層 | 日期時間選擇編輯 | DatePicker、TimePicker |
| 08 | DescriptionEditSection | V1.0.0 | 描述編輯層 | 商家描述備註編輯 | MerchantInput、DescriptionInput |
| 09 | PaymentMethodSection | V1.0.0 | 付款方式層 | 付款方式選擇編輯 | PaymentMethodPicker |
| 10 | LocationEditSection | V1.0.0 | 位置編輯層 | 位置資訊編輯 | LocationPicker、MapWidget |
| 11 | TagsEditSection | V1.0.0 | 標籤編輯層 | 標籤管理編輯 | TagsInput、TagsList |
| 12 | AttachmentEditSection | V1.0.0 | 附件編輯層 | 附件管理編輯 | AttachmentList、AttachmentPicker |
| 13 | SplitEditSection | V1.0.0 | 分割編輯層 | 分割記帳編輯 | SplitItemsList、AddSplitButton |
| 14 | RecurringEditSection | V1.0.0 | 重複編輯層 | 重複記帳設定編輯 | RecurringSettings |
| 15 | AmountInput | V1.0.0 | 輸入元件層 | 金額輸入欄位 | TextFormField、Calculator |
| 16 | TypeToggle | V1.0.0 | 切換元件層 | 收入支出類型切換 | ToggleButtons |
| 17 | CategoryPicker | V1.0.0 | 選擇器層 | 主要科目選擇器 | BottomSheet、ListView |
| 18 | SubcategoryPicker | V1.0.0 | 選擇器層 | 子科目選擇器 | BottomSheet、ListView |
| 19 | DatePicker | V1.0.0 | 選擇器層 | 日期選擇器 | DatePickerDialog |
| 20 | TimePicker | V1.0.0 | 選擇器層 | 時間選擇器 | TimePickerDialog |
| 21 | MerchantInput | V1.0.0 | 輸入元件層 | 商家名稱輸入 | AutoCompleteInput |
| 22 | DescriptionInput | V1.0.0 | 輸入元件層 | 描述備註輸入 | TextFormField |
| 23 | PaymentMethodPicker | V1.0.0 | 選擇器層 | 付款方式選擇器 | BottomSheet、GridView |
| 24 | LocationPicker | V1.0.0 | 選擇器層 | 位置選擇器 | MapView、SearchBar |
| 25 | MapWidget | V1.0.0 | 地圖元件層 | 地圖顯示元件 | GoogleMap |
| 26 | TagsInput | V1.0.0 | 輸入元件層 | 標籤輸入欄位 | ChipsInput |
| 27 | TagsList | V1.0.0 | 清單元件層 | 標籤清單顯示 | Wrap、Chip |
| 28 | AttachmentList | V1.0.0 | 清單元件層 | 附件清單顯示 | ListView、AttachmentItem |
| 29 | AttachmentPicker | V1.0.0 | 選擇器層 | 附件選擇器 | ImagePicker、FilePicker |
| 30 | SplitItemsList | V1.0.0 | 清單元件層 | 分割項目清單 | ListView、SplitItemWidget |
| 31 | AddSplitButton | V1.0.0 | 按鈕元件層 | 新增分割項目按鈕 | FloatingActionButton |
| 32 | RecurringSettings | V1.0.0 | 設定元件層 | 重複記帳設定 | DropdownButton、Switch |
| 33 | CalculatorWidget | V1.0.0 | 計算器層 | 內建計算器 | GridView、Calculator |
| 34 | ValidationIndicator | V1.0.0 | 驗證指示層 | 表單驗證指示器 | Icon、Text |
| 35 | ChangeTracker | V1.0.0 | 變更追蹤層 | 變更狀態追蹤器 | DiffViewer |
| 36 | ConflictResolver | V1.0.0 | 衝突解決層 | 編輯衝突解決器 | Dialog、ComparisonView |
| 37 | AutoSaveIndicator | V1.0.0 | 自動儲存層 | 自動儲存狀態指示 | LinearProgressIndicator |
| 38 | EditActionBar | V1.0.0 | 動作工具列層 | 編輯動作工具列 | Row、ActionButtons |
| 39 | SaveButton | V1.0.0 | 儲存按鈕層 | 儲存變更按鈕 | ElevatedButton |
| 40 | CancelButton | V1.0.0 | 取消按鈕層 | 取消編輯按鈕 | TextButton |

---

## 3.0 核心Widget設計

### 3.1 頁面容器層Widget

#### 3.1.1 TransactionEditPage Widget
```dart
/**
 * 01. 對應 2.1：主記帳編輯頁面容器Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 記帳編輯頁面的主容器，支援完整編輯功能與四模式差異化
 */
class TransactionEditPage extends StatefulWidget {
  static const String routeName = '/transaction/edit';
  final String transactionId;
  final UserMode? userMode;

  const TransactionEditPage({
    Key? key, 
    required this.transactionId,
    this.userMode,
  });

  @override
  State<TransactionEditPage> createState();
}

/**
 * 02. 對應 2.1：記帳編輯頁面狀態類別
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 管理記帳編輯頁面的狀態和動畫控制器
 */
class _TransactionEditPageState extends State<TransactionEditPage> 
    with TickerProviderStateMixin {
  late AnimationController _fadeController;
  late AnimationController _slideController;
  late Animation<double> _fadeAnimation;
  late Animation<Offset> _slideAnimation;
  Timer? _autoSaveTimer;

  @override
  void initState();

  /**
   * 03. 對應 2.1：初始化動畫控制器
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 設定頁面進入動畫效果
   */
  void _initializeAnimations();

  /**
   * 04. 對應 2.1：初始化頁面狀態
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 調用Provider進行編輯頁面初始化
   */
  Future<void> _initializePage();

  /**
   * 05. 對應 2.1：啟動自動儲存
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 設定定期自動儲存機制
   */
  void _startAutoSave();

  @override
  Widget build(BuildContext context);

  @override
  void dispose();
}
```

### 3.2 佈局結構層Widget

#### 3.2.1 EditScaffold Widget
```dart
/**
 * 06. 對應 2.2：編輯頁面佈局結構Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 提供編輯頁面的基礎佈局結構，支援四模式差異化背景
 */
class EditScaffold extends StatelessWidget {
  final Animation<double> fadeAnimation;
  final Animation<Offset> slideAnimation;
  final EditProvider provider;
  final UserMode? userMode;

  const EditScaffold({
    Key? key,
    required this.fadeAnimation,
    required this.slideAnimation,
    required this.provider,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 07. 對應 2.2：建立衝突解決覆蓋層
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 顯示編輯衝突解決對話框的覆蓋層
   */
  Widget _buildConflictOverlay(BuildContext context, EditProvider provider);
}
```

### 3.3 表單容器層Widget

#### 3.3.1 EditFormContainer Widget
```dart
/**
 * 08. 對應 3.1：編輯表單容器Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 主要編輯表單容器，管理所有編輯區塊
 */
class EditFormContainer extends StatefulWidget {
  final EditProvider provider;
  final UserMode? userMode;

  const EditFormContainer({
    Key? key,
    required this.provider,
    this.userMode,
  });

  @override
  State<EditFormContainer> createState();
}

class _EditFormContainerState extends State<EditFormContainer> {
  final GlobalKey<FormState> _formKey = GlobalKey<FormState>();
  final ScrollController _scrollController = ScrollController();

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 09. 對應 3.1：取得模式對應編輯區塊
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 根據使用者模式返回對應的編輯區塊列表
   */
  List<Widget> _getEditSectionsForMode(UserMode? mode);

  /**
   * 10. 對應 3.1：驗證表單完整性
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 驗證整個表單的完整性和有效性
   */
  bool _validateForm();

  @override
  void dispose();
}
```

### 3.4 金額編輯層Widget

#### 3.4.1 AmountEditSection Widget
```dart
/**
 * 11. 對應 3.2：金額編輯區塊Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 金額輸入與收入支出類型編輯區塊
 */
class AmountEditSection extends StatefulWidget {
  final EditProvider provider;
  final UserMode? userMode;

  const AmountEditSection({
    Key? key,
    required this.provider,
    this.userMode,
  });

  @override
  State<AmountEditSection> createState();
}

class _AmountEditSectionState extends State<AmountEditSection> {
  late TextEditingController _amountController;
  bool _showCalculator = false;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 12. 對應 3.2：切換計算器顯示
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 切換內建計算器的顯示狀態
   */
  void _toggleCalculator();

  /**
   * 13. 對應 3.2：處理計算器結果
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理計算器計算結果並更新金額
   */
  void _handleCalculatorResult(double result);

  @override
  void dispose();
}
```

#### 3.4.2 AmountInput & TypeToggle Widgets
```dart
/**
 * 14. 對應 3.2.1：金額輸入欄位Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 金額輸入欄位與格式驗證
 */
class AmountInput extends StatelessWidget {
  final TextEditingController controller;
  final String? Function(String?)? validator;
  final VoidCallback? onCalculatorTap;
  final bool enabled;

  const AmountInput({
    Key? key,
    required this.controller,
    this.validator,
    this.onCalculatorTap,
    this.enabled = true,
  });

  @override
  Widget build(BuildContext context);
}

/**
 * 15. 對應 3.2.2：收入支出類型切換Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 收入支出類型切換控制
 */
class TypeToggle extends StatelessWidget {
  final TransactionType selectedType;
  final ValueChanged<TransactionType> onTypeChanged;
  final bool enabled;

  const TypeToggle({
    Key? key,
    required this.selectedType,
    required this.onTypeChanged,
    this.enabled = true,
  });

  @override
  Widget build(BuildContext context);
}
```

### 3.5 科目編輯層Widget

#### 3.5.1 CategoryEditSection Widget
```dart
/**
 * 16. 對應 3.3：科目編輯區塊Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 主要科目與子科目選擇編輯區塊
 */
class CategoryEditSection extends StatelessWidget {
  final EditProvider provider;
  final UserMode? userMode;

  const CategoryEditSection({
    Key? key,
    required this.provider,
    this.userMode,
  });

  @override
  Widget build(BuildContext context);

  /**
   * 17. 對應 3.3：取得模式可用科目
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 根據使用者模式返回可用的科目列表
   */
  List<Category> _getAvailableCategoriesForMode(UserMode? mode);
}

/**
 * 18. 對應 3.3.1：科目選擇器Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 主要科目選擇器
 */
class CategoryPicker extends StatefulWidget {
  final String? selectedCategoryId;
  final ValueChanged<String?> onCategoryChanged;
  final TransactionType transactionType;
  final List<Category> availableCategories;

  const CategoryPicker({
    Key? key,
    this.selectedCategoryId,
    required this.onCategoryChanged,
    required this.transactionType,
    required this.availableCategories,
  });

  @override
  State<CategoryPicker> createState();
}
```

### 3.6 日期時間編輯層Widget

#### 3.6.1 DateTimeEditSection Widget
```dart
/**
 * 19. 對應 3.4：日期時間編輯區塊Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 交易日期與時間選擇編輯區塊
 */
class DateTimeEditSection extends StatefulWidget {
  final EditProvider provider;
  final UserMode? userMode;

  const DateTimeEditSection({
    Key? key,
    required this.provider,
    this.userMode,
  });

  @override
  State<DateTimeEditSection> createState();
}

class _DateTimeEditSectionState extends State<DateTimeEditSection> {
  @override
  Widget build(BuildContext context);

  /**
   * 20. 對應 3.4：顯示日期選擇器
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 顯示日期選擇對話框
   */
  Future<void> _showDatePicker();

  /**
   * 21. 對應 3.4：顯示時間選擇器
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 顯示時間選擇對話框
   */
  Future<void> _showTimePicker();

  /**
   * 22. 對應 3.4：快速日期設定
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 提供今天、昨天等快速日期設定選項
   */
  void _setQuickDate(QuickDateOption option);
}
```

### 3.7 描述編輯層Widget

#### 3.7.1 DescriptionEditSection Widget
```dart
/**
 * 23. 對應 3.5：描述編輯區塊Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 商家名稱、描述、備註編輯區塊
 */
class DescriptionEditSection extends StatefulWidget {
  final EditProvider provider;
  final UserMode? userMode;

  const DescriptionEditSection({
    Key? key,
    required this.provider,
    this.userMode,
  });

  @override
  State<DescriptionEditSection> createState();
}

class _DescriptionEditSectionState extends State<DescriptionEditSection> {
  late TextEditingController _merchantController;
  late TextEditingController _descriptionController;
  late TextEditingController _notesController;

  @override
  void initState();

  @override
  Widget build(BuildContext context);

  /**
   * 24. 對應 3.5：智慧商家建議
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 根據歷史記錄提供智慧商家名稱建議
   */
  Future<List<String>> _getMerchantSuggestions(String query);

  @override
  void dispose();
}
```

### 3.8 分割編輯層Widget

#### 3.8.1 SplitEditSection Widget
```dart
/**
 * 25. 對應 3.6：分割編輯區塊Widget
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 分割記帳編輯區塊
 */
class SplitEditSection extends StatefulWidget {
  final EditProvider provider;
  final UserMode? userMode;

  const SplitEditSection({
    Key? key,
    required this.provider,
    this.userMode,
  });

  @override
  State<SplitEditSection> createState();
}

class _SplitEditSectionState extends State<SplitEditSection> {
  @override
  Widget build(BuildContext context);

  /**
   * 26. 對應 3.6：新增分割項目
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 新增分割記帳項目
   */
  void _addSplitItem();

  /**
   * 27. 對應 3.6：移除分割項目
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 移除指定的分割記帳項目
   */
  void _removeSplitItem(String splitId);

  /**
   * 28. 對應 3.6：驗證分割總額
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 驗證分割項目總額是否等於主要金額
   */
  bool _validateSplitTotal();
}
```

---

## 4.0 狀態管理架構

### 4.1 EditProvider 狀態管理器
```dart
/**
 * 29. 對應 4.1：記帳編輯頁面狀態管理器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 管理記帳編輯頁面的狀態，支援完整編輯功能和四模式差異化
 */
class EditProvider extends ChangeNotifier {
  // 狀態屬性
  EditPageState _pageState = EditPageState.loading;
  TransactionModel? _originalTransaction;
  TransactionModel? _editedTransaction;
  bool _isLoading = false;
  bool _isSaving = false;
  String? _errorMessage;
  EditErrorType? _errorType;
  bool _hasChanges = false;
  UserMode? _userMode;

  // 表單狀態
  final GlobalKey<FormState> formKey = GlobalKey<FormState>();
  Map<String, dynamic> _validationErrors = {};
  Map<String, dynamic> _fieldStates = {};

  // 衝突處理
  bool _hasConflict = false;
  TransactionModel? _conflictTransaction;
  ConflictResolution? _conflictResolution;

  // 自動儲存
  bool _autoSaveEnabled = false;
  DateTime? _lastAutoSave;

  // Getters
  EditPageState get pageState;
  TransactionModel? get originalTransaction;
  TransactionModel? get editedTransaction;
  bool get isLoading;
  bool get isSaving;
  String? get errorMessage;
  EditErrorType? get errorType;
  bool get hasChanges;
  UserMode? get userMode;
  Map<String, dynamic> get validationErrors;
  bool get hasConflict;
  bool get autoSaveEnabled;

  /**
   * 30. 對應 4.1：初始化編輯頁面
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 初始化編輯頁面狀態和載入原始記錄
   */
  Future<void> initializePage(String transactionId, UserMode? mode);

  /**
   * 31. 對應 4.1：載入交易記錄
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 從API載入要編輯的交易記錄
   */
  Future<void> loadTransaction(String transactionId);

  /**
   * 32. 對應 4.1：更新欄位值
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 更新指定欄位的值並觸發變更追蹤
   */
  void updateField(String fieldName, dynamic value);

  /**
   * 33. 對應 4.1：批次更新欄位
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 批次更新多個欄位值
   */
  void updateFields(Map<String, dynamic> fields);

  /**
   * 34. 對應 4.1：驗證欄位
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 驗證單一欄位的有效性
   */
  String? validateField(String fieldName, dynamic value);

  /**
   * 35. 對應 4.1：驗證整個表單
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 驗證整個表單的有效性
   */
  bool validateForm();

  /**
   * 36. 對應 4.1：儲存變更
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 儲存編輯變更到後端
   */
  Future<void> saveChanges();

  /**
   * 37. 對應 4.1：自動儲存草稿
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 自動儲存編輯草稿到本地
   */
  Future<void> autoSaveDraft();

  /**
   * 38. 對應 4.1：載入草稿
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 載入本地儲存的編輯草稿
   */
  Future<void> loadDraft(String transactionId);

  /**
   * 39. 對應 4.1：清除草稿
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 清除本地儲存的編輯草稿
   */
  Future<void> clearDraft(String transactionId);

  /**
   * 40. 對應 4.1：處理編輯衝突
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理併發編輯衝突
   */
  Future<void> handleConflict(ConflictResolution resolution);

  /**
   * 41. 對應 4.1：重置編輯狀態
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 重置編輯狀態到初始狀態
   */
  void resetEditState();

  /**
   * 42. 對應 4.1：取消編輯
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 取消編輯並返回原始狀態
   */
  Future<void> cancelEdit();

  /**
   * 43. 對應 4.1：檢查編輯權限
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 檢查當前使用者的編輯權限
   */
  Future<bool> checkEditPermission(String transactionId);

  /**
   * 44. 對應 4.1：設定載入狀態
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 更新載入狀態並通知監聽者
   */
  void _setLoading(bool loading);

  /**
   * 45. 對應 4.1：設定頁面狀態
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 更新頁面狀態並通知監聽者
   */
  void _setPageState(EditPageState state);

  /**
   * 46. 對應 4.1：設定錯誤訊息
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 設定錯誤訊息和錯誤類型
   */
  void _setError(String message, EditErrorType type);

  /**
   * 47. 對應 4.1：追蹤變更狀態
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 追蹤編輯變更狀態
   */
  void _trackChanges();
}
```

### 4.2 狀態枚舉定義
```dart
/**
 * 48. 對應 4.2：編輯頁面狀態枚舉
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 定義編輯頁面的各種狀態
 */
enum EditPageState {
  loading,        // 載入中
  editing,        // 編輯中
  validating,     // 驗證中
  saving,         // 儲存中
  conflict,       // 衝突狀態
  success,        // 成功狀態
  error,          // 錯誤狀態
}

/**
 * 49. 對應 4.2：編輯錯誤類型枚舉
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 編輯錯誤類型分類
 */
enum EditErrorType {
  loadError,          // 載入錯誤
  validationError,    // 驗證錯誤
  saveError,         // 儲存錯誤
  permissionError,   // 權限錯誤
  conflictError,     // 衝突錯誤
  networkError,      // 網路錯誤
  unknown,          // 未知錯誤
}

/**
 * 50. 對應 4.2：衝突解決方式枚舉
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 編輯衝突解決方式
 */
enum ConflictResolution {
  overwrite,      // 強制覆蓋
  merge,         // 智慧合併
  reload,        // 重新載入
  cancel,        // 取消編輯
}

/**
 * 51. 對應 4.2：快速日期選項枚舉
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 快速日期設定選項
 */
enum QuickDateOption {
  today,         // 今天
  yesterday,     // 昨天
  thisWeek,      // 本週
  lastWeek,      // 上週
  thisMonth,     // 本月
  lastMonth,     // 上月
}
```

---

## 5.0 API整合設計

### 5.1 EditApiService API服務
```dart
/**
 * 52. 對應 5.1：編輯頁面API服務
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 處理編輯頁面相關的API調用，支援F002記帳功能
 */
class EditApiService {
  final ApiClient _apiClient = ApiClient();

  /**
   * 53. 對應 5.1：取得交易記錄詳細資料
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 取得指定交易記錄的詳細資料（對應F002端點）
   */
  Future<ApiResponse<TransactionModel>> getTransactionById(String transactionId);

  /**
   * 54. 對應 5.1：更新交易記錄
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 更新交易記錄API調用（對應F002端點）
   */
  Future<ApiResponse<TransactionModel>> updateTransaction(
    String transactionId, 
    UpdateTransactionRequest request
  );

  /**
   * 55. 對應 5.1：檢查編輯權限
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 檢查交易記錄編輯權限（對應F008端點）
   */
  Future<ApiResponse<EditPermission>> checkEditPermission(String transactionId);

  /**
   * 56. 對應 5.1：取得科目清單
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 取得可用科目清單（對應F003端點）
   */
  Future<ApiResponse<List<Category>>> getCategories(TransactionType type);

  /**
   * 57. 對應 5.1：取得付款方式清單
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 取得可用付款方式清單
   */
  Future<ApiResponse<List<PaymentMethod>>> getPaymentMethods();

  /**
   * 58. 對應 5.1：取得商家建議
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 根據輸入取得商家名稱建議
   */
  Future<ApiResponse<List<String>>> getMerchantSuggestions(String query);

  /**
   * 59. 對應 5.1：上傳附件
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 上傳交易附件檔案
   */
  Future<ApiResponse<AttachmentModel>> uploadAttachment(File file);

  /**
   * 60. 對應 5.1：刪除附件
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 刪除交易附件檔案
   */
  Future<ApiResponse<void>> deleteAttachment(String attachmentId);
}
```

### 5.2 資料模型定義
```dart
/**
 * 61. 對應 5.2：更新交易請求模型
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 更新交易記錄請求資料結構
 */
class UpdateTransactionRequest {
  final double amount;
  final TransactionType type;
  final String categoryId;
  final DateTime date;
  final String? merchant;
  final String? description;
  final String? notes;
  final String? paymentMethod;
  final List<String>? tags;
  final LocationModel? location;
  final List<String>? attachmentIds;
  final List<SplitItem>? splitItems;
  final RecurringSettings? recurringSettings;
  final int version;

  UpdateTransactionRequest({
    required this.amount,
    required this.type,
    required this.categoryId,
    required this.date,
    this.merchant,
    this.description,
    this.notes,
    this.paymentMethod,
    this.tags,
    this.location,
    this.attachmentIds,
    this.splitItems,
    this.recurringSettings,
    required this.version,
  });

  Map<String, dynamic> toJson();
}

/**
 * 62. 對應 5.2：編輯權限模型
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 編輯權限檢查回應資料結構
 */
class EditPermission {
  final bool canEdit;
  final bool canDelete;
  final List<String> editableFields;
  final String? lockedBy;
  final DateTime? lockedUntil;
  final String? lockReason;

  EditPermission({
    required this.canEdit,
    required this.canDelete,
    required this.editableFields,
    this.lockedBy,
    this.lockedUntil,
    this.lockReason,
  });

  factory EditPermission.fromJson(Map<String, dynamic> json);
}

/**
 * 63. 對應 5.2：交易模型
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 完整的交易記錄資料結構
 */
class TransactionModel {
  final String id;
  final String ledgerId;
  final double amount;
  final TransactionType type;
  final String categoryId;
  final String categoryName;
  final DateTime date;
  final String? merchant;
  final String? description;
  final String? notes;
  final String? paymentMethod;
  final List<String>? tags;
  final LocationModel? location;
  final List<AttachmentModel>? attachments;
  final List<SplitItem>? splitItems;
  final RecurringSettings? recurringSettings;
  final String createdBy;
  final DateTime createdAt;
  final DateTime updatedAt;
  final int version;

  TransactionModel({
    required this.id,
    required this.ledgerId,
    required this.amount,
    required this.type,
    required this.categoryId,
    required this.categoryName,
    required this.date,
    this.merchant,
    this.description,
    this.notes,
    this.paymentMethod,
    this.tags,
    this.location,
    this.attachments,
    this.splitItems,
    this.recurringSettings,
    required this.createdBy,
    required this.createdAt,
    required this.updatedAt,
    required this.version,
  });

  factory TransactionModel.fromJson(Map<String, dynamic> json);
  Map<String, dynamic> toJson();
  TransactionModel copyWith({...});
}
```

---

## 6.0 導航路由設計

### 6.1 路由配置
```dart
/**
 * 64. 對應 6.1：編輯頁面路由配置
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 定義編輯頁面的路由規則與導航邏輯
 */
class EditRoutes {
  static const String edit = '/transaction/edit';
  static const String history = '/transaction/history';
  static const String detail = '/transaction/detail';

  /**
   * 65. 對應 6.1：取得編輯頁面路由
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 建立編輯頁面的GoRoute配置
   */
  static GoRoute getEditRoute();
}

/**
 * 66. 對應 6.1：編輯導航服務
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 處理編輯相關的導航邏輯
 */
class EditNavigationService {
  static final GoRouter _router;
  static GoRouter get router;

  /**
   * 67. 對應 6.1：導航到編輯頁面
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 導航到指定交易的編輯頁面
   */
  Future<void> navigateToEdit(String transactionId);

  /**
   * 68. 對應 6.1：返回歷史頁面
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 編輯完成後返回歷史頁面
   */
  Future<void> navigateBackToHistory();

  /**
   * 69. 對應 6.1：導航到詳細頁面
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 導航到交易詳細頁面
   */
  Future<void> navigateToDetail(String transactionId);

  /**
   * 70. 對應 6.1：處理編輯取消
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理編輯取消的導航邏輯
   */
  void handleEditCancel();
}
```

---

## 7.0 響應式設計

### 7.1 響應式輔助工具
```dart
/**
 * 71. 對應 7.1：編輯頁面響應式設計輔助工具
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 遵循9005文件的響應式設計規範，提供編輯頁面專用的佈局管理
 */
class EditResponsiveHelper {
  final BuildContext context;
  late final MediaQueryData _mediaQuery;
  late final Size _screenSize;

  EditResponsiveHelper.of(BuildContext context);

  // 斷點判斷屬性（9005規範）
  bool get isMobile;
  bool get isTablet;
  bool get isSmallDesktop;
  bool get isLargeDesktop;
  DeviceType get deviceType;

  // 編輯頁面專用尺寸屬性
  double get formSectionSpacing;
  double get inputFieldHeight;
  double get sectionHeaderHeight;
  EdgeInsets get formPadding;
  double get actionButtonHeight;
  double get calculatorHeight;

  /**
   * 72. 對應 7.1：取得四模式編輯佈局
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 根據使用者模式和裝置類型返回適當編輯佈局
   */
  EditLayout getEditLayoutForMode(UserMode? mode);

  /**
   * 73. 對應 7.1：取得模式對應欄位大小
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 根據使用者模式返回適當欄位大小
   */
  double getFieldSizeForMode(UserMode? mode, FieldType fieldType);
}
```

---

## 8.0 無障礙功能

### 8.1 語意化支援
```dart
/**
 * 74. 對應 8.1：編輯頁面無障礙語意化Widget包裝器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 提供WCAG 2.1 AA標準的完整無障礙支援（9005規範）
 */
class EditAccessibilityWrapper {

  /**
   * 75. 對應 8.1：為編輯欄位提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 為各種編輯欄位添加適當的語意化標籤
   */
  static Widget wrapEditField(Widget child, String fieldType, {String? label, String? hint});

  /**
   * 76. 對應 8.1：為動作按鈕提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 為儲存、取消等動作按鈕添加適當的語意化標籤
   */
  static Widget wrapActionButton(Widget child, String actionType, bool isEnabled);

  /**
   * 77. 對應 8.1：為選擇器提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 為科目、日期等選擇器添加適當的語意化標籤
   */
  static Widget wrapPicker(Widget child, String pickerType, String? selectedValue);

  /**
   * 78. 對應 8.1：為驗證訊息提供語意化標籤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 為驗證錯誤訊息添加適當的語意化標籤
   */
  static Widget wrapValidationMessage(Widget child, String fieldName, String? error);
}
```

---

## 9.0 效能最佳化

### 9.1 Widget快取策略
```dart
/**
 * 79. 對應 9.1：編輯頁面Widget快取管理器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 優化編輯頁面Widget重建效能，減少不必要的重建
 */
class EditWidgetCache {
  static final Map<String, Widget> _cache = {};
  static final Map<String, DateTime> _cacheTimestamps = {};
  static const Duration _cacheValidDuration = Duration(minutes: 5);

  /**
   * 80. 對應 9.1：取得快取的科目選擇器或建立新的
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 從快取中獲取科目選擇器Widget或創建新的
   */
  static Widget getCategoryPickerOrCreate(TransactionType type, Widget Function() creator);

  /**
   * 81. 對應 9.1：取得快取的付款方式選擇器或建立新的
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 從快取中獲取付款方式選擇器Widget或創建新的
   */
  static Widget getPaymentMethodPickerOrCreate(Widget Function() creator);

  /**
   * 82. 對應 9.1：清除編輯頁面快取
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 清除編輯頁面相關的Widget快取
   */
  static void clearEditCache();
}
```

### 9.2 編輯流程優化
```dart
/**
 * 83. 對應 9.2：編輯流程效能優化器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 優化編輯流程效能，提升使用者體驗
 */
class EditPerformanceOptimizer {
  static final Map<String, Timer> _debounceTimers = {};
  static const Duration _debounceDelay = Duration(milliseconds: 300);

  /**
   * 84. 對應 9.2：防抖動輸入處理
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 防止使用者快速輸入造成頻繁驗證
   */
  static void debounceInput(String fieldName, VoidCallback callback);

  /**
   * 85. 對應 9.2：預載入編輯相關資源
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 預載入編輯頁面相關的資料和資源
   */
  static Future<void> preloadEditResources(String transactionId);

  /**
   * 86. 對應 9.2：優化表單驗證
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 優化表單驗證效能，避免重複驗證
   */
  static String? optimizedValidation(String fieldName, dynamic value, List<Validator> validators);
}
```

---

## 10.0 錯誤處理機制

### 10.1 統一錯誤處理
```dart
/**
 * 87. 對應 10.1：編輯頁面錯誤處理器
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 根據P010_SRS規範的具體錯誤情境進行詳細處理
 */
class EditErrorHandler {

  /**
   * 88. 對應 10.1：處理載入錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理交易記錄載入錯誤（P010_SRS規範）
   */
  static String handleLoadError(dynamic error);

  /**
   * 89. 對應 10.1：處理驗證錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理表單驗證錯誤（P010_SRS規範）
   */
  static Map<String, String> handleValidationErrors(Map<String, dynamic> errors);

  /**
   * 90. 對應 10.1：處理儲存錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理記錄儲存錯誤（P010_SRS規範）
   */
  static String handleSaveError(dynamic error);

  /**
   * 91. 對應 10.1：處理權限錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理編輯權限錯誤（P010_SRS規範）
   */
  static String handlePermissionError(dynamic error);

  /**
   * 92. 對應 10.1：處理併發編輯衝突
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理併發編輯衝突錯誤（P010_SRS規範）
   */
  static ConflictInfo handleConflictError(dynamic error);

  /**
   * 93. 對應 10.1：處理網路連線錯誤
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理網路連線錯誤（P010_SRS規範）
   */
  static String handleNetworkError(DioError error);

  /**
   * 94. 對應 10.1：顯示錯誤對話框
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 顯示錯誤訊息對話框（P010_SRS規範）
   */
  static Future<void> showErrorDialog(
    BuildContext context, 
    String message, 
    EditErrorType errorType,
    {VoidCallback? onRetry, VoidCallback? onDismiss}
  );

  /**
   * 95. 對應 10.1：顯示衝突解決對話框
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 顯示編輯衝突解決對話框
   */
  static Future<ConflictResolution?> showConflictDialog(
    BuildContext context,
    ConflictInfo conflictInfo,
  );
}
```

### 10.2 錯誤復原機制
```dart
/**
 * 96. 對應 10.2：編輯錯誤復原策略
 * @version 2025-01-26-V1.0.0
 * @date 2025-01-26 20:00:00
 * @description 提供編輯各種錯誤復原機制
 */
class EditErrorRecovery {

  /**
   * 97. 對應 10.2：自動重試載入機制
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 針對網路錯誤等臨時性問題的自動重試機制
   */
  static Future<T> retryLoad<T>(
    Future<T> Function() loadOperation,
    {int maxRetries = 3, Duration initialDelay = const Duration(seconds: 1)}
  );

  /**
   * 98. 對應 10.2：恢復編輯草稿
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 當載入失敗時嘗試恢復本地草稿
   */
  static Future<TransactionModel?> recoverDraft(String transactionId);

  /**
   * 99. 對應 10.2：離線編輯模式
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 處理離線狀態的編輯模式
   */
  static Widget buildOfflineEditWidget({
    required TransactionModel transaction,
    VoidCallback? onRetry,
  });

  /**
   * 100. 對應 10.2：清除損壞的草稿
   * @version 2025-01-26-V1.0.0
   * @date 2025-01-26 20:00:00
   * @description 清除可能導致編輯問題的損壞草稿
   */
  static Future<void> clearCorruptedDrafts();
}
```

---

## 遵循0055設計原則

### 階層架構對應
- 本TDD文件基於P010_記帳編輯頁面_SRS.md的需求規格
- 與9005. Flutter_Presentation layer.md的產品規格保持一致
- 所有函數節點編號對應上層文件的功能條目

### 設計原則遵循
1. **階層架構邏輯**: 嚴格基於SRS文件定義的功能條目設計
2. **同層級依賴關係**: 考量與記帳歷史頁面和詳細記帳頁面的依賴關係
3. **代碼節點編號**: 採用不加前綴的節點編號格式（如2.1、3.2）
4. **命名一致性**: 保持與使用者介面操作流程的名稱同步
5. **可讀性設計**: 採用模組化設計，遵循低耦合、高內聚原則

## 文件維護記錄

| 版本 | 日期 | 修改者 | 修改內容 |
|------|------|--------|----------|
| **v1.0.0** | **2025-01-26 20:00:00 +08:00** | **LCAS SA Team** | **初版建立：完整P010記帳編輯頁面TDD規格，支援完整編輯功能（金額、科目、日期、描述、付款方式、位置、標籤、附件、分割記帳、重複記帳），實作四模式差異化設計，完整錯誤處理機制，衝突解決機制，自動儲存功能，遵循0055設計原則，對應P010 SRS規格** |

---

## 備註

- **純函數宣告**: 完全移除所有實作代碼，僅保留函數名稱、參數定義和註解
- **完整編輯功能**: 支援記帳記錄的所有欄位編輯，包括金額、科目、日期、描述、付款方式、位置、標籤、附件等
- **分割記帳支援**: 完整的分割記帳編輯功能，支援多項目分割和總額驗證
- **重複記帳設定**: 支援定期重複記帳的設定和編輯
- **四模式差異化**: 根據使用者模式提供不同的編輯界面複雜度和可用功能
- **衝突處理機制**: 完整的併發編輯衝突檢測和解決機制
- **自動儲存功能**: 定期自動儲存編輯草稿，防止資料遺失
- **智慧輔助功能**: 商家名稱建議、科目智慧選擇等輔助功能
- **完整技術架構**: Widget分層、狀態管理、API整合、響應式設計、無障礙支援、效能最佳化
- **規範完全對齊**: 與0055、P010_SRS、9005文件完全一致
- **節點編號標準化**: 統一編號01-100，遵循0055規範格式
- **版本統一升級**: 所有組件版本統一為v1.0.0
