name: pl_tests
description: PL 測試專案 - SIT P1階段一重構版 (靜態測試資料)
version: 1.0.0+1

environment:
  sdk: '^3.5.0'

dependencies:
  http: ^1.1.0
  intl: ^0.20.2

dev_dependencies:
  test: ^1.24.0
  http: ^1.1.0

# 階段三清理完成：移除7580/7590模組後的最小化配置
# 專注於7570靜態測試資料流，符合MVP理念