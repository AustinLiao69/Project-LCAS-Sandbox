
# 7101. 系統進入功能群_SRS.md

**文件編號**: 7101  
**版本**: v1.0.0  
**建立日期**: 2025-09-11  
**建立者**: LCAS PM Team  
**最後更新**: 2025-09-11  

---

## 📑 目次 (Table of Contents)

1. [模組概述 (Module Overview)](#10-模組概述module-overview)

2. [功能需求 (Functional Requirements)](#20-功能需求functional-requirements)
   - 2.1 APP啟動與初始化
   - 2.2 使用者註冊功能
   - 2.3 使用者登入功能
   - 2.4 密碼重設功能
   - 2.5 模式評估與設定
   - 2.6 跨平台綁定功能
   - 2.7 APP推廣展示
   - 2.8 錯誤處理與重試機制

3. [輸入/輸出規格 (Input & Output Specifications)](#30-輸入輸出規格input--output-specifications)
   - 3.1 輸入
   - 3.2 輸出

4. [錯誤處理與例外狀況 (Error Handling & Edge Cases)](#40-錯誤處理與例外狀況error-handling--edge-cases)
   - 4.1 認證相關錯誤
   - 4.2 網路連線錯誤
   - 4.3 輸入驗證錯誤
   - 4.4 系統異常處理

---

## 1.0 模組概述（Module Overview）

### 1.1 模組定位與職責

**系統進入功能群**是LCAS 2.0專案Presentation Layer的核心入口模組群，負責處理使用者從首次接觸系統到成功進入主要功能的完整流程。本功能群對應0090 Sprint計畫的**Phase 1**範圍，確保使用者能夠順利完成註冊、登入、模式評估與跨平台整合。

**核心職責範圍**：
- 🚀 **APP啟動管理**：品牌展示、版本檢查、載入狀態
- 👤 **身份認證流程**：註冊、登入、密碼重設完整體驗
- 🎯 **模式評估機制**：四模式理論的實際應用與設定
- 🔗 **跨平台整合**：LINE OA與APP的無縫連接體驗
- 📱 **使用引導**：首次使用者的完整引導流程

### 1.2 功能群範圍定義

基於7021 Screen list文件，本功能群涵蓋以下8個核心畫面：

| 畫面編號 | 畫面名稱 | Phase | 核心功能 | 四模式支援 |
|----------|----------|-------|----------|------------|
| **S-102** | APP啟動頁 | Phase 1 | 品牌展示、載入進度 | 統一體驗 |
| **S-103** | APP註冊頁 | Phase 1 | Email註冊、密碼設定 | ✅ 差異化 |
| **S-104** | APP登入頁 | Phase 1 | 帳號密碼、第三方登入 | ✅ 差異化 |
| **S-105** | 忘記密碼頁 | Phase 1 | Email重設、驗證流程 | ✅ 差異化 |
| **S-106** | 模式評估問卷頁 | Phase 1 | 5題評估、模式推薦 | ✅ 核心功能 |
| **S-107** | 跨平台綁定頁 | Phase 1 | LINE OA綁定、QR Code | 統一體驗 |
| **S-108** | APP下載推廣頁 | Phase 1 | 功能介紹、下載引導 | 統一體驗 |

**備註**：S-101 LINE OA歡迎頁屬於LINE OA平台，不在Flutter APP範圍內。

### 1.3 四模式差異化策略

基於0015文件的四模式理論，本功能群實現以下差異化策略：

#### 1.3.1 Expert模式（專家模式）
- **註冊/登入**：提供完整選項、進階安全設定、批次匯入功能
- **模式評估**：顯示詳細評估邏輯、支援手動調整、提供專業建議
- **視覺風格**：專業藍色調、豐富資訊密度、多層次導航

#### 1.3.2 Inertial模式（慣性模式）
- **註冊/登入**：固定步驟流程、一致性介面佈局、可預測操作
- **模式評估**：標準化問卷、清晰的步驟指示、穩定的進度展示
- **視覺風格**：穩定灰藍色調、簡潔佈局、邏輯清晰

#### 1.3.3 Cultivation模式（養成模式）
- **註冊/登入**：引導式流程、進度追蹤、激勵性訊息
- **模式評估**：互動式問卷、即時回饋、成長建議
- **視覺風格**：活力綠色調、動態效果、成就感設計

#### 1.3.4 Guiding模式（引導模式）
- **註冊/登入**：極簡流程、自動填入、一鍵完成
- **模式評估**：簡化問卷、智慧推薦、最少決策
- **視覺風格**：溫和橘色調、大按鈕設計、清晰指引

### 1.4 技術架構整合

#### 1.4.1 API整合規範
本功能群**嚴格遵循0026文件PL-APL-BL三層架構對應**，僅消費以下API服務：

- **8101 認證服務API**：完整的註冊、登入、密碼重設功能
- **8102 用戶管理服務API**：模式評估、個人資料、偏好設定
- **8111 系統服務API**：APP版本檢查、系統狀態、設定同步

#### 1.4.2 狀態管理架構
```dart
// 進入階段狀態管理結構
EntryFlowStateManager/
├── AuthState          // 認證狀態管理
├── ModeAssessmentState // 模式評估狀態
├── OnboardingState     // 引導流程狀態
└── PlatformBindingState // 跨平台綁定狀態
```

#### 1.4.3 本地資料儲存
- **認證Token**：使用FlutterSecureStorage安全儲存
- **模式設定**：SharedPreferences持久化
- **引導進度**：本地快取機制

### 1.5 品質與安全要求

#### 1.5.1 效能要求
- **APP啟動時間**：≤ 3秒（冷啟動）、≤ 1秒（熱啟動）
- **頁面載入時間**：≤ 1秒（本地頁面）、≤ 3秒（API依賴頁面）
- **記憶體使用**：≤ 50MB（完整進入流程）

#### 1.5.2 安全要求
- **密碼安全**：8位數以上、包含大小寫字母與數字
- **Token管理**：自動更新、安全儲存、過期檢查
- **輸入驗證**：前端驗證 + 後端驗證雙重保護

#### 1.5.3 可用性要求
- **離線支援**：基本功能可離線使用、自動同步機制
- **錯誤復原**：友善錯誤訊息、自動重試、替代方案
- **無障礙支援**：Screen Reader支援、鍵盤導航、對比度符合標準

### 1.6 與其他功能群的協作

#### 1.6.1 功能群介面
- **→ 記帳核心功能群**：完成認證後直接進入記帳主頁（S-202）
- **→ 系統功能群**：模式設定同步至個人設定頁（S-505）
- **↔ LINE OA平台**：跨平台綁定與資料同步

#### 1.6.2 資料流向
```
進入階段 → 認證完成 → 模式設定 → 主要功能群
    ↓
本地儲存 ← API同步 ← 雲端資料
```

---

## 2.0 功能需求（Functional Requirements）

### 2.1 新增帳號
**對應畫面**：S-103 APP註冊頁
**開發優先級**：P1（Phase 1 必須完成）
**API對應**：8101 認證服務 - POST /auth/register

#### 2.1.1 基本功能需求
- **FR-201-001**：提供Email地址註冊輸入框與格式驗證
- **FR-201-002**：提供密碼設定輸入框與強度檢查（至少8位數、含大小寫字母與數字）
- **FR-201-003**：提供密碼確認輸入框與一致性驗證
- **FR-201-004**：整合Google OAuth第三方註冊選項
- **FR-201-005**：註冊成功後自動導向模式評估頁面進行四模式設定

#### 2.1.2 四模式差異化需求
- **Expert模式**：提供完整註冊選項、進階安全設定、批次匯入功能
- **Inertial模式**：固定步驟流程、一致性介面佈局、可預測操作
- **Cultivation模式**：引導式流程、進度追蹤、激勵性訊息
- **Guiding模式**：極簡流程、自動填入、一鍵完成

### 2.2 帳號登入
**對應畫面**：S-104 APP登入頁
**開發優先級**：P1（Phase 1 必須完成）
**API對應**：8101 認證服務 - POST /auth/login

#### 2.2.1 基本功能需求
- **FR-202-001**：提供Email/密碼登入表單與驗證
- **FR-202-002**：整合Google OAuth第三方登入選項
- **FR-202-003**：提供「記住我」選項與自動登入功能
- **FR-202-004**：登入成功後根據用戶模式導向對應的主要功能頁面
- **FR-202-005**：提供忘記密碼連結導向重設頁面

#### 2.2.2 四模式差異化需求
- **Expert模式**：顯示登入歷史、安全狀態、多重驗證選項
- **Inertial模式**：保持一致的登入流程與介面配置
- **Cultivation模式**：顯示連續登入天數、成就提醒、激勵訊息
- **Guiding模式**：簡化登入選項、智慧記憶、快速進入

### 2.3 帳號修改
**對應畫面**：S-105 忘記密碼頁、跨功能群個人設定
**開發優先級**：P1（Phase 1 必須完成）
**API對應**：8101 認證服務 - POST /auth/forgot-password, /auth/reset-password

#### 2.3.1 密碼重設功能需求
- **FR-203-001**：提供Email輸入框發送重設連結
- **FR-203-002**：顯示重設郵件發送狀態與確認訊息
- **FR-203-003**：處理重設連結點擊與密碼更新流程
- **FR-203-004**：新密碼設定與強度驗證機制

#### 2.3.2 帳號資料修改需求
- **FR-203-005**：個人資料編輯（顯示名稱、語言偏好、時區設定）
- **FR-203-006**：四模式切換與重新評估功能
- **FR-203-007**：帳號安全設定（密碼修改、兩步驟驗證）

### 2.4 帳號查詢
**對應畫面**：跨功能群個人資料顯示
**開發優先級**：P1（Phase 1 必須完成）
**API對應**：8102 用戶管理服務 - GET /users/profile

#### 2.4.1 基本功能需求
- **FR-204-001**：顯示當前用戶基本資料（Email、顯示名稱、註冊日期）
- **FR-204-002**：顯示當前用戶模式設定與特性說明
- **FR-204-003**：顯示帳號使用統計（登入次數、使用天數等）
- **FR-204-004**：顯示綁定的第三方帳號狀態（Google、LINE）

#### 2.4.2 四模式差異化顯示
- **Expert模式**：完整的帳號詳細資訊、安全日誌、系統狀態
- **Inertial模式**：標準化的資料展示格式、清晰的資訊層次
- **Cultivation模式**：成就進度、使用統計、成長軌跡視覺化
- **Guiding模式**：簡化的核心資訊、重點功能快捷入口

### 2.5 跨平台帳號整合
**對應畫面**：S-107 跨平台綁定頁
**開發優先級**：P1（Phase 1 必須完成）
**API對應**：8101 認證服務 - POST /auth/bind-line

#### 2.5.1 LINE OA綁定功能需求
- **FR-205-001**：顯示LINE OA介紹與功能說明
- **FR-205-002**：提供QR Code掃描功能加入LINE OA
- **FR-205-003**：檢測LINE OA綁定狀態與帳號確認
- **FR-205-004**：完成綁定後同步用戶資料與設定
- **FR-205-005**：提供跳過綁定選項與後續設定提醒

#### 2.5.2 Google帳號整合需求
- **FR-205-006**：Google OAuth認證與帳號綁定
- **FR-205-007**：Google帳號解除綁定功能
- **FR-205-008**：多平台帳號資料同步機制

### 2.6 用戶科目初始化管理
**對應畫面**：S-106 模式評估問卷頁、初次設定流程
**開發優先級**：P1（Phase 1 必須完成）
**API對應**：8102 用戶管理服務 - POST /users/assessment

#### 2.6.1 模式評估與初始化需求
- **FR-206-001**：提供5題標準化評估問卷介面
- **FR-206-002**：根據評估結果推薦最適合的使用模式
- **FR-206-003**：提供四模式詳細說明與比較資訊
- **FR-206-004**：支援手動模式選擇與確認機制
- **FR-206-005**：完成評估後保存模式設定至用戶資料

#### 2.6.2 初始科目設定需求
- **FR-206-006**：根據選定模式提供對應的預設科目結構
- **FR-206-007**：允許用戶自訂初始科目分類與名稱
- **FR-206-008**：提供常用科目模板供快速選擇

### 2.7 系統啟動與推廣功能
**對應畫面**：S-102 APP啟動頁、S-108 APP下載推廣頁
**開發優先級**：P1（Phase 1 必須完成）
**API對應**：8111 系統服務 - GET /system/app-info

#### 2.7.1 APP啟動功能需求
- **FR-207-001**：應用程式啟動時顯示LCAS品牌Logo與載入動畫
- **FR-207-002**：自動檢查應用程式版本，提示更新資訊
- **FR-207-003**：載入用戶認證狀態，判斷是否需要登入
- **FR-207-004**：初始化四模式設定，載入用戶偏好配置
- **FR-207-005**：檢查網路連線狀態，處理離線模式切換

#### 2.7.2 APP推廣展示需求
- **FR-207-006**：展示APP核心功能與四模式特色
- **FR-207-007**：提供APP Store與Google Play下載連結
- **FR-207-008**：顯示用戶評價與使用統計資訊
- **FR-207-009**：提供社群分享功能與推薦機制

### 2.8 錯誤處理與重試機制
**跨畫面通用功能**
**開發優先級**：P1（Phase 1 必須完成）
**錯誤規範**：8088 API設計規範

#### 2.8.1 基本功能需求
- **FR-208-001**：網路連線中斷時提供離線模式與重試機制
- **FR-208-002**：API調用失敗時顯示友善錯誤訊息與建議
- **FR-208-003**：輸入驗證錯誤時提供即時回饋與修正建議
- **FR-208-004**：系統異常時提供錯誤回報與客服聯繫方式

#### 2.8.2 帳號相關錯誤處理
- **FR-208-005**：註冊/登入失敗的分類錯誤處理與引導
- **FR-208-006**：密碼重設流程的異常狀況處理
- **FR-208-007**：跨平台綁定失敗的錯誤恢復機制
- **FR-208-008**：模式評估過程的中斷恢復功能

---

## 3.0 輸入/輸出規格（Input & Output Specifications）

### 3.1 輸入

#### 3.1.1 註冊功能輸入規格（S-103對應POST /auth/register）

**基於8101認證服務API規格**：
```yaml
email:
  type: string
  format: email
  required: true
  max_length: 255
  example: "user@example.com"
  description: "使用者 Email 地址"

password:
  type: string
  minLength: 8
  required: true
  example: "SecurePassword123"
  description: "密碼，至少 8 個字元"

confirmPassword:
  type: string
  required: true  # 非Guiding模式必填
  example: "SecurePassword123"
  description: "確認密碼（非 Guiding 模式必填）"

displayName:
  type: string
  minLength: 2
  maxLength: 50
  required: true  # 非Guiding模式必填
  example: "張小明"
  description: "顯示名稱（非 Guiding 模式必填）"

userMode:
  type: string
  enum: [Expert, Inertial, Cultivation, Guiding]
  required: true
  example: "Expert"
  description: "使用者模式"

acceptTerms:
  type: boolean
  required: true
  example: true
  description: "同意服務條款"

acceptPrivacy:
  type: boolean
  required: false
  example: true
  description: "同意隱私政策"

timezone:
  type: string
  required: false
  example: "Asia/Taipei"
  description: "時區設定"

language:
  type: string
  required: false
  example: "zh-TW"
  description: "語言偏好"
```

#### 3.1.2 登入功能輸入規格（S-104對應POST /auth/login）

**基於8101認證服務API規格**：
```yaml
email:
  type: string
  format: email
  required: true
  example: "user@example.com"

password:
  type: string
  required: true
  example: "SecurePassword123"

rememberMe:
  type: boolean
  required: false
  default: false
  example: true
  description: "記住登入狀態"

deviceInfo:
  type: object
  required: false
  properties:
    deviceId:
      type: string
      example: "device-uuid"
    platform:
      type: string
      enum: [iOS, Android, Web]
      example: "iOS"
    appVersion:
      type: string
      example: "1.0.0"
```

#### 3.1.3 密碼重設輸入規格（S-105對應POST /auth/forgot-password）

**基於8101認證服務API規格**：
```yaml
forgot_password_input:
  email:
    type: string
    format: email
    required: true
    example: "user@example.com"

reset_password_input:
  token:
    type: string
    required: true
    example: "reset-token-string"
    description: "密碼重設 Token"
  newPassword:
    type: string
    minLength: 8
    required: true
    example: "NewSecurePassword123"
    description: "新密碼"
  confirmPassword:
    type: string
    required: true
    example: "NewSecurePassword123"
    description: "確認新密碼"
```

#### 3.1.4 模式評估輸入規格（S-106對應POST /users/assessment）

**基於8102用戶管理服務API規格**：
```yaml
assessment_input:
  answers:
    type: array
    required: true
    items:
      type: object
      properties:
        questionId:
          type: integer
          range: [1, 5]
        answer:
          type: string
          enum: ["A", "B", "C", "D"]
    example:
      - questionId: 1
        answer: "A"
      - questionId: 2
        answer: "C"
```

#### 3.1.5 跨平台綁定輸入規格（S-107對應POST /auth/bind-line）

**基於8101認證服務API規格**：
```yaml
line_binding_input:
  lineUserId:
    type: string
    required: true
    example: "U1234567890abcdef"
    description: "LINE 使用者 ID"
  lineAccessToken:
    type: string
    required: true
    example: "line-access-token"
    description: "LINE 存取 Token"
  lineProfile:
    type: object
    required: false
    properties:
      displayName:
        type: string
        example: "LINE 使用者"
      pictureUrl:
        type: string
        format: uri
        example: "https://profile.line-scdn.net/..."
```

### 3.2 輸出

#### 3.2.1 註冊功能輸出規格（S-103）

**成功回應（HTTP 201）基於8101 API規格**：
```yaml
registration_success:
  success:
    type: boolean
    example: true
  data:
    type: object
    properties:
      userId:
        type: string
        example: "uuid-string"
        description: "使用者 ID"
      email:
        type: string
        example: "user@example.com"
      userMode:
        type: string
        example: "Expert"
      verificationSent:
        type: boolean
        example: true
        description: "是否已發送驗證信"
      needsAssessment:
        type: boolean
        example: false
        description: "是否需要模式評估"
      token:
        type: string
        example: "jwt-token-string"
        description: "存取 Token"
      refreshToken:
        type: string
        example: "refresh-token-string"
        description: "刷新 Token"
      expiresAt:
        type: string
        format: date-time
        example: "2025-01-28T00:00:00Z"
        description: "Token 過期時間"
  metadata:
    type: object
    required:
      - timestamp
      - requestId
      - userMode
    properties:
      timestamp:
        type: string
        format: date-time
        example: "2025-01-27T12:00:00Z"
      requestId:
        type: string
        example: "req-uuid-string"
      userMode:
        type: string
        enum: [Expert, Inertial, Cultivation, Guiding]
        example: "Expert"
```

#### 3.2.2 登入功能輸出規格（S-104）

**成功回應（HTTP 200）基於8101 API規格四模式差異化**：
```yaml
login_success_expert:
  success:
    type: boolean
    example: true
  data:
    token:
      type: string
      example: "jwt-token"
    refreshToken:
      type: string
      example: "refresh-jwt-token"
    expiresAt:
      type: string
      format: date-time
      example: "2025-01-28T00:00:00Z"
    user:
      type: object
      # UserProfile schema from 8101
    loginHistory:
      type: object
      description: "Expert 模式專用"
      properties:
        lastLogin:
          type: string
          format: date-time
        loginCount:
          type: integer
        newDeviceDetected:
          type: boolean
  metadata:
    timestamp:
      type: string
      format: date-time
    requestId:
      type: string
    userMode:
      type: string
      example: "Expert"

login_success_cultivation:
  # 相同基本結構，但增加激勵資訊
  data:
    # ... 基本欄位
    streakInfo:
      type: object
      description: "Cultivation 模式專用"
      properties:
        currentStreak:
          type: integer
        longestStreak:
          type: integer
        streakMessage:
          type: string
```

#### 3.2.3 模式評估輸出規格（S-106）

**基於8102用戶管理服務API規格**：
```yaml
assessment_result:
  success:
    type: boolean
    example: true
  data:
    recommendedMode:
      type: string
      enum: [Expert, Inertial, Cultivation, Guiding]
      example: "Expert"
    confidence:
      type: integer
      range: [0, 100]
      example: 85
      description: "推薦信心度"
    explanation:
      type: string
      example: "基於您的回答，建議使用專家模式以獲得完整功能體驗"
    modeCharacteristics:
      type: object
      properties:
        Expert:
          type: string
          example: "完整功能、專業工具、深度設定"
        alternative:
          type: string
          example: "Inertial"
  metadata:
    timestamp:
      type: string
      format: date-time
    requestId:
      type: string
    userMode:
      type: string
```

#### 3.2.4 跨平台綁定輸出規格（S-107）

**基於8101認證服務API規格**：
```yaml
binding_success:
  success:
    type: boolean
    example: true
  data:
    message:
      type: string
      example: "LINE 帳號綁定成功"
    linkedAccounts:
      type: object
      properties:
        email:
          type: string
          example: "user@example.com"
        line:
          type: string
          example: "U1234567890abcdef"
        bindingDate:
          type: string
          format: date-time
          example: "2025-01-27T12:00:00Z"
  metadata:
    timestamp:
      type: string
      format: date-time
    requestId:
      type: string
    userMode:
      type: string
```

#### 3.2.5 系統服務輸出規格

**APP啟動頁面輸出（S-102對應GET /system/app-info）**：
```yaml
app_info_response:
  success:
    type: boolean
    example: true
  data:
    version:
      type: string
      example: "1.0.0"
    minVersion:
      type: string
      example: "1.0.0"
    forceUpdate:
      type: boolean
      example: false
    features:
      type: object
      description: "功能開關"
    maintenanceMode:
      type: boolean
      example: false
  metadata:
    timestamp:
      type: string
      format: date-time
    requestId:
      type: string
    userMode:
      type: string
```

---

## 4.0 錯誤處理與例外狀況（Error Handling & Edge Cases）

### 4.1 認證相關錯誤（基於8101 API規格）

#### 4.1.1 註冊錯誤處理（POST /auth/register）
```yaml
EMAIL_ALREADY_EXISTS:
  http_status: 409
  code: "EMAIL_ALREADY_EXISTS"
  message: "此電子郵件已被註冊，請使用其他信箱或直接登入"
  field: "email"
  ui_action: "導向登入頁面"
  retry_allowed: false
  
VALIDATION_ERROR:
  http_status: 400
  code: "VALIDATION_ERROR" 
  message: "密碼長度不足，請輸入至少8位數"
  field: "password"
  ui_action: "重新輸入密碼"
  retry_allowed: true
  details:
    validation:
      - field: "password"
        message: "密碼長度不足"
        code: "PASSWORD_TOO_SHORT"
```

#### 4.1.2 登入錯誤處理（POST /auth/login）
```yaml
INVALID_CREDENTIALS:
  http_status: 401
  code: "INVALID_CREDENTIALS"
  message: "帳號或密碼錯誤，請重新輸入"
  field: null
  ui_action: "重新輸入或重設密碼"
  retry_allowed: true
  max_attempts: 5
  
ACCOUNT_LOCKED:
  http_status: 423
  code: "ACCOUNT_LOCKED"
  message: "帳號因多次登入失敗被暫時鎖定"
  field: null
  ui_action: "聯繫客服或等待解鎖"
  retry_allowed: false
  unlock_at: "2025-01-27T14:00:00Z"
  
EMAIL_NOT_VERIFIED:
  http_status: 401
  code: "EMAIL_NOT_VERIFIED"
  message: "電子郵件尚未驗證，請先完成郵件驗證"
  field: "email"
  ui_action: "重新發送驗證郵件"
  retry_allowed: true
```

#### 4.1.3 密碼重設錯誤處理（POST /auth/forgot-password & /auth/reset-password）
```yaml
EMAIL_NOT_FOUND:
  http_status: 404
  code: "EMAIL_NOT_FOUND"
  message: "找不到此電子郵件地址"
  field: "email"
  ui_action: "確認郵件地址或註冊新帳號"
  retry_allowed: true
  
INVALID_RESET_TOKEN:
  http_status: 400
  code: "INVALID_RESET_TOKEN"
  message: "重設連結無效或已過期"
  field: "token"
  ui_action: "重新申請密碼重設"
  retry_allowed: true
```

#### 4.1.4 Google OAuth錯誤處理（POST /auth/google-login）
```yaml
GOOGLE_AUTH_FAILED:
  http_status: 401
  code: "GOOGLE_AUTH_FAILED"
  message: "Google 認證失敗，請重新嘗試"
  field: "googleToken"
  ui_action: "重新進行Google登入"
  retry_allowed: true
```

#### 4.1.5 跨平台綁定錯誤處理（POST /auth/bind-line）
```yaml
LINE_ACCOUNT_ALREADY_BOUND:
  http_status: 409
  code: "LINE_ACCOUNT_ALREADY_BOUND"
  message: "此LINE帳號已被其他使用者綁定"
  field: "lineUserId"
  ui_action: "使用其他LINE帳號或解除原綁定"
  retry_allowed: false
  
INVALID_LINE_TOKEN:
  http_status: 401
  code: "INVALID_LINE_TOKEN"
  message: "LINE存取權杖無效或已過期"
  field: "lineAccessToken"
  ui_action: "重新授權LINE帳號"
  retry_allowed: true
```

### 4.2 模式評估錯誤（基於8102 API規格）

#### 4.2.1 評估問卷錯誤處理（POST /users/assessment）
```yaml
INVALID_QUESTIONNAIRE_ID:
  http_status: 400
  code: "INVALID_QUESTIONNAIRE_ID"
  message: "問卷版本無效或已過期"
  field: "questionnaireId"
  ui_action: "重新載入問卷"
  retry_allowed: true
  
INCOMPLETE_ANSWERS:
  http_status: 400
  code: "VALIDATION_ERROR"
  message: "請完成所有必填題目"
  field: "answers"
  ui_action: "檢查並完成未答題目"
  retry_allowed: true
  details:
    validation:
      - field: "answers[2]"
        message: "第3題為必填"
        code: "REQUIRED_FIELD"
```

### 4.3 網路連線錯誤（基於8088規範）

#### 4.3.1 連線中斷處理
```yaml
CONNECTION_TIMEOUT:
  http_status: null  # 網路層錯誤
  code: "NETWORK_TIMEOUT"
  message: "網路連線逾時，請檢查您的網路設定"
  ui_action: "重試連線或啟用離線模式"
  retry_allowed: true
  retry_delay: 3000  # 3秒後重試
  max_retries: 3
  
NO_INTERNET_CONNECTION:
  http_status: null
  code: "NO_INTERNET"
  message: "無法連接網路，某些功能將暫時無法使用"
  ui_action: "啟用離線模式或檢查網路"
  offline_mode_available: true
  
DNS_RESOLUTION_FAILED:
  http_status: null
  code: "DNS_ERROR"
  message: "無法解析伺服器位址"
  ui_action: "檢查DNS設定或稍後再試"
  retry_allowed: true
```

### 4.4 伺服器錯誤（基於8088規範）

#### 4.4.1 API伺服器錯誤
```yaml
INTERNAL_SERVER_ERROR:
  http_status: 500
  code: "INTERNAL_SERVER_ERROR"
  message: "伺服器暫時無法處理請求，請稍後再試"
  ui_action: "自動重試或聯繫客服"
  retry_allowed: true
  retry_strategy: "exponential_backoff"
  
SERVICE_UNAVAILABLE:
  http_status: 503
  code: "SERVICE_UNAVAILABLE"
  message: "服務暫時維護中，預計30分鐘後恢復"
  ui_action: "等待服務恢復或查看系統公告"
  retry_allowed: false
  estimated_recovery: "2025-01-27T14:30:00Z"
  
RATE_LIMIT_EXCEEDED:
  http_status: 429
  code: "RATE_LIMIT_EXCEEDED"
  message: "請求頻率過高，請稍後再試"
  ui_action: "降低操作頻率或稍後重試"
  retry_allowed: true
  reset_time: "2025-01-27T13:01:00Z"
```

### 4.5 業務邏輯錯誤（基於8088規範）

#### 4.5.1 業務規則違反
```yaml
BUSINESS_LOGIC_ERROR:
  http_status: 422
  code: "BUSINESS_LOGIC_ERROR"
  message: "操作違反業務規則"
  ui_action: "檢查操作條件後重試"
  retry_allowed: true
  
MODE_SWITCH_TOO_FREQUENT:
  http_status: 422
  code: "MODE_SWITCH_LIMIT"
  message: "模式切換過於頻繁，請24小時後再試"
  field: "userMode"
  ui_action: "等待冷卻時間結束"
  retry_allowed: false
  cooldown_until: "2025-01-28T12:00:00Z"
```

### 4.6 輸入驗證錯誤（統一格式）

#### 4.6.1 表單驗證錯誤
```yaml
VALIDATION_ERROR:
  http_status: 400
  code: "VALIDATION_ERROR"
  message: "請求參數驗證失敗"
  ui_action: "修正輸入內容"
  retry_allowed: true
  details:
    validation:
      - field: "email"
        message: "電子郵件格式不正確"
        code: "INVALID_EMAIL_FORMAT"
        value: "invalid-email"
      - field: "password"
        message: "密碼長度不足"
        code: "PASSWORD_TOO_SHORT"
        value: "123"
```

### 4.7 裝置與系統錯誤

#### 4.7.1 裝置相關錯誤
```yaml
INSUFFICIENT_STORAGE:
  code: "DEVICE_STORAGE_FULL"
  message: "裝置儲存空間不足，請清理後重試"
  ui_action: "清理裝置空間或移除快取"
  retry_allowed: true
  
BIOMETRIC_NOT_AVAILABLE:
  code: "BIOMETRIC_UNAVAILABLE"
  message: "生物辨識功能不可用"
  ui_action: "使用PIN碼或密碼登入"
  fallback_available: true
  
CAMERA_PERMISSION_DENIED:
  code: "PERMISSION_DENIED"
  message: "需要相機權限以掃描QR Code"
  ui_action: "前往設定開啟相機權限"
  permission_required: "camera"
```

### 4.8 錯誤回饋與復原機制

#### 4.8.1 自動重試策略
```yaml
retry_strategies:
  immediate:
    delay: 0
    max_attempts: 1
    applicable_errors: ["VALIDATION_ERROR"]
    
  linear_backoff:
    initial_delay: 1000
    increment: 1000
    max_attempts: 3
    applicable_errors: ["NETWORK_TIMEOUT"]
    
  exponential_backoff:
    initial_delay: 1000
    multiplier: 2
    max_delay: 30000
    max_attempts: 5
    applicable_errors: ["INTERNAL_SERVER_ERROR"]
```

#### 4.8.2 離線模式支援
```yaml
offline_capabilities:
  available_features:
    - "APP啟動與初始化"
    - "已快取的模式設定"
    - "本地驗證（PIN碼）"
    
  unavailable_features:
    - "註冊新帳號"
    - "模式評估問卷"
    - "跨平台綁定"
    - "密碼重設"
    
  sync_strategy: "background_sync_when_online"
```

#### 4.8.3 錯誤報告機制
```yaml
error_reporting:
  automatic_report:
    - "INTERNAL_SERVER_ERROR"
    - "UNEXPECTED_ERROR"
    - "SERVICE_UNAVAILABLE"
    
  user_report_option:
    - "BUSINESS_LOGIC_ERROR"
    - "NETWORK_TIMEOUT"
    
  include_data:
    - device_info
    - app_version
    - user_mode
    - error_context
    
  exclude_data:
    - passwords
    - tokens
    - personal_data
```
