你是一位資深系統分析師，請根據以下需求，為專案 {{PROJECT_NAME}}、模組 {{MODULE_NAME}} 生成「資料契約文件」與檢查用文件。

需求重點：
1. 建立「資料契約表」（Data Contract Sheet）
   - 用表格列出每個欄位：欄位中文名稱、英文代碼、資料類型（文字/數字/日期/布林）、格式限制（如 YYYY-MM-DD、小數點2位）、是否必填、範例值。

2. 建立「欄位檢查清單」（Checklists）
   - 列出 SIT 測試時人工檢查要逐一確認的規則，例如「日期格式必須符合 YYYY-MM-DD」、「金額不可為負數」、「幣別必須是 ISO-4217 三碼」。
   - 用清單形式呈現。

3. 提供「範例資料集」
   - 生成一組合法的測試資料（JSON 或 CSV），完全符合契約表。
   - 另生成一組常見錯誤資料（日期格式錯誤、金額為負數、缺少必填欄位），方便 SIT 測試。

4. 加入「版本管理說明」
   - 契約文件需有版本號（v1.0 起），並在文件裡加上「版本號、更新日期、更新內容」欄位。

5. 設計「錯誤回報模版」
   - 提供一個表格，包含：欄位名稱、實際值、契約規範、錯誤描述、回報人、責任 Owner。

請用 Markdown 格式輸出，並依照以上 5 個步驟依序完成。
