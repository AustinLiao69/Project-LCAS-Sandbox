
# 7201. 系統進入功能群_LLD.md

**文件編號**: 7201  
**版本**: v1.0.0  
**建立日期**: 2025-09-11  
**建立者**: LCAS SA Team  
**最後更新**: 2025-09-11  

---

## 📑 目次 (Table of Contents)

1. [架構設計 (Architecture Design)](#10-架構設計architecture-design)
2. [Widget結構設計 (Widget Structure Design)](#20-widget結構設計widget-structure-design)
3. [狀態管理設計 (State Management Design)](#30-狀態管理設計state-management-design)
4. [導航流程設計 (Navigation Flow Design)](#40-導航流程設計navigation-flow-design)
5. [API集成設計 (API Integration Design)](#50-api集成設計api-integration-design)
6. [函數介面規格 (Function Interface Specifications)](#60-函數介面規格function-interface-specifications)

---

## 1.0 架構設計（Architecture Design）

### 1.1 模組架構概覽

系統進入功能群採用Flutter的分層架構設計，嚴格遵循PL層職責範圍：

```
系統進入功能群架構
├── Presentation Layer (PL)
│   ├── Pages/               # 頁面組件
│   │   ├── SplashPage       # S-102 APP啟動頁
│   │   ├── RegisterPage     # S-103 APP註冊頁
│   │   ├── LoginPage        # S-104 APP登入頁
│   │   ├── ForgotPasswordPage # S-105 忘記密碼頁
│   │   ├── ModeAssessmentPage # S-106 模式評估問卷頁
│   │   ├── PlatformBindingPage # S-107 跨平台綁定頁
│   │   └── AppPromotionPage   # S-108 APP下載推廣頁
│   ├── Widgets/             # 共用組件
│   │   ├── AuthForm         # 認證表單組件
│   │   ├── ModeSelector     # 模式選擇組件
│   │   ├── ProgressIndicator # 進度指示組件
│   │   └── ErrorDisplay     # 錯誤顯示組件
│   ├── Providers/           # 狀態管理
│   │   ├── AuthProvider     # 認證狀態
│   │   ├── ModeProvider     # 模式狀態
│   │   └── OnboardingProvider # 引導狀態
│   ├── Services/            # API客戶端
│   │   ├── AuthApiClient    # 8101認證服務客戶端
│   │   ├── UserApiClient    # 8102用戶管理服務客戶端
│   │   └── SystemApiClient  # 8111系統服務客戶端
│   └── Utils/               # 工具類
│       ├── ModeTheme        # 四模式主題
│       ├── Validators       # 輸入驗證
│       └── Constants        # 常數定義
```

### 1.2 四模式適配架構

基於0015文件的四模式理論，實現動態UI適配：

```dart
abstract class ModeAdaptiveWidget extends StatelessWidget {
  final UserMode userMode;
  
  const ModeAdaptiveWidget({required this.userMode});
  
  Widget buildExpertMode(BuildContext context);
  Widget buildInertialMode(BuildContext context);
  Widget buildCultivationMode(BuildContext context);
  Widget buildGuidingMode(BuildContext context);
}
```

---

## 2.0 Widget結構設計（Widget Structure Design）

### 2.1 S-102 APP啟動頁Widget結構

```dart
SplashPage
├── SplashScreen
│   ├── BrandLogo           # LCAS品牌Logo
│   ├── LoadingIndicator    # 載入動畫
│   ├── VersionInfo         # 版本資訊
│   └── StatusMessage       # 狀態訊息
└── InitializationHandler   # 初始化處理
```

### 2.2 S-103 APP註冊頁Widget結構

```dart
RegisterPage
├── ModeAdaptiveHeader      # 四模式差異化標題
├── RegisterForm
│   ├── EmailField          # Email輸入框
│   ├── PasswordField       # 密碼輸入框
│   ├── ConfirmPasswordField # 確認密碼輸入框
│   └── ValidationMessages  # 驗證訊息
├── SocialLoginSection
│   └── GoogleLoginButton   # Google登入按鈕
├── TermsAcceptance         # 條款同意
└── SubmitButton           # 註冊按鈕
```

### 2.3 S-104 APP登入頁Widget結構

```dart
LoginPage
├── ModeAdaptiveHeader      # 四模式差異化標題
├── LoginForm
│   ├── EmailField          # Email輸入框
│   ├── PasswordField       # 密碼輸入框
│   ├── RememberMeCheckbox  # 記住我選項
│   └── ValidationMessages  # 驗證訊息
├── SocialLoginSection
│   └── GoogleLoginButton   # Google登入按鈕
├── ForgotPasswordLink      # 忘記密碼連結
├── RegisterLink           # 註冊連結
└── SubmitButton           # 登入按鈕
```

### 2.4 S-105 忘記密碼頁Widget結構

```dart
ForgotPasswordPage
├── ModeAdaptiveHeader      # 四模式差異化標題
├── ResetRequestForm
│   ├── EmailField          # Email輸入框
│   └── ValidationMessages  # 驗證訊息
├── StatusDisplay          # 發送狀態顯示
├── BackToLoginLink        # 返回登入連結
└── SubmitButton           # 發送重設連結按鈕
```

### 2.5 S-106 模式評估問卷頁Widget結構

```dart
ModeAssessmentPage
├── AssessmentHeader        # 評估說明標題
├── QuestionnaireForm
│   ├── Question1Widget     # 第1題
│   ├── Question2Widget     # 第2題
│   ├── Question3Widget     # 第3題
│   ├── Question4Widget     # 第4題
│   └── Question5Widget     # 第5題
├── ProgressIndicator       # 進度指示器
├── RecommendationDisplay   # 推薦結果顯示
│   ├── ModeDescription     # 模式說明
│   ├── ConfidenceScore     # 信心分數
│   └── ModeComparison      # 模式比較
├── ManualModeSelector      # 手動模式選擇
└── ConfirmButton          # 確認選擇按鈕
```

### 2.6 S-107 跨平台綁定頁Widget結構

```dart
PlatformBindingPage
├── BindingHeader           # 綁定說明標題
├── LineIntroduction        # LINE OA介紹
├── QRCodeSection
│   ├── QRCodeDisplay       # QR Code顯示
│   ├── ScanButton          # 掃描按鈕
│   └── ScanInstructions    # 掃描說明
├── BindingStatus          # 綁定狀態
├── LinkedAccountsList     # 已連結帳號清單
├── SkipButton            # 跳過綁定按鈕
└── NextButton            # 下一步按鈕
```

### 2.7 S-108 APP下載推廣頁Widget結構

```dart
AppPromotionPage
├── PromotionHeader         # 推廣標題
├── FeatureShowcase         # 功能展示
│   ├── CoreFeatures        # 核心功能
│   ├── ModeFeatures        # 四模式特色
│   └── BenefitsList        # 優勢清單
├── DownloadSection
│   ├── AppStoreButton      # App Store下載
│   ├── GooglePlayButton    # Google Play下載
│   └── QRCodeDownload      # QR Code下載
├── ReviewsSection          # 用戶評價
├── SocialShareSection     # 社群分享
└── GetStartedButton       # 開始使用按鈕
```

---

## 3.0 狀態管理設計（State Management Design）

### 3.1 認證狀態管理（AuthProvider）

```dart
class AuthState {
  final bool isAuthenticated;
  final User? currentUser;
  final String? token;
  final AuthStatus status;
  final String? errorMessage;
}

enum AuthStatus {
  initial,
  loading,
  authenticated,
  unauthenticated,
  error
}
```

### 3.2 模式評估狀態管理（ModeProvider）

```dart
class ModeAssessmentState {
  final List<AssessmentAnswer> answers;
  final int currentQuestionIndex;
  final UserMode? recommendedMode;
  final double? confidence;
  final AssessmentStatus status;
  final String? errorMessage;
}

enum AssessmentStatus {
  initial,
  inProgress,
  completed,
  modeSelected,
  error
}
```

### 3.3 引導流程狀態管理（OnboardingProvider）

```dart
class OnboardingState {
  final OnboardingStep currentStep;
  final Map<OnboardingStep, bool> completedSteps;
  final bool isFirstTime;
  final OnboardingStatus status;
}

enum OnboardingStep {
  splash,
  authentication,
  modeAssessment,
  platformBinding,
  completed
}
```

---

## 4.0 導航流程設計（Navigation Flow Design）

### 4.1 主要導航流程

```dart
enum AppRoute {
  splash('/'),
  register('/register'),
  login('/login'),
  forgotPassword('/forgot-password'),
  modeAssessment('/mode-assessment'),
  platformBinding('/platform-binding'),
  appPromotion('/app-promotion'),
  main('/main');
}
```

### 4.2 導航決策邏輯

```dart
class NavigationDecisionLogic {
  static AppRoute determineInitialRoute(
    bool isAuthenticated,
    bool hasCompletedAssessment,
    bool hasCompletedBinding,
  ) {
    if (!isAuthenticated) return AppRoute.login;
    if (!hasCompletedAssessment) return AppRoute.modeAssessment;
    if (!hasCompletedBinding) return AppRoute.platformBinding;
    return AppRoute.main;
  }
}
```

---

## 5.0 API集成設計（API Integration Design）

### 5.1 認證服務API客戶端（AuthApiClient）

基於81號資料夾 → 8101認證服務API規格

```dart
class AuthApiClient {
  final String baseUrl;
  final Dio httpClient;
  
  AuthApiClient({required this.baseUrl, required this.httpClient});
}
```

### 5.2 用戶管理服務API客戶端（UserApiClient）

基於81號資料夾 → 8102用戶管理服務API規格

```dart
class UserApiClient {
  final String baseUrl;
  final Dio httpClient;
  
  UserApiClient({required this.baseUrl, required this.httpClient});
}
```

### 5.3 系統服務API客戶端（SystemApiClient）

基於81號資料夾 → 8111系統服務API規格

```dart
class SystemApiClient {
  final String baseUrl;
  final Dio httpClient;
  
  SystemApiClient({required this.baseUrl, required this.httpClient});
}
```

---

## 6.0 函數介面規格（Function Interface Specifications）

### 6.1 APP啟動與初始化函數

#### 6.1.1 應用程式初始化

```dart
/**
 * 01. 初始化應用程式
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> initializeApp();
```

#### 6.1.2 版本檢查

```dart
/**
 * 02. 檢查應用程式版本
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<AppVersionInfo> checkAppVersion();
```

#### 6.1.3 載入用戶認證狀態

```dart
/**
 * 03. 載入用戶認證狀態
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<AuthState> loadAuthenticationState();
```

#### 6.1.4 初始化四模式設定

```dart
/**
 * 04. 初始化四模式設定
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<ModeConfiguration> initializeModeConfiguration();
```

### 6.2 使用者註冊函數

#### 6.2.1 Email註冊

```dart
/**
 * 05. 使用Email註冊帳號
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<RegisterResponse> registerWithEmail(RegisterRequest request);
```

#### 6.2.2 Google OAuth註冊

```dart
/**
 * 06. 使用Google帳號註冊
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<RegisterResponse> registerWithGoogle();
```

#### 6.2.3 Email格式驗證

```dart
/**
 * 07. 驗證Email格式
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
bool validateEmailFormat(String email);
```

#### 6.2.4 密碼強度檢查

```dart
/**
 * 08. 檢查密碼強度
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
PasswordStrength checkPasswordStrength(String password);
```

### 6.3 使用者登入函數

#### 6.3.1 Email登入

```dart
/**
 * 09. 使用Email登入
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<LoginResponse> loginWithEmail(LoginRequest request);
```

#### 6.3.2 Google OAuth登入

```dart
/**
 * 10. 使用Google帳號登入
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<LoginResponse> loginWithGoogle();
```

#### 6.3.3 自動登入檢查

```dart
/**
 * 11. 檢查自動登入狀態
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<bool> checkAutoLoginStatus();
```

#### 6.3.4 保存登入狀態

```dart
/**
 * 12. 保存登入狀態
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> saveLoginState(String token, bool rememberMe);
```

### 6.4 密碼重設函數

#### 6.4.1 發送重設連結

```dart
/**
 * 13. 發送密碼重設連結
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<ForgotPasswordResponse> sendPasswordResetLink(String email);
```

#### 6.4.2 驗證重設Token

```dart
/**
 * 14. 驗證密碼重設Token
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<bool> validateResetToken(String token);
```

#### 6.4.3 重設密碼

```dart
/**
 * 15. 重設密碼
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<ResetPasswordResponse> resetPassword(ResetPasswordRequest request);
```

### 6.5 模式評估函數

#### 6.5.1 載入評估問卷

```dart
/**
 * 16. 載入模式評估問卷
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<AssessmentQuestionnaire> loadAssessmentQuestionnaire();
```

#### 6.5.2 提交評估答案

```dart
/**
 * 17. 提交評估答案
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<AssessmentResult> submitAssessmentAnswers(List<AssessmentAnswer> answers);
```

#### 6.5.3 計算模式推薦

```dart
/**
 * 18. 計算模式推薦
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
ModeRecommendation calculateModeRecommendation(List<AssessmentAnswer> answers);
```

#### 6.5.4 保存模式設定

```dart
/**
 * 19. 保存使用者模式設定
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> saveModeConfiguration(UserMode selectedMode);
```

### 6.6 跨平台綁定函數

#### 6.6.1 生成綁定QR Code

```dart
/**
 * 20. 生成LINE綁定QR Code
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<String> generateLineBindingQRCode();
```

#### 6.6.2 執行平台綁定

```dart
/**
 * 21. 執行LINE平台綁定
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<PlatformBindingResponse> bindLinePlatform(BindingRequest request);
```

#### 6.6.3 檢查綁定狀態

```dart
/**
 * 22. 檢查平台綁定狀態
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<BindingStatus> checkPlatformBindingStatus();
```

#### 6.6.4 同步綁定資料

```dart
/**
 * 23. 同步跨平台資料
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> syncCrossPlatformData();
```

### 6.7 APP推廣展示函數

#### 6.7.1 載入功能展示內容

```dart
/**
 * 24. 載入APP功能展示內容
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<FeatureShowcaseContent> loadFeatureShowcaseContent();
```

#### 6.7.2 生成下載連結

```dart
/**
 * 25. 生成APP下載連結
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Map<String, String> generateDownloadLinks();
```

#### 6.7.3 記錄推廣互動

```dart
/**
 * 26. 記錄推廣頁面互動
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> trackPromotionInteraction(PromotionEvent event);
```

### 6.8 四模式主題函數

#### 6.8.1 載入模式主題

```dart
/**
 * 27. 載入使用者模式主題
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
ThemeData loadModeTheme(UserMode userMode);
```

#### 6.8.2 切換模式主題

```dart
/**
 * 28. 切換使用者模式主題
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> switchModeTheme(UserMode newMode);
```

#### 6.8.3 獲取模式顏色

```dart
/**
 * 29. 獲取模式專用顏色
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Color getModeColor(UserMode userMode);
```

### 6.9 錯誤處理函數

#### 6.9.1 處理網路錯誤

```dart
/**
 * 30. 處理網路連線錯誤
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
void handleNetworkError(NetworkException exception);
```

#### 6.9.2 處理認證錯誤

```dart
/**
 * 31. 處理認證相關錯誤
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
void handleAuthenticationError(AuthException exception);
```

#### 6.9.3 顯示錯誤訊息

```dart
/**
 * 32. 顯示使用者友善錯誤訊息
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
void showUserFriendlyError(String errorMessage, ErrorType errorType);
```

### 6.10 本地儲存函數

#### 6.10.1 保存認證Token

```dart
/**
 * 33. 安全保存認證Token
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> saveAuthToken(String token);
```

#### 6.10.2 讀取認證Token

```dart
/**
 * 34. 讀取已保存的認證Token
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<String?> getAuthToken();
```

#### 6.10.3 清除本地資料

```dart
/**
 * 35. 清除所有本地儲存資料
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> clearLocalStorage();
```

### 6.11 導航控制函數

#### 6.11.1 導航至目標頁面

```dart
/**
 * 36. 導航至指定頁面
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> navigateToPage(AppRoute route, {Map<String, dynamic>? arguments});
```

#### 6.11.2 替換當前頁面

```dart
/**
 * 37. 替換當前頁面
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
Future<void> replaceCurrentPage(AppRoute route, {Map<String, dynamic>? arguments});
```

#### 6.11.3 返回上一頁

```dart
/**
 * 38. 返回上一頁面
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
void navigateBack({dynamic result});
```

### 6.12 表單驗證函數

#### 6.12.1 即時表單驗證

```dart
/**
 * 39. 執行即時表單驗證
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
ValidationResult validateFormField(String fieldName, String value);
```

#### 6.12.2 完整表單驗證

```dart
/**
 * 40. 執行完整表單驗證
 * @version 2025-09-11-V1.0.0
 * @date 2025-09-11
 * @update: 初始版本
 */
FormValidationResult validateCompleteForm(Map<String, String> formData);
```

---

**文件結束**

**版本控制記錄**：
- v1.0.0 (2025-09-11): 初始版本，完整的系統進入功能群LLD設計
